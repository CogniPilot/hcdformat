# GENERATED by generate_hcdfdom.py from hcdf.xsd -- DO NOT EDIT.
# Regenerate: python3 generate_hcdfdom.py hcdf.xsd hcdfdom/model.py
"""Typed HCDF DOM, generated from hcdf.xsd (the single source of truth)."""
from __future__ import annotations

import copy
from dataclasses import dataclass, field
from enum import Enum
from typing import Any, List, Optional

from lxml import etree


def _children(el):
    return [c for c in el if isinstance(c.tag, str)]


def _ln(c):
    return etree.QName(c.tag).localname

class AntennaType(str, Enum):

    """Wireless antenna technology type. wifi: IEEE 802.11 (any band). bluetooth: Bluetooth Classic or BLE. lora: LoRa/LoRaWAN. cellular: 4G/5G cellular. uwb: Ultra-Wideband (IEEE 802.15.4z). gnss: GNSS reception (GPS, Galileo, GLONASS, BeiDou). NFC: Near Field Communication. 802.15.4: IEEE 802.15.4 (Zigbee, Thread)."""

    wifi = "wifi"

    bluetooth = "bluetooth"

    lora = "lora"

    cellular = "cellular"

    uwb = "uwb"

    gnss = "gnss"

    NFC = "NFC"

    V_802_15_4 = "802.15.4"



class AudioSensorType(str, Enum):

    """Audio/acoustic sensor sub-type. microphone: audio capture for speech, sound localization, acoustic monitoring. ultrasonic: ultrasonic transducer for proximity sensing and ranging (typically 40kHz). sonar: underwater acoustic ranging and imaging. hydrophone: underwater passive acoustic sensor for listening."""

    microphone = "microphone"

    ultrasonic = "ultrasonic"

    sonar = "sonar"

    hydrophone = "hydrophone"



class AxisValue(str, Enum):

    """Sensor axis alignment mapping. Maps a sensor's internal axis to the component's body frame axis. Supports positive and negative directions: X, -X, Y, -Y, Z, -Z. Used to correct for sensors mounted in non-standard orientations."""

    X = "X"

    _X = "-X"

    Y = "Y"

    _Y = "-Y"

    Z = "Z"

    _Z = "-Z"



class BodyFrame(str, Enum):

    """Body-frame axis convention describing how the robot's local coordinate axes are oriented relative to its physical body. The three-letter code specifies the direction each axis points: first letter = X, second = Y, third = Z. F=Forward, B=Back, L=Left, R=Right, U=Up, D=Down. FLU (X-forward, Y-left, Z-up): common in ground robotics, humanoids, and wheeled platforms. Positive X faces the direction the robot drives or walks. FRD (X-forward, Y-right, Z-down): common in aerospace, aviation, and flight"""

    FLU = "FLU"

    FRD = "FRD"



class BusParticipantRole(str, Enum):

    """Role of a device on a shared bus. controller: bus master that initiates transactions (e.g., I2C controller, SPI controller, CAN node with scheduling authority). target: responds to controller requests (e.g., I2C target, SPI target). source: produces data onto the bus (e.g., sensor streaming). sink: consumes data from the bus (e.g., actuator receiving commands)."""

    controller = "controller"

    target = "target"

    source = "source"

    sink = "sink"



class BusTopology(str, Enum):

    """Physical wiring topology of a shared bus. daisy-chain: devices wired in series. star: devices wired to a central hub. multidrop: devices tapped along a common wire (e.g., CAN, 10BASE-T1S)."""

    daisy_chain = "daisy-chain"

    star = "star"

    multidrop = "multidrop"



class BusType(str, Enum):

    """Shared-medium bus protocol type. CAN: Controller Area Network (CAN 2.0B, CAN-FD, CAN-XL). UART: multi-drop serial (STS servo, Dynamixel, RS-485 multidrop). I2C: multi-target I2C bus. SPI: SPI with multiple chip-selects on shared MISO/MOSI/CLK. ethernet: 10BASE-T1S multidrop Ethernet (802.3cg PLCA) — the only Ethernet variant that is a true shared bus. For point-to-point Ethernet use link; for daisy-chain bridged Ethernet use chain. power: power distribution bus (shared voltage rail)."""

    CAN = "CAN"

    UART = "UART"

    I2C = "I2C"

    SPI = "SPI"

    ethernet = "ethernet"

    power = "power"



class ChainMode(str, Enum):

    """Forwarding mode for a daisy-chain. switched: each hop is a store-and-forward bridge (e.g., TSN Ethernet). pass-through: frames are processed on-the-fly by each device's hardware without store-and-forward buffering (e.g., EtherCAT)."""

    switched = "switched"

    pass_through = "pass-through"



class ChainProtocol(str, Enum):

    """Protocol/PHY standard for a bridged daisy-chain. 802.3dm: IEEE 802.3dm multi-drop Ethernet for automotive/robotics. 100base-t1: 100 Mbit/s single-pair Ethernet (IEEE 802.3bw). 1000base-t1: 1 Gbit/s single-pair Ethernet (IEEE 802.3bp). 10gbase-t1: 10 Gbit/s single-pair Ethernet. ethercat: EtherCAT (IEC 61158). profinet: PROFINET IRT. sercos: SERCOS III."""

    V_802_3dm = "802.3dm"

    V_100base_t1 = "100base-t1"

    V_1000base_t1 = "1000base-t1"

    V_10gbase_t1 = "10gbase-t1"

    ethercat = "ethercat"

    profinet = "profinet"

    sercos = "sercos"



class ChainTopology(str, Enum):

    """Physical topology of a daisy-chain. line: open-ended chain from first hop to last. ring: last hop connects back to first hop for redundancy (requires port-primary and port-secondary on hops)."""

    line = "line"

    ring = "ring"



class ChemicalSensorType(str, Enum):

    """Chemical/environmental sensor sub-type. gas: gas concentration sensor (CO2, VOC, methane, etc.). ph: pH sensor for liquid acidity measurement. humidity: relative humidity sensor."""

    gas = "gas"

    ph = "ph"

    humidity = "humidity"



class CompRole(str, Enum):

    """Functional role of a component in the robot. sensor: sensing-only device (e.g., camera module, IMU board). compute: processing unit (e.g., SoC, SBC, FPGA). actuator: motor driver or actuator assembly. parent: structural/mechanical parent that groups child components (e.g., chassis, arm segment)."""

    sensor = "sensor"

    compute = "compute"

    actuator = "actuator"

    parent = "parent"



class ConfidentialityOffset(str, Enum):

    """MACsec confidentiality offset in bytes per IEEE 802.1AE. Specifies how many bytes of payload after the SecTAG remain unencrypted. 0: encrypt entire payload (maximum security). 30: leave first 30 bytes unencrypted (exposes L3/L4 headers for QoS classification). 50: leave first 50 bytes unencrypted (exposes more header fields)."""

    V_0 = "0"

    V_30 = "30"

    V_50 = "50"



class DcMode(str, Enum):

    """EtherCAT Distributed Clock mode for a hop. free-run: no clock synchronization (device uses local oscillator). sync0: synchronized to SYNC0 pulse from the DC reference clock. sync0-sync1: synchronized to both SYNC0 and SYNC1 pulses for multi-rate processing."""

    free_run = "free-run"

    sync0 = "sync0"

    sync0_sync1 = "sync0-sync1"



class EeePolicyValue(str, Enum):

    """IEEE 802.3az Energy Efficient Ethernet policy for the network. enabled: allow ports to enter Low Power Idle when no traffic (saves power, adds wake latency). disabled: ports remain active at all times (deterministic latency, higher power). Typically disabled for TSN networks where latency guarantees are critical."""

    enabled = "enabled"

    disabled = "disabled"



class EmSensorType(str, Enum):

    """Near-field electromagnetic sensor sub-type. Measures static or quasi-static EM fields — NOT propagating radio waves (those are rf). mag: magnetometer (measures Earth's magnetic field or local fields). metal_detector: inductive proximity sensing. eddy_current: non-destructive testing via induced currents. emf: electromotive force measurement. hall-effect: Hall effect proximity/position sensor. fluxgate: fluxgate magnetometer (high-precision compass)."""

    mag = "mag"

    metal_detector = "metal_detector"

    eddy_current = "eddy_current"

    emf = "emf"

    hall_effect = "hall-effect"

    fluxgate = "fluxgate"



class EncoderPrinciple(str, Enum):

    """Physical sensing principle of the encoder. optical: light source + photodetector + code disk (e.g., US Digital, Broadcom AEDR). magnetic: Hall effect or magnetoresistive element + rotating magnet (e.g., AS5048A, AS5600). inductive: resolver, electromagnetic induction between coils. capacitive: capacitance change between rotating plates. resistive: potentiometer, resistance proportional to position."""

    optical = "optical"

    magnetic = "magnetic"

    inductive = "inductive"

    capacitive = "capacitive"

    resistive = "resistive"



class EncoderSensorType(str, Enum):

    """Encoder measurement type. incremental: relative position from index/home, requires homing. absolute: absolute position within one revolution (single-turn) or multiple revolutions (multi-turn). linear: linear position encoder (e.g., optical strip, magnetic linear)."""

    incremental = "incremental"

    absolute = "absolute"

    linear = "linear"



class ForceSensorType(str, Enum):

    """Force/torque sensor sub-type. strain: strain gauge for deformation measurement. pressure: pressure transducer (barometric or contact). torque: rotary torque sensor. load_cell: load cell for weight/force measurement."""

    strain = "strain"

    pressure = "pressure"

    torque = "torque"

    load_cell = "load_cell"



class FrustumShape(str, Enum):

    """Cross-section shape of a frustum used for sensor field-of-view visualization."""

    conical = "conical"

    pyramidal = "pyramidal"



class HmiType(str, Enum):

    """Human-Machine Interface element type. display: visual output (LCD, OLED, LED matrix, e-ink). speaker: audio output (speaker, buzzer, piezo). led-status: status indicator LED(s) (on/off, color). led-illumination: scene illumination (headlights, work lights, IR illumination for night vision cameras). button: physical pushbutton or switch input. touchscreen: combined display + touch input. indicator: generic visual/audio indicator."""

    display = "display"

    speaker = "speaker"

    buzzer = "buzzer"

    led_status = "led-status"

    led_illumination = "led-illumination"

    button = "button"

    touchscreen = "touchscreen"

    indicator = "indicator"



class HopRole(str, Enum):

    """Role of a device in a daisy-chain. root: chain controller / EtherCAT master (first hop, initiates frames). master: intermediate forwarding hop. tail: last hop in the chain (for line topology, frames turn around here)."""

    root = "root"

    master = "master"

    tail = "tail"



class InertialSensorType(str, Enum):

    """Inertial sensor sub-type. accel: accelerometer only (3-axis linear acceleration). gyro: gyroscope only (3-axis angular rate). accel_gyro: combined IMU with both accelerometer and gyroscope (6-axis)."""

    accel = "accel"

    gyro = "gyro"

    accel_gyro = "accel_gyro"



class JointType(str, Enum):

    """Enumeration of kinematic joint types. revolute: bounded single-axis rotation. continuous: unbounded single-axis rotation. prismatic: bounded linear translation (sliding). fixed: rigid (zero DOF). ball: 3-DOF spherical. universal: 2-DOF (requires axis + axis2). planar: 2-DOF translation on a plane. screw: helical (requires thread_pitch). cylindrical: 2-DOF combined rotation and translation on the same axis (like a piston that can also rotate — requires axis, limit defines both rotation and transl"""

    revolute = "revolute"

    continuous = "continuous"

    prismatic = "prismatic"

    fixed = "fixed"

    ball = "ball"

    universal = "universal"

    planar = "planar"

    screw = "screw"

    cylindrical = "cylindrical"

    free = "free"



class KeyAgreement(str, Enum):

    """MACsec key agreement protocol. psk: Pre-Shared Key (simpler, manual key distribution). eap-tls: EAP-TLS via IEEE 802.1X (certificate-based, suitable for scalable deployments)."""

    psk = "psk"

    eap_tls = "eap-tls"



class KeyStoreType(str, Enum):

    """Credential storage backend for MACsec key material. secure-element: hardware secure element (e.g., ATECC608). hsm: Hardware Security Module. tpm: Trusted Platform Module (TPM 2.0). file: filesystem-based key storage (development only). runtime: keys provisioned at runtime by an external agent."""

    secure_element = "secure-element"

    hsm = "hsm"

    tpm = "tpm"

    file = "file"

    runtime = "runtime"



class MacsecCipher(str, Enum):

    """MACsec cipher suite per IEEE 802.1AE. gcm-aes-128: GCM-AES-128 (default, widely supported). gcm-aes-256: GCM-AES-256. gcm-aes-xpn-128: Extended Packet Numbering with AES-128 (for long-lived associations). gcm-aes-xpn-256: Extended Packet Numbering with AES-256."""

    gcm_aes_128 = "gcm-aes-128"

    gcm_aes_256 = "gcm-aes-256"

    gcm_aes_xpn_128 = "gcm-aes-xpn-128"

    gcm_aes_xpn_256 = "gcm-aes-xpn-256"



class MacsecPolicy(str, Enum):

    """IEEE 802.1AE MACsec enforcement policy. must-secure: drop all frames that are not MACsec-protected (strict security). should-secure: prefer MACsec but accept unprotected frames (transitional). integrity-only: authenticate frames via ICV but do not encrypt payload. disabled: no MACsec processing."""

    must_secure = "must-secure"

    should_secure = "should-secure"

    integrity_only = "integrity-only"

    disabled = "disabled"



class MeshRole(str, Enum):

    """Role of a device in a wireless mesh network."""

    coordinator = "coordinator"

    border_router = "border-router"

    router = "router"

    end_device = "end-device"



class MotorType(str, Enum):

    """Motor or actuator technology type. bldc: brushless DC motor. brushed: brushed DC motor. stepper: stepper motor. servo: hobby/industrial servo. linear: linear actuator (proportional control over stroke length). solenoid: electromagnetic on/off linear actuator (binary pull/push action with no proportional position control — used for valves, latches, locks, pin releases, landing gear, and gripper release mechanisms). hydraulic: hydraulic actuator. pneumatic: pneumatic actuator. thrust: thrust motor"""

    bldc = "bldc"

    brushed = "brushed"

    stepper = "stepper"

    servo = "servo"

    linear = "linear"

    solenoid = "solenoid"

    hydraulic = "hydraulic"

    pneumatic = "pneumatic"

    thrust = "thrust"

    ice = "ice"



class NameOrigin(str, Enum):

    """Provenance of a visual/collision name: "authored" = meaningful, human/source-supplied (round-trips, emitted to URDF where the URDF schema allows it); "synthesized" = generated by a converter to satisfy HCDF's required-name rule (stripped on URDF export so the output stays byte-clean). Absence is interpreted as "authored" by the companion validator."""

    authored = "authored"

    synthesized = "synthesized"



class NoiseType(str, Enum):

    """Noise distribution model for sensor simulation. gaussian: Gaussian (normal) distribution with mean and stddev. uniform: uniform distribution over a range. none: no noise applied (ideal sensor)."""

    gaussian = "gaussian"

    uniform = "uniform"

    none = "none"



class OpticalSensorType(str, Enum):

    """Optical sensor sub-type. camera: visible-spectrum image sensor (RGB, mono, stereo, depth). thermal: infrared thermal camera (LWIR/MWIR). lidar: Light Detection and Ranging (spinning or solid-state). tof: Time-of-Flight depth sensor. optical_flow: optical flow sensor for velocity estimation."""

    camera = "camera"

    thermal = "thermal"

    lidar = "lidar"

    tof = "tof"

    optical_flow = "optical_flow"



class PhyRole(str, Enum):

    """PHY master/slave role for point-to-point single-pair Ethernet links (100BASE-T1, 1000BASE-T1, 802.3dm). Semantic rule: each T1 link requires exactly one master and one slave. auto: device negotiates role at link-up."""

    master = "master"

    slave = "slave"

    auto = "auto"



class PortType(str, Enum):

    """Physical wired port interface type. Includes data buses, camera/sensor serial links, debug interfaces, general-purpose signal connectors, and power. SIGNAL is for mixed-signal connectors carrying any combination of analog/digital IO, encoder, Hall, thermistor, etc. — use when the connector doesn't fit a standard bus type. Camera interfaces (MIPI-CSI, GMSL2, ASA-ML, FPD-Link, CoaXPress) are local to the SoC and do NOT traverse the TSN network."""

    ethernet = "ethernet"

    SPI = "SPI"

    I2C = "I2C"

    UART = "UART"

    CAN = "CAN"

    USB = "USB"

    MIPI_CSI = "MIPI-CSI"

    GMSL = "GMSL"

    GMSL2 = "GMSL2"

    ASA_ML = "ASA-ML"

    FPD_Link = "FPD-Link"

    CoaXPress = "CoaXPress"

    JTAG = "JTAG"

    SWD = "SWD"

    SIGNAL = "SIGNAL"

    POWER = "POWER"

    CARD = "CARD"



class PreemptionClass(str, Enum):

    """IEEE 802.1Qbu frame preemption classification. express: frames in this traffic class cannot be preempted (high priority, latency-sensitive). preemptable: frames can be interrupted mid-transmission to allow express traffic through."""

    express = "express"

    preemptable = "preemptable"



class RadiationSensorType(str, Enum):

    """Radiation sensor sub-type for nuclear inspection, environmental monitoring, and space robotics. geiger: Geiger-Muller tube for ionizing radiation counting (alpha, beta, gamma). scintillation: scintillation detector for gamma spectroscopy (higher energy resolution than Geiger). neutron: neutron detector for nuclear material detection. dosimeter: cumulative radiation dose measurement."""

    geiger = "geiger"

    scintillation = "scintillation"

    neutron = "neutron"

    dosimeter = "dosimeter"



class RfSensorType(str, Enum):

    """Radio-frequency sensor sub-type. Uses propagating radio waves for ranging, positioning, or imaging — distinct from em which measures static/quasi-static fields. gnss: satellite navigation receiver. uwb: Ultra-Wideband ranging. radar: radio detection and ranging (24/77/79 GHz). radio_altimeter: radio altitude measurement."""

    gnss = "gnss"

    uwb = "uwb"

    radar = "radar"

    radio_altimeter = "radio_altimeter"



class TactileSensorType(str, Enum):

    """Tactile sensor sub-type. Tactile sensors measure contact, pressure, or deformation distributed across a surface area, as distinct from force sensors which measure at a single point. capacitive: measures pressure via capacitance change between conductive layers — high sensitivity, good spatial resolution, common in robot fingertips and e-skin (e.g., PPS TakkTile, SynTouch BioTac). resistive: force-sensitive resistor (FSR) arrays or piezoresistive films — lower cost, simpler electronics, used in g"""

    capacitive = "capacitive"

    resistive = "resistive"

    piezoelectric = "piezoelectric"

    barometric = "barometric"

    optical = "optical"



class TemperatureSensorType(str, Enum):

    """Temperature sensor type. thermistor: NTC or PTC resistive element (common in motors for winding temp). rtd: Resistance Temperature Detector (PT100/PT1000, high accuracy). thermocouple: K/J/T/E type junction (wide range, industrial). ir: non-contact infrared temperature sensor."""

    thermistor = "thermistor"

    rtd = "rtd"

    thermocouple = "thermocouple"

    ir = "ir"



class TransmissionType(str, Enum):

    """Mechanical power transmission mechanism between motor and joint. simple: direct drive or single-stage reduction. differential: differential gear mechanism (couples two motors to two outputs). belt: belt/pulley transmission. gear: spur or helical gear train. planetary: planetary (epicyclic) gear set (compact, high ratio). cycloidal: cycloidal drive (high ratio, zero backlash, high shock load capacity — used in robot joints). harmonic: harmonic drive / strain-wave gearing (very high ratio in compa"""

    simple = "simple"

    differential = "differential"

    belt = "belt"

    gear = "gear"

    planetary = "planetary"

    cycloidal = "cycloidal"

    harmonic = "harmonic"

    strain_wave = "strain-wave"

    worm = "worm"



class TunnelProtocol(str, Enum):

    """Encapsulation protocol for tunneling one chain's traffic over another network. etg-1700: EtherCAT-over-EtherCAT tunneling (ETG.1700 specification). ieee-1722: IEEE 1722 AVTP encapsulation (used for EtherCAT-over-TSN). raw: raw Ethernet frame encapsulation with no additional headers."""

    etg_1700 = "etg-1700"

    ieee_1722 = "ieee-1722"

    raw = "raw"



class WorldFrame(str, Enum):

    """World-frame (navigation frame) axis convention describing how global/map coordinate axes are oriented. ENU (X-east, Y-north, Z-up): common in geographic information systems, ground robotics, and surveying. Z-up aligns with gravity opposition, making vertical reasoning intuitive. NED (X-north, Y-east, Z-down): common in aerospace, aviation, marine navigation, and most inertial navigation systems. Z-down aligns with gravity direction, matching accelerometer sign conventions in flight controllers. """

    ENU = "ENU"

    NED = "NED"



@dataclass
class AerofoilSurface:
    """Aerodynamic lifting surface (wing, horizontal stabilizer, canard). Generates lift and drag from airflow over an airfoil cross-section. Key parameters determine flight performance: lift coefficient, stall characteristics, and induced drag."""
    span: Optional[Any] = None
    chord: Optional[Any] = None
    profile: Optional[Any] = None
    sweep: Optional[Any] = None
    dihedral: Optional[Any] = None
    area: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "span"), None)
        self.span = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "chord"), None)
        self.chord = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "profile"), None)
        self.profile = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "sweep"), None)
        self.sweep = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "dihedral"), None)
        self.dihedral = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "area"), None)
        self.area = MeasuredValue.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.span is not None:
            el.append(self.span.to_xml("span"))
        if self.chord is not None:
            el.append(self.chord.to_xml("chord"))
        if self.profile is not None:
            _e = etree.SubElement(el, "profile"); _e.text = self.profile
        if self.sweep is not None:
            el.append(self.sweep.to_xml("sweep"))
        if self.dihedral is not None:
            el.append(self.dihedral.to_xml("dihedral"))
        if self.area is not None:
            el.append(self.area.to_xml("area"))
        return el

@dataclass
class Antenna:
    """Wireless radio antenna on a component. Used for WiFi, Bluetooth, LoRa, cellular, UWB, GNSS, NFC, and IEEE 802.15.4 connections. Antennas are referenced by name in wireless link definitions."""
    capabilities: Optional[Any] = None
    pose: Optional[Any] = None
    geometry: Optional[Any] = None
    name: Optional[Any] = None
    type: Optional[Any] = None
    visual: Optional[Any] = None
    mesh: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "capabilities"), None)
        self.capabilities = AntennaCapabilities.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        _v = el.get("type")
        self.type = AntennaType(_v) if _v is not None else None
        self.visual = el.get("visual")
        self.mesh = el.get("mesh")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.visual is not None:
            el.set("visual", self.visual)
        if self.mesh is not None:
            el.set("mesh", self.mesh)
        if self.capabilities is not None:
            el.append(self.capabilities.to_xml("capabilities"))
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class AntennaCapabilities:
    """Wireless antenna hardware capabilities. Declares supported frequency bands, wireless standards, protocols, gain, and polarization."""
    band: List[Any] = field(default_factory=list)
    standard: List[Any] = field(default_factory=list)
    protocol: List[Any] = field(default_factory=list)
    gain: Optional[Any] = None
    polarization: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.band = [_c.text for _c in _children(el) if _ln(_c) == "band"]
        self.standard = [_c.text for _c in _children(el) if _ln(_c) == "standard"]
        self.protocol = [_c.text for _c in _children(el) if _ln(_c) == "protocol"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "gain"), None)
        self.gain = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "polarization"), None)
        self.polarization = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        for _i in self.band:
            _e = etree.SubElement(el, "band")
            if _i is not None: _e.text = _i
        for _i in self.standard:
            _e = etree.SubElement(el, "standard")
            if _i is not None: _e.text = _i
        for _i in self.protocol:
            _e = etree.SubElement(el, "protocol")
            if _i is not None: _e.text = _i
        if self.gain is not None:
            el.append(self.gain.to_xml("gain"))
        if self.polarization is not None:
            _e = etree.SubElement(el, "polarization"); _e.text = self.polarization
        return el

@dataclass
class AudioSensor:
    """Audio/acoustic sensor for sound capture, ultrasonic ranging, and underwater acoustics. Can be a passive receiver (microphone, hydrophone) or an active transducer (ultrasonic, sonar) that emits and receives. Extends sensor_base with audio type attribute and optional frequency/directivity parameters."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    frequency_range: Optional[Any] = None
    sensitivity: Optional[Any] = None
    channels: Optional[Any] = None
    beam_width: Optional[Any] = None
    type: Optional[Any] = None
    active: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "frequency-range"), None)
        self.frequency_range = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "sensitivity"), None)
        self.sensitivity = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "channels"), None)
        self.channels = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "beam-width"), None)
        self.beam_width = MeasuredValue.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = AudioSensorType(_v) if _v is not None else None
        self.active = el.get("active")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.active is not None:
            el.set("active", self.active)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        if self.frequency_range is not None:
            el.append(self.frequency_range.to_xml("frequency-range"))
        if self.sensitivity is not None:
            el.append(self.sensitivity.to_xml("sensitivity"))
        if self.channels is not None:
            _e = etree.SubElement(el, "channels"); _e.text = self.channels
        if self.beam_width is not None:
            el.append(self.beam_width.to_xml("beam-width"))
        return el

@dataclass
class Axis:
    """Joint axis of rotation or translation. Defines the direction vector along or around which the joint moves, expressed in the joint's local frame."""
    xyz: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.xyz = el.get("xyz")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.xyz is not None:
            el.set("xyz", self.xyz)
        return el

@dataclass
class AxisAlign:
    """Axis alignment mapping from a sensor's internal coordinate frame to the component's body frame. Used to correct for sensors mounted in non-standard orientations without changing the pose."""
    x: Optional[Any] = None
    y: Optional[Any] = None
    z: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _v = el.get("x")
        self.x = AxisValue(_v) if _v is not None else None
        _v = el.get("y")
        self.y = AxisValue(_v) if _v is not None else None
        _v = el.get("z")
        self.z = AxisValue(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.x is not None:
            el.set("x", self.x.value)
        if self.y is not None:
            el.set("y", self.y.value)
        if self.z is not None:
            el.set("z", self.z.value)
        return el

@dataclass
class BatterySource:
    """Rechargeable battery energy source. Defines cell configuration, chemistry, capacity, and discharge limits. An agent uses this to compute runtime (energy / total power consumption) and validate that bus voltage stays within all consumers' operating range throughout discharge."""
    chemistry: Optional[Any] = None
    cells_series: Optional[Any] = None
    cells_parallel: Optional[Any] = None
    nominal_voltage: Optional[Any] = None
    capacity: Optional[Any] = None
    energy: Optional[Any] = None
    max_discharge: Optional[Any] = None
    min_voltage: Optional[Any] = None
    max_voltage: Optional[Any] = None
    max_charge_rate: Optional[Any] = None
    charge_protocol: Optional[Any] = None
    cycle_life: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "chemistry"), None)
        self.chemistry = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cells-series"), None)
        self.cells_series = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cells-parallel"), None)
        self.cells_parallel = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "nominal-voltage"), None)
        self.nominal_voltage = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "capacity"), None)
        self.capacity = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "energy"), None)
        self.energy = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-discharge"), None)
        self.max_discharge = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "min-voltage"), None)
        self.min_voltage = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-voltage"), None)
        self.max_voltage = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-charge-rate"), None)
        self.max_charge_rate = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "charge-protocol"), None)
        self.charge_protocol = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cycle-life"), None)
        self.cycle_life = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.chemistry is not None:
            _e = etree.SubElement(el, "chemistry"); _e.text = self.chemistry
        if self.cells_series is not None:
            _e = etree.SubElement(el, "cells-series"); _e.text = self.cells_series
        if self.cells_parallel is not None:
            _e = etree.SubElement(el, "cells-parallel"); _e.text = self.cells_parallel
        if self.nominal_voltage is not None:
            el.append(self.nominal_voltage.to_xml("nominal-voltage"))
        if self.capacity is not None:
            el.append(self.capacity.to_xml("capacity"))
        if self.energy is not None:
            el.append(self.energy.to_xml("energy"))
        if self.max_discharge is not None:
            el.append(self.max_discharge.to_xml("max-discharge"))
        if self.min_voltage is not None:
            el.append(self.min_voltage.to_xml("min-voltage"))
        if self.max_voltage is not None:
            el.append(self.max_voltage.to_xml("max-voltage"))
        if self.max_charge_rate is not None:
            el.append(self.max_charge_rate.to_xml("max-charge-rate"))
        if self.charge_protocol is not None:
            _e = etree.SubElement(el, "charge-protocol"); _e.text = self.charge_protocol
        if self.cycle_life is not None:
            _e = etree.SubElement(el, "cycle-life"); _e.text = self.cycle_life
        return el

@dataclass
class Box:
    """Axis-aligned box geometry primitive centered at the parent pose origin."""
    size: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "size"), None)
        self.size = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.size is not None:
            _e = etree.SubElement(el, "size"); _e.text = self.size
        return el

@dataclass
class Bus:
    """Shared medium network where all participants are electrically connected and see all traffic simultaneously. No per-hop forwarding — latency is the same for all participants. Use for CAN, I2C, SPI, UART (multi-drop), 10BASE-T1S, servo buses (STS/Dynamixel), and power distribution."""
    description: Optional[Any] = None
    bitrate: Optional[Any] = None
    speed: Optional[Any] = None
    baud: Optional[Any] = None
    standard: Optional[Any] = None
    protocol: List[Any] = field(default_factory=list)
    voltage: Optional[Any] = None
    plca: Optional[Any] = None
    participant: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    type: Optional[Any] = None
    topology: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "bitrate"), None)
        self.bitrate = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "speed"), None)
        self.speed = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "baud"), None)
        self.baud = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "standard"), None)
        self.standard = _c.text if _c is not None else None
        self.protocol = [_c.text for _c in _children(el) if _ln(_c) == "protocol"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "voltage"), None)
        self.voltage = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "plca"), None)
        self.plca = PlcaConfig.from_xml(_c) if _c is not None else None
        self.participant = [BusParticipant.from_xml(_c) for _c in _children(el) if _ln(_c) == "participant"]
        self.name = el.get("name")
        _v = el.get("type")
        self.type = BusType(_v) if _v is not None else None
        _v = el.get("topology")
        self.topology = BusTopology(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.topology is not None:
            el.set("topology", self.topology.value)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.bitrate is not None:
            _e = etree.SubElement(el, "bitrate"); _e.text = self.bitrate
        if self.speed is not None:
            el.append(self.speed.to_xml("speed"))
        if self.baud is not None:
            el.append(self.baud.to_xml("baud"))
        if self.standard is not None:
            _e = etree.SubElement(el, "standard"); _e.text = self.standard
        for _i in self.protocol:
            _e = etree.SubElement(el, "protocol")
            if _i is not None: _e.text = _i
        if self.voltage is not None:
            _e = etree.SubElement(el, "voltage"); _e.text = self.voltage
        if self.plca is not None:
            el.append(self.plca.to_xml("plca"))
        for _i in self.participant:
            el.append(_i.to_xml("participant"))
        return el

@dataclass
class BusParticipant:
    """A device connected to a shared bus. Identifies the component, port, bus address, and role on the bus. Multiple participants share the same physical medium and see all traffic simultaneously."""
    protocol: List[Any] = field(default_factory=list)
    device: Optional[Any] = None
    port: Optional[Any] = None
    position: Optional[Any] = None
    id: Optional[Any] = None
    address: Optional[Any] = None
    role: Optional[Any] = None
    terminator: Optional[Any] = None
    cs: Optional[Any] = None
    plca_id: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.protocol = [_c.text for _c in _children(el) if _ln(_c) == "protocol"]
        self.device = el.get("device")
        self.port = el.get("port")
        self.position = el.get("position")
        self.id = el.get("id")
        self.address = el.get("address")
        _v = el.get("role")
        self.role = BusParticipantRole(_v) if _v is not None else None
        self.terminator = el.get("terminator")
        self.cs = el.get("cs")
        self.plca_id = el.get("plca-id")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.device is not None:
            el.set("device", self.device)
        if self.port is not None:
            el.set("port", self.port)
        if self.position is not None:
            el.set("position", self.position)
        if self.id is not None:
            el.set("id", self.id)
        if self.address is not None:
            el.set("address", self.address)
        if self.role is not None:
            el.set("role", self.role.value)
        if self.terminator is not None:
            el.set("terminator", self.terminator)
        if self.cs is not None:
            el.set("cs", self.cs)
        if self.plca_id is not None:
            el.set("plca-id", self.plca_id)
        for _i in self.protocol:
            _e = etree.SubElement(el, "protocol")
            if _i is not None: _e.text = _i
        return el

@dataclass
class CameraCalibration:
    """Camera calibration results from an actual calibration procedure on a specific unit. Distinct from the design-spec intrinsics/distortion: those are nominal datasheet values, while calibration contains measured values for this particular camera+lens assembly. An agent doing visual servoing, SLAM, or stereo depth must use the calibrated values. If calibration is present, it overrides the design-spec intrinsics and distortion for runtime use."""
    fx: Optional[Any] = None
    fy: Optional[Any] = None
    cx: Optional[Any] = None
    cy: Optional[Any] = None
    k1: Optional[Any] = None
    k2: Optional[Any] = None
    k3: Optional[Any] = None
    p1: Optional[Any] = None
    p2: Optional[Any] = None
    reprojection_error: Optional[Any] = None
    date: Optional[Any] = None
    temperature: Optional[Any] = None
    method: Optional[Any] = None
    images: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "fx"), None)
        self.fx = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "fy"), None)
        self.fy = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cx"), None)
        self.cx = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cy"), None)
        self.cy = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "k1"), None)
        self.k1 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "k2"), None)
        self.k2 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "k3"), None)
        self.k3 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "p1"), None)
        self.p1 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "p2"), None)
        self.p2 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "reprojection-error"), None)
        self.reprojection_error = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "date"), None)
        self.date = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "temperature"), None)
        self.temperature = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "method"), None)
        self.method = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "images"), None)
        self.images = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.fx is not None:
            _e = etree.SubElement(el, "fx"); _e.text = self.fx
        if self.fy is not None:
            _e = etree.SubElement(el, "fy"); _e.text = self.fy
        if self.cx is not None:
            _e = etree.SubElement(el, "cx"); _e.text = self.cx
        if self.cy is not None:
            _e = etree.SubElement(el, "cy"); _e.text = self.cy
        if self.k1 is not None:
            _e = etree.SubElement(el, "k1"); _e.text = self.k1
        if self.k2 is not None:
            _e = etree.SubElement(el, "k2"); _e.text = self.k2
        if self.k3 is not None:
            _e = etree.SubElement(el, "k3"); _e.text = self.k3
        if self.p1 is not None:
            _e = etree.SubElement(el, "p1"); _e.text = self.p1
        if self.p2 is not None:
            _e = etree.SubElement(el, "p2"); _e.text = self.p2
        if self.reprojection_error is not None:
            el.append(self.reprojection_error.to_xml("reprojection-error"))
        if self.date is not None:
            _e = etree.SubElement(el, "date"); _e.text = self.date
        if self.temperature is not None:
            el.append(self.temperature.to_xml("temperature"))
        if self.method is not None:
            _e = etree.SubElement(el, "method"); _e.text = self.method
        if self.images is not None:
            _e = etree.SubElement(el, "images"); _e.text = self.images
        return el

@dataclass
class CameraDistortion:
    """Camera lens distortion parameters following the Brown-Conrady model. Used by vision pipelines to undistort images."""
    k1: Optional[Any] = None
    k2: Optional[Any] = None
    k3: Optional[Any] = None
    p1: Optional[Any] = None
    p2: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "k1"), None)
        self.k1 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "k2"), None)
        self.k2 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "k3"), None)
        self.k3 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "p1"), None)
        self.p1 = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "p2"), None)
        self.p2 = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.k1 is not None:
            _e = etree.SubElement(el, "k1"); _e.text = self.k1
        if self.k2 is not None:
            _e = etree.SubElement(el, "k2"); _e.text = self.k2
        if self.k3 is not None:
            _e = etree.SubElement(el, "k3"); _e.text = self.k3
        if self.p1 is not None:
            _e = etree.SubElement(el, "p1"); _e.text = self.p1
        if self.p2 is not None:
            _e = etree.SubElement(el, "p2"); _e.text = self.p2
        return el

@dataclass
class CameraIntrinsics:
    """Camera intrinsic parameters following the pinhole camera model. Defines image dimensions, pixel format, and focal length/principal point for projection calculations."""
    width: Optional[Any] = None
    height: Optional[Any] = None
    format: Optional[Any] = None
    fx: Optional[Any] = None
    fy: Optional[Any] = None
    cx: Optional[Any] = None
    cy: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "width"), None)
        self.width = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "height"), None)
        self.height = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "format"), None)
        self.format = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "fx"), None)
        self.fx = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "fy"), None)
        self.fy = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cx"), None)
        self.cx = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cy"), None)
        self.cy = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.width is not None:
            _e = etree.SubElement(el, "width"); _e.text = self.width
        if self.height is not None:
            _e = etree.SubElement(el, "height"); _e.text = self.height
        if self.format is not None:
            _e = etree.SubElement(el, "format"); _e.text = self.format
        if self.fx is not None:
            _e = etree.SubElement(el, "fx"); _e.text = self.fx
        if self.fy is not None:
            _e = etree.SubElement(el, "fy"); _e.text = self.fy
        if self.cx is not None:
            _e = etree.SubElement(el, "cx"); _e.text = self.cx
        if self.cy is not None:
            _e = etree.SubElement(el, "cy"); _e.text = self.cy
        return el

@dataclass
class CameraParams:
    """Camera-specific parameters affecting perception pipeline design, bandwidth planning, and environmental capability. Applies to optical sensors with type="camera" or type="thermal"."""
    shutter: Optional[Any] = None
    hdr: Optional[Any] = None
    dynamic_range: Optional[Any] = None
    depth_range: Optional[Any] = None
    compression: Optional[Any] = None
    trigger: Optional[Any] = None
    ir_cut: Optional[Any] = None
    spectral_band: Optional[Any] = None
    netd: Optional[Any] = None
    temperature_range: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "shutter"), None)
        self.shutter = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "hdr"), None)
        self.hdr = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "dynamic-range"), None)
        self.dynamic_range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "depth-range"), None)
        self.depth_range = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "compression"), None)
        self.compression = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "trigger"), None)
        self.trigger = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "ir-cut"), None)
        self.ir_cut = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "spectral-band"), None)
        self.spectral_band = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "netd"), None)
        self.netd = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "temperature-range"), None)
        self.temperature_range = RangeValue.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.shutter is not None:
            _e = etree.SubElement(el, "shutter"); _e.text = self.shutter
        if self.hdr is not None:
            _e = etree.SubElement(el, "hdr"); _e.text = self.hdr
        if self.dynamic_range is not None:
            el.append(self.dynamic_range.to_xml("dynamic-range"))
        if self.depth_range is not None:
            el.append(self.depth_range.to_xml("depth-range"))
        if self.compression is not None:
            _e = etree.SubElement(el, "compression"); _e.text = self.compression
        if self.trigger is not None:
            _e = etree.SubElement(el, "trigger"); _e.text = self.trigger
        if self.ir_cut is not None:
            _e = etree.SubElement(el, "ir-cut"); _e.text = self.ir_cut
        if self.spectral_band is not None:
            _e = etree.SubElement(el, "spectral-band"); _e.text = self.spectral_band
        if self.netd is not None:
            el.append(self.netd.to_xml("netd"))
        if self.temperature_range is not None:
            el.append(self.temperature_range.to_xml("temperature-range"))
        return el

@dataclass
class Capsule:
    """Capsule (cylinder with hemispherical caps) geometry primitive, aligned with the Z axis. Total height = length + 2 * radius."""
    radius: Optional[Any] = None
    length: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "radius"), None)
        self.radius = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "length"), None)
        self.length = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.radius is not None:
            _e = etree.SubElement(el, "radius"); _e.text = self.radius
        if self.length is not None:
            _e = etree.SubElement(el, "length"); _e.text = self.length
        return el

@dataclass
class Chain:
    """Bridged daisy-chain network where each device actively forwards frames to the next hop. Two modes: switched (store-and-forward, e.g., TSN Ethernet with per-hop Qbv scheduling) and pass-through (process-on-the-fly, e.g., EtherCAT where each slave processes frames in hardware as they pass). Hop order defines the physical chain topology. Latency accumulates per hop."""
    description: Optional[Any] = None
    vlan: Optional[Any] = None
    macsec: Optional[Any] = None
    cycle_time_us: Optional[Any] = None
    dc: Optional[Any] = None
    tunnel: Optional[Any] = None
    hop: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    type: Optional[Any] = None
    mode: Optional[Any] = None
    topology: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "vlan"), None)
        self.vlan = Vlan.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "macsec"), None)
        self.macsec = MacsecPolicy_.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cycle-time-us"), None)
        self.cycle_time_us = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "dc"), None)
        self.dc = DcChain.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "tunnel"), None)
        self.tunnel = Tunnel.from_xml(_c) if _c is not None else None
        self.hop = [Hop.from_xml(_c) for _c in _children(el) if _ln(_c) == "hop"]
        self.name = el.get("name")
        _v = el.get("type")
        self.type = ChainProtocol(_v) if _v is not None else None
        _v = el.get("mode")
        self.mode = ChainMode(_v) if _v is not None else None
        _v = el.get("topology")
        self.topology = ChainTopology(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.mode is not None:
            el.set("mode", self.mode.value)
        if self.topology is not None:
            el.set("topology", self.topology.value)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.vlan is not None:
            el.append(self.vlan.to_xml("vlan"))
        if self.macsec is not None:
            el.append(self.macsec.to_xml("macsec"))
        if self.cycle_time_us is not None:
            _e = etree.SubElement(el, "cycle-time-us"); _e.text = self.cycle_time_us
        if self.dc is not None:
            el.append(self.dc.to_xml("dc"))
        if self.tunnel is not None:
            el.append(self.tunnel.to_xml("tunnel"))
        for _i in self.hop:
            el.append(_i.to_xml("hop"))
        return el

@dataclass
class ChemicalSensor:
    """Chemical/environmental sensor (gas detector, pH sensor, humidity sensor). Extends sensor_base with all common elements."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = ChemicalSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class Collision:
    """Collision geometry for physics simulation and proximity detection. Typically a simplified version of the visual geometry (bounding box, convex hull, or low-poly mesh). Multiple collision elements per component are allowed for complex shapes."""
    pose: Optional[Any] = None
    geometry: Optional[Any] = None
    surface: Optional[Any] = None
    name: Optional[Any] = None
    name_origin: Optional[Any] = None
    verbose: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = CollisionGeometry.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "surface"), None)
        self.surface = Surface.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        _v = el.get("name-origin")
        self.name_origin = NameOrigin(_v) if _v is not None else None
        self.verbose = el.get("verbose")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.name_origin is not None:
            el.set("name-origin", self.name_origin.value)
        if self.verbose is not None:
            el.set("verbose", self.verbose)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        if self.surface is not None:
            el.append(self.surface.to_xml("surface"))
        return el

@dataclass
class CollisionGeometry:
    """Collision geometry: primitive shapes (box, cylinder, sphere, capsule, cone, ellipsoid) plus an external <mesh>. Prefer primitives or a convex hull for efficiency; a collision <mesh> should be a LEAN format (STL/OBJ/DAE/convex), NOT a GLB -- collision needs shape, not baked appearance (validator-enforced; URI format is not XSD-expressible). Frustum is excluded (sensor-FOV only). Carries no material (collision has no appearance)."""
    box: Optional[Any] = None
    cylinder: Optional[Any] = None
    sphere: Optional[Any] = None
    capsule: Optional[Any] = None
    cone: Optional[Any] = None
    ellipsoid: Optional[Any] = None
    mesh: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "box"), None)
        self.box = Box.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cylinder"), None)
        self.cylinder = Cylinder.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "sphere"), None)
        self.sphere = Sphere.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "capsule"), None)
        self.capsule = Capsule.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cone"), None)
        self.cone = Cone.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "ellipsoid"), None)
        self.ellipsoid = Ellipsoid.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "mesh"), None)
        self.mesh = Mesh.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.box is not None:
            el.append(self.box.to_xml("box"))
        if self.cylinder is not None:
            el.append(self.cylinder.to_xml("cylinder"))
        if self.sphere is not None:
            el.append(self.sphere.to_xml("sphere"))
        if self.capsule is not None:
            el.append(self.capsule.to_xml("capsule"))
        if self.cone is not None:
            el.append(self.cone.to_xml("cone"))
        if self.ellipsoid is not None:
            el.append(self.ellipsoid.to_xml("ellipsoid"))
        if self.mesh is not None:
            el.append(self.mesh.to_xml("mesh"))
        return el

@dataclass
class CollisionPair:
    """A pair of components to skip for self-collision checking. Adjacent links, links on opposite sides of the robot, and always-in-contact pairs are common candidates."""
    comp1: Optional[Any] = None
    comp2: Optional[Any] = None
    reason: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.comp1 = el.get("comp1")
        self.comp2 = el.get("comp2")
        self.reason = el.get("reason")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.comp1 is not None:
            el.set("comp1", self.comp1)
        if self.comp2 is not None:
            el.set("comp2", self.comp2)
        if self.reason is not None:
            el.set("reason", self.reason)
        return el

@dataclass
class Color:
    """RGBA appearance color. Used three ways: (1) DEFINE a reusable color at document level with @name + @rgba; (2) REFERENCE a document-level color from a visual by @name alone; (3) INLINE a one-off color with @rgba alone. This is HCDF's flat-appearance tier (the URDF-portable color); rich appearance (PBR/textures) is baked into the GLB <model>, never expressed here. There is no separate <material> element -- a color is the appearance unit, and contact physics lives independently on <collision><surfa"""
    name: Optional[Any] = None
    rgba: Optional[Any] = None
    description: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.name = el.get("name")
        self.rgba = el.get("rgba")
        self.description = el.get("description")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.rgba is not None:
            el.set("rgba", self.rgba)
        if self.description is not None:
            el.set("description", self.description)
        return el

@dataclass
class Comp:
    """A component representing a rigid body or hardware assembly. Contains physical properties (inertial, visual, collision), hardware interfaces (ports, antennas, sensors, motors), an optional switch (bridging fabric), firmware identity, and runtime discovery state. Equivalent to URDF's link element but extended with networking and cyber infrastructure."""
    description: Optional[Any] = None
    board: Optional[Any] = None
    operating_temp: Optional[Any] = None
    inertial: Optional[Any] = None
    visual: List[Any] = field(default_factory=list)
    collision: List[Any] = field(default_factory=list)
    frame: List[Any] = field(default_factory=list)
    switch: Optional[Any] = None
    port: List[Any] = field(default_factory=list)
    antenna: List[Any] = field(default_factory=list)
    sensor: List[Any] = field(default_factory=list)
    motor: List[Any] = field(default_factory=list)
    hmi: List[Any] = field(default_factory=list)
    dynamic_surface: List[Any] = field(default_factory=list)
    power_source: List[Any] = field(default_factory=list)
    software: Optional[Any] = None
    discovered: Optional[Any] = None
    urdf_compat: Optional[Any] = None
    extension: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    role: Optional[Any] = None
    hwid: Optional[Any] = None
    struct_type: Optional[Any] = None
    ip_rating: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "board"), None)
        self.board = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "operating-temp"), None)
        self.operating_temp = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "inertial"), None)
        self.inertial = InertialProperties.from_xml(_c) if _c is not None else None
        self.visual = [Visual.from_xml(_c) for _c in _children(el) if _ln(_c) == "visual"]
        self.collision = [Collision.from_xml(_c) for _c in _children(el) if _ln(_c) == "collision"]
        self.frame = [Frame.from_xml(_c) for _c in _children(el) if _ln(_c) == "frame"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "switch"), None)
        self.switch = Switch.from_xml(_c) if _c is not None else None
        self.port = [Port.from_xml(_c) for _c in _children(el) if _ln(_c) == "port"]
        self.antenna = [Antenna.from_xml(_c) for _c in _children(el) if _ln(_c) == "antenna"]
        self.sensor = [Sensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "sensor"]
        self.motor = [Motor.from_xml(_c) for _c in _children(el) if _ln(_c) == "motor"]
        self.hmi = [HmiElement.from_xml(_c) for _c in _children(el) if _ln(_c) == "hmi"]
        self.dynamic_surface = [DynamicSurface.from_xml(_c) for _c in _children(el) if _ln(_c) == "dynamic-surface"]
        self.power_source = [PowerSource.from_xml(_c) for _c in _children(el) if _ln(_c) == "power-source"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "software"), None)
        self.software = Software.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "discovered"), None)
        self.discovered = Discovered.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "urdf-compat"), None)
        self.urdf_compat = UrdfCompatComp.from_xml(_c) if _c is not None else None
        self.extension = [Extension.from_xml(_c) for _c in _children(el) if _ln(_c) == "extension"]
        self.name = el.get("name")
        _v = el.get("role")
        self.role = CompRole(_v) if _v is not None else None
        self.hwid = el.get("hwid")
        self.struct_type = el.get("struct-type")
        self.ip_rating = el.get("ip-rating")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.role is not None:
            el.set("role", self.role.value)
        if self.hwid is not None:
            el.set("hwid", self.hwid)
        if self.struct_type is not None:
            el.set("struct-type", self.struct_type)
        if self.ip_rating is not None:
            el.set("ip-rating", self.ip_rating)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.board is not None:
            _e = etree.SubElement(el, "board"); _e.text = self.board
        if self.operating_temp is not None:
            el.append(self.operating_temp.to_xml("operating-temp"))
        if self.inertial is not None:
            el.append(self.inertial.to_xml("inertial"))
        for _i in self.visual:
            el.append(_i.to_xml("visual"))
        for _i in self.collision:
            el.append(_i.to_xml("collision"))
        for _i in self.frame:
            el.append(_i.to_xml("frame"))
        if self.switch is not None:
            el.append(self.switch.to_xml("switch"))
        for _i in self.port:
            el.append(_i.to_xml("port"))
        for _i in self.antenna:
            el.append(_i.to_xml("antenna"))
        for _i in self.sensor:
            el.append(_i.to_xml("sensor"))
        for _i in self.motor:
            el.append(_i.to_xml("motor"))
        for _i in self.hmi:
            el.append(_i.to_xml("hmi"))
        for _i in self.dynamic_surface:
            el.append(_i.to_xml("dynamic-surface"))
        for _i in self.power_source:
            el.append(_i.to_xml("power-source"))
        if self.software is not None:
            el.append(self.software.to_xml("software"))
        if self.discovered is not None:
            el.append(self.discovered.to_xml("discovered"))
        if self.urdf_compat is not None:
            el.append(self.urdf_compat.to_xml("urdf-compat"))
        for _i in self.extension:
            el.append(_i.to_xml("extension"))
        return el

@dataclass
class Cone:
    """Solid cone primitive. Base circle of given radius, tapering to a point at the given length along the Z axis. Use for nose cones, drill bits, funnels, pointed housings. Distinct from frustum which is a truncated cone for FOV visualization."""
    radius: Optional[Any] = None
    length: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "radius"), None)
        self.radius = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "length"), None)
        self.length = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.radius is not None:
            _e = etree.SubElement(el, "radius"); _e.text = self.radius
        if self.length is not None:
            _e = etree.SubElement(el, "length"); _e.text = self.length
        return el

@dataclass
class Contact:
    """Contact dynamics parameters for collision response in physics simulation."""
    stiffness: Optional[Any] = None
    damping: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.stiffness = el.get("stiffness")
        self.damping = el.get("damping")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.stiffness is not None:
            el.set("stiffness", self.stiffness)
        if self.damping is not None:
            el.set("damping", self.damping)
        return el

@dataclass
class ControlModes:
    """Control interfaces supported by the motor. Determines how an agent can command this actuator: position (servo-like), velocity (speed control), torque/force (direct effort), current (low-level FOC)."""
    mode: List[Any] = field(default_factory=list)

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.mode = [_c.text for _c in _children(el) if _ln(_c) == "mode"]
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        for _i in self.mode:
            _e = etree.SubElement(el, "mode")
            if _i is not None: _e.text = _i
        return el

@dataclass
class ControlSurfaceDef:
    """Aerodynamic control surface (aileron, elevon, elevator, rudder, flap, spoiler). A hinged portion of a wing or tail that deflects to change aerodynamic forces. The joint connecting this comp defines the hinge axis; this element defines the aerodynamic properties."""
    chord_ratio: Optional[Any] = None
    span_fraction: Optional[Any] = None
    deflection: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "chord-ratio"), None)
        self.chord_ratio = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "span-fraction"), None)
        self.span_fraction = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "deflection"), None)
        self.deflection = RangeValue.from_xml(_c) if _c is not None else None
        self.type = el.get("type")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type)
        if self.chord_ratio is not None:
            _e = etree.SubElement(el, "chord-ratio"); _e.text = self.chord_ratio
        if self.span_fraction is not None:
            _e = etree.SubElement(el, "span-fraction"); _e.text = self.span_fraction
        if self.deflection is not None:
            el.append(self.deflection.to_xml("deflection"))
        return el

@dataclass
class CurrentCapability:
    """Port current capability. Text content is the rated current value. Used for power delivery sizing."""
    text: Optional[str] = None
    unit: Optional[Any] = None
    max: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.text = el.text
        self.unit = el.get("unit")
        self.max = el.get("max")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.unit is not None:
            el.set("unit", self.unit)
        if self.max is not None:
            el.set("max", self.max)
        if self.text is not None:
            el.text = self.text
        return el

@dataclass
class Cylinder:
    """Cylinder geometry primitive centered at the parent pose origin, aligned with the Z axis."""
    radius: Optional[Any] = None
    length: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "radius"), None)
        self.radius = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "length"), None)
        self.length = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.radius is not None:
            _e = etree.SubElement(el, "radius"); _e.text = self.radius
        if self.length is not None:
            _e = etree.SubElement(el, "length"); _e.text = self.length
        return el

@dataclass
class DataOutput:
    """Sensor data output characteristics for network bandwidth planning. An agent uses this to validate that network links and TSN schedules can carry the sensor's data. Only relevant for high-bandwidth sensors (cameras, lidars, radars) that transmit over the network. Not needed for low-bandwidth sensors (IMU, baro, mag) connected via SPI/I2C."""
    bandwidth: Optional[Any] = None
    bandwidth_compressed: Optional[Any] = None
    frame_size: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "bandwidth"), None)
        self.bandwidth = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "bandwidth-compressed"), None)
        self.bandwidth_compressed = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "frame-size"), None)
        self.frame_size = MeasuredValue.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.bandwidth is not None:
            el.append(self.bandwidth.to_xml("bandwidth"))
        if self.bandwidth_compressed is not None:
            el.append(self.bandwidth_compressed.to_xml("bandwidth-compressed"))
        if self.frame_size is not None:
            el.append(self.frame_size.to_xml("frame-size"))
        return el

@dataclass
class DcChain:
    """EtherCAT Distributed Clock configuration at the chain level. Identifies the reference clock source for the entire EtherCAT segment."""
    reference_clock: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.reference_clock = el.get("reference-clock")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.reference_clock is not None:
            el.set("reference-clock", self.reference_clock)
        return el

@dataclass
class DcHop:
    """EtherCAT Distributed Clock configuration for a single hop (slave device). Defines the synchronization mode and timing parameters for this slave's local clock."""
    mode: Optional[Any] = None
    cycle_time_us: Optional[Any] = None
    shift_time_ns: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _v = el.get("mode")
        self.mode = DcMode(_v) if _v is not None else None
        self.cycle_time_us = el.get("cycle-time-us")
        self.shift_time_ns = el.get("shift-time-ns")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.mode is not None:
            el.set("mode", self.mode.value)
        if self.cycle_time_us is not None:
            el.set("cycle-time-us", self.cycle_time_us)
        if self.shift_time_ns is not None:
            el.set("shift-time-ns", self.shift_time_ns)
        return el

@dataclass
class Discovered:
    """Runtime discovery state for a component. Populated by device discovery agents (mDNS, LLDP, EtherCAT scan) at runtime. Not typically authored by hand — updated automatically by HCDF tooling after network scanning."""
    ip: Optional[Any] = None
    port: Optional[Any] = None
    last_seen: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "ip"), None)
        self.ip = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "port"), None)
        self.port = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "last-seen"), None)
        self.last_seen = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.ip is not None:
            _e = etree.SubElement(el, "ip"); _e.text = self.ip
        if self.port is not None:
            _e = etree.SubElement(el, "port"); _e.text = self.port
        if self.last_seen is not None:
            _e = etree.SubElement(el, "last-seen"); _e.text = self.last_seen
        return el

@dataclass
class DynamicSurface:
    """Dynamic surface that interacts with the environment (air, water, ground, or grasped objects). Includes aerodynamic surfaces (wings, control surfaces, propellers), hydrodynamic surfaces (hydrofoils), ground contact surfaces (wheels, tracks), and gripper contact surfaces (mechanical pads, suction cups, magnetic, adhesive). Each surface has a name and contains exactly one surface type definition. The comp's joints define the actuation (hinge axes, deflection); the surface defines the physical inter"""
    prop: Optional[Any] = None
    aerofoil: Optional[Any] = None
    hydrofoil: Optional[Any] = None
    control_surface: Optional[Any] = None
    wheel: Optional[Any] = None
    track: Optional[Any] = None
    gripper: Optional[Any] = None
    name: Optional[Any] = None
    visual: Optional[Any] = None
    mesh: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "prop"), None)
        self.prop = PropSurface.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "aerofoil"), None)
        self.aerofoil = AerofoilSurface.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "hydrofoil"), None)
        self.hydrofoil = HydrofoilSurface.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "control-surface"), None)
        self.control_surface = ControlSurfaceDef.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "wheel"), None)
        self.wheel = WheelSurface.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "track"), None)
        self.track = TrackSurface.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "gripper"), None)
        self.gripper = GripperSurface.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        self.visual = el.get("visual")
        self.mesh = el.get("mesh")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.visual is not None:
            el.set("visual", self.visual)
        if self.mesh is not None:
            el.set("mesh", self.mesh)
        if self.prop is not None:
            el.append(self.prop.to_xml("prop"))
        if self.aerofoil is not None:
            el.append(self.aerofoil.to_xml("aerofoil"))
        if self.hydrofoil is not None:
            el.append(self.hydrofoil.to_xml("hydrofoil"))
        if self.control_surface is not None:
            el.append(self.control_surface.to_xml("control-surface"))
        if self.wheel is not None:
            el.append(self.wheel.to_xml("wheel"))
        if self.track is not None:
            el.append(self.track.to_xml("track"))
        if self.gripper is not None:
            el.append(self.gripper.to_xml("gripper"))
        return el

@dataclass
class EeeCapability:
    """IEEE 802.3az Energy Efficient Ethernet capability of a port."""
    supported: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.supported = el.get("supported")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.supported is not None:
            el.set("supported", self.supported)
        return el

@dataclass
class Ellipsoid:
    """Ellipsoid primitive defined by three semi-axis radii. A sphere is a special case where all three radii are equal. Use for approximating heads, torsos, drone fuselages, and other non-uniform rounded shapes. Supported by SDF."""
    radii: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "radii"), None)
        self.radii = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.radii is not None:
            _e = etree.SubElement(el, "radii"); _e.text = self.radii
        return el

@dataclass
class EmSensor:
    """Electromagnetic field sensor (magnetometer, metal detector, eddy current probe, EMF sensor). Extends sensor_base with all common elements (pose, driver, range, resolution, ODR, bandwidth, FIFO, noise, geometry)."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = EmSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class EncoderSensor:
    """Position/velocity encoder sensor. Extends sensor_base with encoder-specific attributes: measurement type (incremental, absolute, linear), physical sensing principle (optical, magnetic, inductive, capacitive, resistive), and communication interface (quadrature, SSI, BiSS, EnDat, SPI). Can be mounted on a motor shaft, joint output, wheel, or linear axis. Motors and transmissions reference encoder sensors by name."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    type: Optional[Any] = None
    principle: Optional[Any] = None
    interface: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = EncoderSensorType(_v) if _v is not None else None
        _v = el.get("principle")
        self.principle = EncoderPrinciple(_v) if _v is not None else None
        self.interface = el.get("interface")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.principle is not None:
            el.set("principle", self.principle.value)
        if self.interface is not None:
            el.set("interface", self.interface)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class EthercatIdentity:
    """EtherCAT slave device identity from the ESI (EtherCAT Slave Information) file. Used to match the physical device to its configuration during startup."""
    vendor: Optional[Any] = None
    product: Optional[Any] = None
    revision: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.vendor = el.get("vendor")
        self.product = el.get("product")
        self.revision = el.get("revision")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.vendor is not None:
            el.set("vendor", self.vendor)
        if self.product is not None:
            el.set("product", self.product)
        if self.revision is not None:
            el.set("revision", self.revision)
        return el

@dataclass
class Extension:
    """Vendor or domain-specific extension container. Allows embedding arbitrary XML content from external domains (e.g., simulation parameters, vendor-specific configurations, CI/CD metadata) without modifying the HCDF core schema. Content is processed with lax validation."""
    any_content: List[Any] = field(default_factory=list)
    domain: Optional[Any] = None
    version: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.any_content = [copy.deepcopy(_c) for _c in _children(el)]
        self.domain = el.get("domain")
        self.version = el.get("version")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.domain is not None:
            el.set("domain", self.domain)
        if self.version is not None:
            el.set("version", self.version)
        for _a in self.any_content:
            el.append(copy.deepcopy(_a))
        return el

@dataclass
class FifoConfig:
    """Hardware FIFO (First-In-First-Out) buffer configuration. Many sensors (IMUs, ADCs) collect samples into a hardware FIFO and trigger an interrupt when the watermark level is reached. This reduces interrupt overhead and allows burst processing. Effective update rate = ODR / watermark."""
    depth: Optional[Any] = None
    watermark: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.depth = el.get("depth")
        self.watermark = el.get("watermark")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.depth is not None:
            el.set("depth", self.depth)
        if self.watermark is not None:
            el.set("watermark", self.watermark)
        return el

@dataclass
class FlowCharacterization:
    """Flow characterization for a fuel/gas tank — how fluid exits the tank and at what rate. Without this, a tank is just a container; the flow characterization tells an agent how fast energy can be delivered to consumers. An agent uses this to compute: max power delivery (flow_rate x energy_density), surge capability (peak flow for acceleration transients), and runtime at load (volume x density / operating_flow_rate)."""
    method: Optional[Any] = None
    outlet_pressure: Optional[Any] = None
    max_flow_rate: Optional[Any] = None
    peak_flow_rate: Optional[Any] = None
    regulator: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "method"), None)
        self.method = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "outlet-pressure"), None)
        self.outlet_pressure = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-flow-rate"), None)
        self.max_flow_rate = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "peak-flow-rate"), None)
        self.peak_flow_rate = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "regulator"), None)
        self.regulator = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.method is not None:
            _e = etree.SubElement(el, "method"); _e.text = self.method
        if self.outlet_pressure is not None:
            el.append(self.outlet_pressure.to_xml("outlet-pressure"))
        if self.max_flow_rate is not None:
            el.append(self.max_flow_rate.to_xml("max-flow-rate"))
        if self.peak_flow_rate is not None:
            el.append(self.peak_flow_rate.to_xml("peak-flow-rate"))
        if self.regulator is not None:
            _e = etree.SubElement(el, "regulator"); _e.text = self.regulator
        return el

@dataclass
class ForceSensor:
    """Force/torque sensor (strain gauge, pressure transducer, torque sensor, load cell). Extends sensor_base with all common elements."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = ForceSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class Frame:
    """Named coordinate frame attached to a component. Used for expressing sensor mounts, tool center points, mounting holes, or any reference point that other elements can relate to via the relative-to attribute."""
    description: Optional[Any] = None
    pose: Optional[Any] = None
    name: Optional[Any] = None
    relative_to: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        self.relative_to = el.get("relative-to")
        self.type = el.get("type")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.relative_to is not None:
            el.set("relative-to", self.relative_to)
        if self.type is not None:
            el.set("type", self.type)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        return el

@dataclass
class Friction:
    """Friction coefficients for contact simulation between surfaces."""
    static: Optional[Any] = None
    dynamic: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.static = el.get("static")
        self.dynamic = el.get("dynamic")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.static is not None:
            el.set("static", self.static)
        if self.dynamic is not None:
            el.set("dynamic", self.dynamic)
        return el

@dataclass
class Frustum:
    """Frustum (truncated cone/pyramid) for sensor field-of-view visualization. Not a solid physics primitive — use cone/box/cylinder for collision. The shape attribute determines the cross-section: "conical" (circular, uses fov angle) or "pyramidal" (rectangular, uses hfov + vfov). Semantic rule: conical requires fov; pyramidal requires hfov + vfov."""
    near: Optional[Any] = None
    far: Optional[Any] = None
    fov: Optional[Any] = None
    hfov: Optional[Any] = None
    vfov: Optional[Any] = None
    shape: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "near"), None)
        self.near = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "far"), None)
        self.far = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "fov"), None)
        self.fov = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "hfov"), None)
        self.hfov = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "vfov"), None)
        self.vfov = _c.text if _c is not None else None
        _v = el.get("shape")
        self.shape = FrustumShape(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.shape is not None:
            el.set("shape", self.shape.value)
        if self.near is not None:
            _e = etree.SubElement(el, "near"); _e.text = self.near
        if self.far is not None:
            _e = etree.SubElement(el, "far"); _e.text = self.far
        if self.fov is not None:
            _e = etree.SubElement(el, "fov"); _e.text = self.fov
        if self.hfov is not None:
            _e = etree.SubElement(el, "hfov"); _e.text = self.hfov
        if self.vfov is not None:
            _e = etree.SubElement(el, "vfov"); _e.text = self.vfov
        return el

@dataclass
class FuelCell:
    """Fuel cell energy source — converts chemical fuel (hydrogen, methanol, etc.) directly to electricity through electrochemical reaction. Unlike batteries (stored charge) or tanks (stored fuel for combustion), fuel cells continuously generate electricity as long as fuel is supplied. The tank element should describe the fuel storage; the fuel cell describes the conversion."""
    type: Optional[Any] = None
    fuel: Optional[Any] = None
    rated_power: Optional[Any] = None
    peak_power: Optional[Any] = None
    efficiency: Optional[Any] = None
    output_voltage: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "type"), None)
        self.type = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "fuel"), None)
        self.fuel = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "rated-power"), None)
        self.rated_power = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "peak-power"), None)
        self.peak_power = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "efficiency"), None)
        self.efficiency = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "output-voltage"), None)
        self.output_voltage = RangeValue.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            _e = etree.SubElement(el, "type"); _e.text = self.type
        if self.fuel is not None:
            _e = etree.SubElement(el, "fuel"); _e.text = self.fuel
        if self.rated_power is not None:
            el.append(self.rated_power.to_xml("rated-power"))
        if self.peak_power is not None:
            el.append(self.peak_power.to_xml("peak-power"))
        if self.efficiency is not None:
            _e = etree.SubElement(el, "efficiency"); _e.text = self.efficiency
        if self.output_voltage is not None:
            el.append(self.output_voltage.to_xml("output-voltage"))
        return el

@dataclass
class Gate:
    """A single entry in a Qbv gate control list. Specifies which traffic classes can transmit and for how long during one segment of the repeating schedule cycle."""
    tc_mask: Optional[Any] = None
    duration_us: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.tc_mask = el.get("tc-mask")
        self.duration_us = el.get("duration-us")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.tc_mask is not None:
            el.set("tc-mask", self.tc_mask)
        if self.duration_us is not None:
            el.set("duration-us", self.duration_us)
        return el

@dataclass
class Geometry:
    """General geometry container holding exactly one shape. Used for sensor field-of-view geometry (frustum) and general references. NOTE: <visual> uses visual_geometry (primitives only -- a visual mesh is the GLB <model>) and <collision> uses collision_geometry (primitives + lean mesh); both exclude frustum. This type retains mesh + frustum for sensor/FOV usage."""
    box: Optional[Any] = None
    cylinder: Optional[Any] = None
    sphere: Optional[Any] = None
    capsule: Optional[Any] = None
    cone: Optional[Any] = None
    ellipsoid: Optional[Any] = None
    mesh: Optional[Any] = None
    frustum: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "box"), None)
        self.box = Box.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cylinder"), None)
        self.cylinder = Cylinder.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "sphere"), None)
        self.sphere = Sphere.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "capsule"), None)
        self.capsule = Capsule.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cone"), None)
        self.cone = Cone.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "ellipsoid"), None)
        self.ellipsoid = Ellipsoid.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "mesh"), None)
        self.mesh = Mesh.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "frustum"), None)
        self.frustum = Frustum.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.box is not None:
            el.append(self.box.to_xml("box"))
        if self.cylinder is not None:
            el.append(self.cylinder.to_xml("cylinder"))
        if self.sphere is not None:
            el.append(self.sphere.to_xml("sphere"))
        if self.capsule is not None:
            el.append(self.capsule.to_xml("capsule"))
        if self.cone is not None:
            el.append(self.cone.to_xml("cone"))
        if self.ellipsoid is not None:
            el.append(self.ellipsoid.to_xml("ellipsoid"))
        if self.mesh is not None:
            el.append(self.mesh.to_xml("mesh"))
        if self.frustum is not None:
            el.append(self.frustum.to_xml("frustum"))
        return el

@dataclass
class GnssParams:
    """GNSS receiver-specific parameters defining constellation support, correction capabilities, and positioning accuracy. Applies to rf sensors with type="gnss"."""
    constellation: List[Any] = field(default_factory=list)
    frequency: List[Any] = field(default_factory=list)
    rtk: Optional[Any] = None
    accuracy_standalone: Optional[Any] = None
    accuracy_rtk: Optional[Any] = None
    pps: Optional[Any] = None
    dead_reckoning: Optional[Any] = None
    anti_spoofing: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.constellation = [_c.text for _c in _children(el) if _ln(_c) == "constellation"]
        self.frequency = [_c.text for _c in _children(el) if _ln(_c) == "frequency"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "rtk"), None)
        self.rtk = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "accuracy-standalone"), None)
        self.accuracy_standalone = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "accuracy-rtk"), None)
        self.accuracy_rtk = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pps"), None)
        self.pps = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "dead-reckoning"), None)
        self.dead_reckoning = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "anti-spoofing"), None)
        self.anti_spoofing = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        for _i in self.constellation:
            _e = etree.SubElement(el, "constellation")
            if _i is not None: _e.text = _i
        for _i in self.frequency:
            _e = etree.SubElement(el, "frequency")
            if _i is not None: _e.text = _i
        if self.rtk is not None:
            _e = etree.SubElement(el, "rtk"); _e.text = self.rtk
        if self.accuracy_standalone is not None:
            el.append(self.accuracy_standalone.to_xml("accuracy-standalone"))
        if self.accuracy_rtk is not None:
            el.append(self.accuracy_rtk.to_xml("accuracy-rtk"))
        if self.pps is not None:
            _e = etree.SubElement(el, "pps"); _e.text = self.pps
        if self.dead_reckoning is not None:
            _e = etree.SubElement(el, "dead-reckoning"); _e.text = self.dead_reckoning
        if self.anti_spoofing is not None:
            _e = etree.SubElement(el, "anti-spoofing"); _e.text = self.anti_spoofing
        return el

@dataclass
class Gptp:
    """IEEE 802.1AS generalized Precision Time Protocol configuration. Defines the clock domain, grandmaster election priorities, and per-port timing parameters. All TSN scheduling depends on gPTP time synchronization."""
    clock: List[Any] = field(default_factory=list)
    port_defaults: Optional[Any] = None
    domain: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.clock = [GptpClock.from_xml(_c) for _c in _children(el) if _ln(_c) == "clock"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "port-defaults"), None)
        self.port_defaults = GptpPortDefaults.from_xml(_c) if _c is not None else None
        self.domain = el.get("domain")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.domain is not None:
            el.set("domain", self.domain)
        for _i in self.clock:
            el.append(_i.to_xml("clock"))
        if self.port_defaults is not None:
            el.append(self.port_defaults.to_xml("port-defaults"))
        return el

@dataclass
class GptpClock:
    """gPTP clock instance participating in the IEEE 802.1AS time domain. Each clock represents a device's gPTP-capable port and its grandmaster election parameters."""
    device: Optional[Any] = None
    port: Optional[Any] = None
    gm_capable: Optional[Any] = None
    priority1: Optional[Any] = None
    priority2: Optional[Any] = None
    clock_class: Optional[Any] = None
    clock_accuracy: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.device = el.get("device")
        self.port = el.get("port")
        self.gm_capable = el.get("gm-capable")
        self.priority1 = el.get("priority1")
        self.priority2 = el.get("priority2")
        self.clock_class = el.get("clock-class")
        self.clock_accuracy = el.get("clock-accuracy")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.device is not None:
            el.set("device", self.device)
        if self.port is not None:
            el.set("port", self.port)
        if self.gm_capable is not None:
            el.set("gm-capable", self.gm_capable)
        if self.priority1 is not None:
            el.set("priority1", self.priority1)
        if self.priority2 is not None:
            el.set("priority2", self.priority2)
        if self.clock_class is not None:
            el.set("clock-class", self.clock_class)
        if self.clock_accuracy is not None:
            el.set("clock-accuracy", self.clock_accuracy)
        return el

@dataclass
class GptpPortDefaults:
    """Default gPTP timing parameters applied to all ports in the domain unless overridden per-port. These values control sync message rates, path delay measurements, and announce timeout."""
    log_sync_interval: Optional[Any] = None
    log_announce_interval: Optional[Any] = None
    log_pdelay_req_interval: Optional[Any] = None
    announce_receipt_timeout: Optional[Any] = None
    neighbor_prop_delay_thresh_ns: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.log_sync_interval = el.get("log-sync-interval")
        self.log_announce_interval = el.get("log-announce-interval")
        self.log_pdelay_req_interval = el.get("log-pdelay-req-interval")
        self.announce_receipt_timeout = el.get("announce-receipt-timeout")
        self.neighbor_prop_delay_thresh_ns = el.get("neighbor-prop-delay-thresh-ns")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.log_sync_interval is not None:
            el.set("log-sync-interval", self.log_sync_interval)
        if self.log_announce_interval is not None:
            el.set("log-announce-interval", self.log_announce_interval)
        if self.log_pdelay_req_interval is not None:
            el.set("log-pdelay-req-interval", self.log_pdelay_req_interval)
        if self.announce_receipt_timeout is not None:
            el.set("announce-receipt-timeout", self.announce_receipt_timeout)
        if self.neighbor_prop_delay_thresh_ns is not None:
            el.set("neighbor-prop-delay-thresh-ns", self.neighbor_prop_delay_thresh_ns)
        return el

@dataclass
class GripperSurface:
    """Gripper or end-effector contact surface that interacts with grasped objects. Describes the physical interface between the robot's gripper and the objects it manipulates. Different gripper types use fundamentally different physics: mechanical fingers squeeze via friction, suction cups use vacuum, magnetic grippers use electromagnetic force, and adhesive surfaces use van der Waals or chemical bonding. An agent uses this to determine grasp feasibility: can this gripper pick up a given object based """
    grip_force: Optional[Any] = None
    contact_area: Optional[Any] = None
    payload: Optional[Any] = None
    shore_hardness: Optional[Any] = None
    vacuum_level: Optional[Any] = None
    magnetic_force: Optional[Any] = None
    friction: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "grip-force"), None)
        self.grip_force = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "contact-area"), None)
        self.contact_area = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "payload"), None)
        self.payload = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "shore-hardness"), None)
        self.shore_hardness = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "vacuum-level"), None)
        self.vacuum_level = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "magnetic-force"), None)
        self.magnetic_force = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "friction"), None)
        self.friction = Friction.from_xml(_c) if _c is not None else None
        self.type = el.get("type")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type)
        if self.grip_force is not None:
            el.append(self.grip_force.to_xml("grip-force"))
        if self.contact_area is not None:
            el.append(self.contact_area.to_xml("contact-area"))
        if self.payload is not None:
            el.append(self.payload.to_xml("payload"))
        if self.shore_hardness is not None:
            _e = etree.SubElement(el, "shore-hardness"); _e.text = self.shore_hardness
        if self.vacuum_level is not None:
            el.append(self.vacuum_level.to_xml("vacuum-level"))
        if self.magnetic_force is not None:
            el.append(self.magnetic_force.to_xml("magnetic-force"))
        if self.friction is not None:
            el.append(self.friction.to_xml("friction"))
        return el

@dataclass
class GroupJointRef:
    """Reference to a joint within a group."""
    ref: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.ref = el.get("ref")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.ref is not None:
            el.set("ref", self.ref)
        return el

@dataclass
class GroupRef:
    """Reference to another group (for nested/composite groups)."""
    ref: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.ref = el.get("ref")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.ref is not None:
            el.set("ref", self.ref)
        return el

@dataclass
class HmiElement:
    """Human-Machine Interface element — displays, speakers, buttons, LEDs, and other devices for human interaction or scene manipulation. Unlike sensors (which observe) and motors (which actuate mechanisms), HMI elements communicate with humans or modify the visual/acoustic environment. Examples: status LEDs on a robot body, a speaker for audio alerts, a touchscreen for operator control, IR illumination LEDs for night-vision cameras, an e-stop button."""
    description: Optional[Any] = None
    pose: Optional[Any] = None
    geometry: Optional[Any] = None
    name: Optional[Any] = None
    type: Optional[Any] = None
    visual: Optional[Any] = None
    mesh: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        _v = el.get("type")
        self.type = HmiType(_v) if _v is not None else None
        self.visual = el.get("visual")
        self.mesh = el.get("mesh")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.visual is not None:
            el.set("visual", self.visual)
        if self.mesh is not None:
            el.set("mesh", self.mesh)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class Hop:
    """A device in a daisy-chain that actively forwards frames to the next device. Each hop represents one store-and-forward bridge (switched mode) or one process-on-the-fly node (pass-through mode). Hop order in the XML defines the physical chain topology from root to tail."""
    description: Optional[Any] = None
    dc: Optional[Any] = None
    identity: Optional[Any] = None
    spur: List[Any] = field(default_factory=list)
    device: Optional[Any] = None
    port: Optional[Any] = None
    port_primary: Optional[Any] = None
    port_secondary: Optional[Any] = None
    ingress: Optional[Any] = None
    egress: Optional[Any] = None
    role: Optional[Any] = None
    alias: Optional[Any] = None
    position: Optional[Any] = None
    fmmu_offset: Optional[Any] = None
    fmmu_size: Optional[Any] = None
    processing_delay_ns: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "dc"), None)
        self.dc = DcHop.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "identity"), None)
        self.identity = EthercatIdentity.from_xml(_c) if _c is not None else None
        self.spur = [Spur.from_xml(_c) for _c in _children(el) if _ln(_c) == "spur"]
        self.device = el.get("device")
        self.port = el.get("port")
        self.port_primary = el.get("port-primary")
        self.port_secondary = el.get("port-secondary")
        self.ingress = el.get("ingress")
        self.egress = el.get("egress")
        _v = el.get("role")
        self.role = HopRole(_v) if _v is not None else None
        self.alias = el.get("alias")
        self.position = el.get("position")
        self.fmmu_offset = el.get("fmmu-offset")
        self.fmmu_size = el.get("fmmu-size")
        self.processing_delay_ns = el.get("processing-delay-ns")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.device is not None:
            el.set("device", self.device)
        if self.port is not None:
            el.set("port", self.port)
        if self.port_primary is not None:
            el.set("port-primary", self.port_primary)
        if self.port_secondary is not None:
            el.set("port-secondary", self.port_secondary)
        if self.ingress is not None:
            el.set("ingress", self.ingress)
        if self.egress is not None:
            el.set("egress", self.egress)
        if self.role is not None:
            el.set("role", self.role.value)
        if self.alias is not None:
            el.set("alias", self.alias)
        if self.position is not None:
            el.set("position", self.position)
        if self.fmmu_offset is not None:
            el.set("fmmu-offset", self.fmmu_offset)
        if self.fmmu_size is not None:
            el.set("fmmu-size", self.fmmu_size)
        if self.processing_delay_ns is not None:
            el.set("processing-delay-ns", self.processing_delay_ns)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.dc is not None:
            el.append(self.dc.to_xml("dc"))
        if self.identity is not None:
            el.append(self.identity.to_xml("identity"))
        for _i in self.spur:
            el.append(_i.to_xml("spur"))
        return el

@dataclass
class HydrofoilSurface:
    """Hydrodynamic lifting surface — generates lift in water. Used on underwater vehicles (AUVs), hydrofoil boats, and submarine planes. Same principles as aerofoil but in water (much higher fluid density)."""
    span: Optional[Any] = None
    chord: Optional[Any] = None
    profile: Optional[Any] = None
    area: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "span"), None)
        self.span = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "chord"), None)
        self.chord = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "profile"), None)
        self.profile = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "area"), None)
        self.area = MeasuredValue.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.span is not None:
            el.append(self.span.to_xml("span"))
        if self.chord is not None:
            el.append(self.chord.to_xml("chord"))
        if self.profile is not None:
            _e = etree.SubElement(el, "profile"); _e.text = self.profile
        if self.area is not None:
            el.append(self.area.to_xml("area"))
        return el

@dataclass
class Include:
    """Include reference for model composition. Imports another HCDF file's components, joints, and networks into this document. Used to build complex robots from reusable sub-assemblies (e.g., arm module, leg module, sensor head). The included model's components are merged into this document with optional name prefix and pose offset."""
    uri: Optional[Any] = None
    sha: Optional[Any] = None
    name: Optional[Any] = None
    pose: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.uri = el.get("uri")
        self.sha = el.get("sha")
        self.name = el.get("name")
        self.pose = el.get("pose")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.uri is not None:
            el.set("uri", self.uri)
        if self.sha is not None:
            el.set("sha", self.sha)
        if self.name is not None:
            el.set("name", self.name)
        if self.pose is not None:
            el.set("pose", self.pose)
        return el

@dataclass
class InertialProperties:
    """Inertial properties of a rigid body component. Defines mass, center of mass, and rotational inertia for dynamics simulation. Equivalent to URDF's inertial element."""
    mass: Optional[Any] = None
    inertia_origin: Optional[Any] = None
    inertia: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "mass"), None)
        self.mass = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "inertia_origin"), None)
        self.inertia_origin = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "inertia"), None)
        self.inertia = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.mass is not None:
            _e = etree.SubElement(el, "mass"); _e.text = self.mass
        if self.inertia_origin is not None:
            el.append(self.inertia_origin.to_xml("inertia_origin"))
        if self.inertia is not None:
            _e = etree.SubElement(el, "inertia"); _e.text = self.inertia
        return el

@dataclass
class InertialSensor:
    """Inertial measurement sensor (accelerometer, gyroscope, or combined IMU). Contains pose, driver identification, and separate parameter blocks for accelerometer and gyroscope axes."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    accel: Optional[Any] = None
    gyro: Optional[Any] = None
    geometry: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "accel"), None)
        self.accel = SensorParams.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "gyro"), None)
        self.gyro = SensorParams.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = InertialSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.accel is not None:
            el.append(self.accel.to_xml("accel"))
        if self.gyro is not None:
            el.append(self.gyro.to_xml("gyro"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class Joint:
    """Kinematic connection between two components defining degrees of freedom. Supports 10 joint types: revolute (bounded rotation), continuous (unbounded rotation), prismatic (linear), fixed (rigid), ball (3-DOF spherical), universal (2-DOF), planar (2D translation), screw (helical), cylindrical (rotation + translation on same axis), and free (6-DOF unconstrained). Semantic rules: revolute/prismatic/cylindrical require axis + limit; fixed and free forbid axis and limit; universal requires axis + axis"""
    description: Optional[Any] = None
    parent: Optional[Any] = None
    child: Optional[Any] = None
    origin: Optional[Any] = None
    axis: Optional[Any] = None
    axis2: Optional[Any] = None
    limit: Optional[Any] = None
    dynamics: Optional[Any] = None
    calibration: Optional[Any] = None
    mimic: Optional[Any] = None
    loop: Optional[Any] = None
    extension: Optional[Any] = None
    urdf_compat: Optional[Any] = None
    name: Optional[Any] = None
    type: Optional[Any] = None
    thread_pitch: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "parent"), None)
        self.parent = JointParent.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "child"), None)
        self.child = JointChild.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "origin"), None)
        self.origin = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "axis"), None)
        self.axis = Axis.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "axis2"), None)
        self.axis2 = Axis.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "limit"), None)
        self.limit = JointLimit.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "dynamics"), None)
        self.dynamics = JointDynamics.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "calibration"), None)
        self.calibration = JointCalibration.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "mimic"), None)
        self.mimic = Mimic.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "loop"), None)
        self.loop = LoopClosure.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "extension"), None)
        self.extension = Extension.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "urdf-compat"), None)
        self.urdf_compat = UrdfCompatJoint.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        _v = el.get("type")
        self.type = JointType(_v) if _v is not None else None
        self.thread_pitch = el.get("thread_pitch")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.thread_pitch is not None:
            el.set("thread_pitch", self.thread_pitch)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.parent is not None:
            el.append(self.parent.to_xml("parent"))
        if self.child is not None:
            el.append(self.child.to_xml("child"))
        if self.origin is not None:
            el.append(self.origin.to_xml("origin"))
        if self.axis is not None:
            el.append(self.axis.to_xml("axis"))
        if self.axis2 is not None:
            el.append(self.axis2.to_xml("axis2"))
        if self.limit is not None:
            el.append(self.limit.to_xml("limit"))
        if self.dynamics is not None:
            el.append(self.dynamics.to_xml("dynamics"))
        if self.calibration is not None:
            el.append(self.calibration.to_xml("calibration"))
        if self.mimic is not None:
            el.append(self.mimic.to_xml("mimic"))
        if self.loop is not None:
            el.append(self.loop.to_xml("loop"))
        if self.extension is not None:
            el.append(self.extension.to_xml("extension"))
        if self.urdf_compat is not None:
            el.append(self.urdf_compat.to_xml("urdf-compat"))
        return el

@dataclass
class JointCalibration:
    """Joint calibration data for homing and reference position determination. Used to relate encoder readings to absolute joint positions."""
    reference_position: Optional[Any] = None
    rising: Optional[Any] = None
    falling: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.reference_position = el.get("reference_position")
        self.rising = el.get("rising")
        self.falling = el.get("falling")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.reference_position is not None:
            el.set("reference_position", self.reference_position)
        if self.rising is not None:
            el.set("rising", self.rising)
        if self.falling is not None:
            el.set("falling", self.falling)
        return el

@dataclass
class JointChild:
    """Child component reference in a joint. The child is the component that moves relative to the parent through the joint's degrees of freedom."""
    comp: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.comp = el.get("comp")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.comp is not None:
            el.set("comp", self.comp)
        return el

@dataclass
class JointDynamics:
    """Joint dynamics parameters for simulation. Defines viscous damping, Coulomb friction, and optional spring behavior."""
    damping: Optional[Any] = None
    friction: Optional[Any] = None
    spring_stiffness: Optional[Any] = None
    spring_reference: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.damping = el.get("damping")
        self.friction = el.get("friction")
        self.spring_stiffness = el.get("spring_stiffness")
        self.spring_reference = el.get("spring_reference")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.damping is not None:
            el.set("damping", self.damping)
        if self.friction is not None:
            el.set("friction", self.friction)
        if self.spring_stiffness is not None:
            el.set("spring_stiffness", self.spring_stiffness)
        if self.spring_reference is not None:
            el.set("spring_reference", self.spring_reference)
        return el

@dataclass
class JointGroup:
    """Named collection of joints that can be planned and controlled as a unit. Groups enable efficient motion planning (plan for 7 arm joints, not 30+ total), task decomposition ("use left_arm to pick up cup"), and cross-embodiment transfer (different robots both define a "manipulation" group). Groups can contain other groups for hierarchical composition. Equivalent to SRDF joint groups."""
    description: Optional[Any] = None
    joint: List[Any] = field(default_factory=list)
    group: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    type: Optional[Any] = None
    tip_comp: Optional[Any] = None
    tip_frame: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        self.joint = [GroupJointRef.from_xml(_c) for _c in _children(el) if _ln(_c) == "joint"]
        self.group = [GroupRef.from_xml(_c) for _c in _children(el) if _ln(_c) == "group"]
        self.name = el.get("name")
        self.type = el.get("type")
        self.tip_comp = el.get("tip-comp")
        self.tip_frame = el.get("tip-frame")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type)
        if self.tip_comp is not None:
            el.set("tip-comp", self.tip_comp)
        if self.tip_frame is not None:
            el.set("tip-frame", self.tip_frame)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        for _i in self.joint:
            el.append(_i.to_xml("joint"))
        for _i in self.group:
            el.append(_i.to_xml("group"))
        return el

@dataclass
class JointLimit:
    """Position, velocity, effort, acceleration, and jerk limits for a joint. Required for revolute and prismatic joints. Units depend on joint type: radians and Nm for revolute, meters and N for prismatic."""
    lower: Optional[Any] = None
    upper: Optional[Any] = None
    effort: Optional[Any] = None
    velocity: Optional[Any] = None
    acceleration: Optional[Any] = None
    jerk: Optional[Any] = None
    deceleration: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.lower = el.get("lower")
        self.upper = el.get("upper")
        self.effort = el.get("effort")
        self.velocity = el.get("velocity")
        self.acceleration = el.get("acceleration")
        self.jerk = el.get("jerk")
        self.deceleration = el.get("deceleration")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.lower is not None:
            el.set("lower", self.lower)
        if self.upper is not None:
            el.set("upper", self.upper)
        if self.effort is not None:
            el.set("effort", self.effort)
        if self.velocity is not None:
            el.set("velocity", self.velocity)
        if self.acceleration is not None:
            el.set("acceleration", self.acceleration)
        if self.jerk is not None:
            el.set("jerk", self.jerk)
        if self.deceleration is not None:
            el.set("deceleration", self.deceleration)
        return el

@dataclass
class JointParent:
    """Parent component reference in a joint. The parent is the component that remains "fixed" while the child moves relative to it through the joint's degrees of freedom."""
    comp: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.comp = el.get("comp")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.comp is not None:
            el.set("comp", self.comp)
        return el

@dataclass
class JointPosition:
    """A single joint position value within a named kinematic state. References a joint by name and provides its position in radians (revolute/continuous) or meters (prismatic)."""
    joint: Optional[Any] = None
    value: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.joint = el.get("joint")
        self.value = el.get("value")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.joint is not None:
            el.set("joint", self.joint)
        if self.value is not None:
            el.set("value", self.value)
        return el

@dataclass
class KeyStore:
    """Reference to the credential storage backend for MACsec key material. Points the runtime MACsec daemon to the appropriate key source."""
    type: Optional[Any] = None
    ref: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _v = el.get("type")
        self.type = KeyStoreType(_v) if _v is not None else None
        self.ref = el.get("ref")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.ref is not None:
            el.set("ref", self.ref)
        return el

@dataclass
class KinematicState:
    """Named kinematic state (pose/keyframe) defining a joint configuration. Examples: "home" (standard ready position), "transport" (compact for shipping), "calibration" (known reference for sensor alignment). An AI agent can reference states by name instead of specifying individual joint angles. One state can be marked as default."""
    description: Optional[Any] = None
    joint_position: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    default: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        self.joint_position = [JointPosition.from_xml(_c) for _c in _children(el) if _ln(_c) == "joint-position"]
        self.name = el.get("name")
        self.default = el.get("default")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.default is not None:
            el.set("default", self.default)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        for _i in self.joint_position:
            el.append(_i.to_xml("joint-position"))
        return el

@dataclass
class LidarParams:
    """Lidar-specific parameters defining scan geometry, point cloud density, and environmental robustness. Applies to optical sensors with type="lidar"."""
    scan_type: Optional[Any] = None
    channels: Optional[Any] = None
    points_per_second: Optional[Any] = None
    scan_rate: Optional[Any] = None
    returns: Optional[Any] = None
    wavelength: Optional[Any] = None
    horizontal_fov: Optional[Any] = None
    vertical_fov: Optional[Any] = None
    scan_pattern: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "scan-type"), None)
        self.scan_type = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "channels"), None)
        self.channels = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "points-per-second"), None)
        self.points_per_second = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "scan-rate"), None)
        self.scan_rate = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "returns"), None)
        self.returns = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "wavelength"), None)
        self.wavelength = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "horizontal-fov"), None)
        self.horizontal_fov = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "vertical-fov"), None)
        self.vertical_fov = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "scan-pattern"), None)
        self.scan_pattern = LidarParamsScanPattern.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.scan_type is not None:
            _e = etree.SubElement(el, "scan-type"); _e.text = self.scan_type
        if self.channels is not None:
            _e = etree.SubElement(el, "channels"); _e.text = self.channels
        if self.points_per_second is not None:
            _e = etree.SubElement(el, "points-per-second"); _e.text = self.points_per_second
        if self.scan_rate is not None:
            el.append(self.scan_rate.to_xml("scan-rate"))
        if self.returns is not None:
            _e = etree.SubElement(el, "returns"); _e.text = self.returns
        if self.wavelength is not None:
            el.append(self.wavelength.to_xml("wavelength"))
        if self.horizontal_fov is not None:
            el.append(self.horizontal_fov.to_xml("horizontal-fov"))
        if self.vertical_fov is not None:
            el.append(self.vertical_fov.to_xml("vertical-fov"))
        if self.scan_pattern is not None:
            el.append(self.scan_pattern.to_xml("scan-pattern"))
        return el

@dataclass
class LidarScanAxis:
    """One axis of a lidar/ray angular scan pattern, matching a URDF ray <horizontal>/<vertical> element (samples, angular resolution, and min/max sweep angle). This is the lossless target for a converted URDF ray sensor; it complements the aggregate fields (channels, fov) which describe the device rather than the per-axis sampling."""
    samples: Optional[Any] = None
    resolution: Optional[Any] = None
    min_angle: Optional[Any] = None
    max_angle: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.samples = el.get("samples")
        self.resolution = el.get("resolution")
        self.min_angle = el.get("min-angle")
        self.max_angle = el.get("max-angle")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.samples is not None:
            el.set("samples", self.samples)
        if self.resolution is not None:
            el.set("resolution", self.resolution)
        if self.min_angle is not None:
            el.set("min-angle", self.min_angle)
        if self.max_angle is not None:
            el.set("max-angle", self.max_angle)
        return el

@dataclass
class Link:
    """Point-to-point connection between exactly two ports (wired) or two antennas (wireless). The standard attribute identifies the PHY standard (e.g., 25gbase-sr, 100base-t1, 802.3dm). Can include VLAN assignment, MACsec policy, and IP configuration."""
    description: Optional[Any] = None
    wired: Optional[Any] = None
    wireless: Optional[Any] = None
    name: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "wired"), None)
        self.wired = Wired.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "wireless"), None)
        self.wireless = Wireless.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.wired is not None:
            el.append(self.wired.to_xml("wired"))
        if self.wireless is not None:
            el.append(self.wireless.to_xml("wireless"))
        return el

@dataclass
class LldpCapability:
    """IEEE 802.1AB LLDP (Link Layer Discovery Protocol) capability of a port."""
    tsn_tlvs: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.tsn_tlvs = el.get("tsn-tlvs")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.tsn_tlvs is not None:
            el.set("tsn-tlvs", self.tsn_tlvs)
        return el

@dataclass
class LoopClosure:
    """Marks a joint as closing a kinematic loop. Contains the predecessor/successor bodies and which DOFs are constrained at the closure point. Used by dynamics solvers (HyRoDyn, Drake) to handle parallel mechanisms that URDF's tree-only structure cannot represent."""
    predecessor: Optional[Any] = None
    successor: Optional[Any] = None
    constraint_axes: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "predecessor"), None)
        self.predecessor = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "successor"), None)
        self.successor = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "constraint-axes"), None)
        self.constraint_axes = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.predecessor is not None:
            _e = etree.SubElement(el, "predecessor"); _e.text = self.predecessor
        if self.successor is not None:
            _e = etree.SubElement(el, "successor"); _e.text = self.successor
        if self.constraint_axes is not None:
            _e = etree.SubElement(el, "constraint-axes"); _e.text = self.constraint_axes
        return el

@dataclass
class MacsecCapability:
    """IEEE 802.1AE MACsec hardware capability of a port. Lists the cipher suites the port's MACsec engine can accelerate in hardware."""
    cipher: List[Any] = field(default_factory=list)

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.cipher = [MacsecCipher(_c.text) for _c in _children(el) if _ln(_c) == "cipher"]
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        for _i in self.cipher:
            _e = etree.SubElement(el, "cipher"); _e.text = _i.value
        return el

@dataclass
class MacsecConfig:
    """IEEE 802.1AE MACsec network-wide default security policy. Individual links and chains can override. No actual keys — only policy intent. Keys are provisioned at runtime via the key-store reference."""
    key_store: Optional[Any] = None
    policy: Optional[Any] = None
    cipher: Optional[Any] = None
    key_agreement: Optional[Any] = None
    rekey_interval_sec: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "key-store"), None)
        self.key_store = KeyStore.from_xml(_c) if _c is not None else None
        _v = el.get("policy")
        self.policy = MacsecPolicy(_v) if _v is not None else None
        _v = el.get("cipher")
        self.cipher = MacsecCipher(_v) if _v is not None else None
        _v = el.get("key-agreement")
        self.key_agreement = KeyAgreement(_v) if _v is not None else None
        self.rekey_interval_sec = el.get("rekey-interval-sec")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.policy is not None:
            el.set("policy", self.policy.value)
        if self.cipher is not None:
            el.set("cipher", self.cipher.value)
        if self.key_agreement is not None:
            el.set("key-agreement", self.key_agreement.value)
        if self.rekey_interval_sec is not None:
            el.set("rekey-interval-sec", self.rekey_interval_sec)
        if self.key_store is not None:
            el.append(self.key_store.to_xml("key-store"))
        return el

@dataclass
class MacsecPolicy_:
    """Per-link or per-chain IEEE 802.1AE MACsec security policy. Overrides the network-wide macsec default when present. Specifies policy intent only — actual keys are provisioned at runtime."""
    policy: Optional[Any] = None
    cipher: Optional[Any] = None
    key_agreement: Optional[Any] = None
    key_ref: Optional[Any] = None
    confidentiality_offset: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _v = el.get("policy")
        self.policy = MacsecPolicy(_v) if _v is not None else None
        _v = el.get("cipher")
        self.cipher = MacsecCipher(_v) if _v is not None else None
        _v = el.get("key-agreement")
        self.key_agreement = KeyAgreement(_v) if _v is not None else None
        self.key_ref = el.get("key-ref")
        _v = el.get("confidentiality-offset")
        self.confidentiality_offset = ConfidentialityOffset(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.policy is not None:
            el.set("policy", self.policy.value)
        if self.cipher is not None:
            el.set("cipher", self.cipher.value)
        if self.key_agreement is not None:
            el.set("key-agreement", self.key_agreement.value)
        if self.key_ref is not None:
            el.set("key-ref", self.key_ref)
        if self.confidentiality_offset is not None:
            el.set("confidentiality-offset", self.confidentiality_offset.value)
        return el

@dataclass
class MeasuredValue:
    """A scalar measurement with an optional unit. The text content is the numeric value. HCDF assumes SI units by default — if the unit attribute is omitted, the parent element's documented default SI unit applies (meters for length, kg for mass, radians for angles, seconds for time, Hz for frequency, V for voltage, A for current, W for power, ohm for resistance, Nm for torque, N for force, degC for temperature, Mbps for network speed). Specify unit explicitly only when using a non-SI unit or when the"""
    text: Optional[str] = None
    unit: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.text = el.text
        self.unit = el.get("unit")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.unit is not None:
            el.set("unit", self.unit)
        if self.text is not None:
            el.text = self.text
        return el

@dataclass
class Mesh:
    """External 3D mesh geometry loaded from a file. Supports GLB, glTF, STL, OBJ, and DAE formats."""
    uri: Optional[Any] = None
    scale: Optional[Any] = None
    sha: Optional[Any] = None
    source_uri: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.uri = el.get("uri")
        self.scale = el.get("scale")
        self.sha = el.get("sha")
        self.source_uri = el.get("source-uri")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.uri is not None:
            el.set("uri", self.uri)
        if self.scale is not None:
            el.set("scale", self.scale)
        if self.sha is not None:
            el.set("sha", self.sha)
        if self.source_uri is not None:
            el.set("source-uri", self.source_uri)
        return el

@dataclass
class MeshNode:
    """A participant in a wireless mesh network. Unlike bus participants (shared medium) or chain hops (ordered forwarding), mesh nodes self-organize their routing topology. The mesh protocol handles path discovery, multi-hop forwarding, and self-healing."""
    device: Optional[Any] = None
    antenna: Optional[Any] = None
    role: Optional[Any] = None
    id: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.device = el.get("device")
        self.antenna = el.get("antenna")
        _v = el.get("role")
        self.role = MeshRole(_v) if _v is not None else None
        self.id = el.get("id")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.device is not None:
            el.set("device", self.device)
        if self.antenna is not None:
            el.set("antenna", self.antenna)
        if self.role is not None:
            el.set("role", self.role.value)
        if self.id is not None:
            el.set("id", self.id)
        return el

@dataclass
class Mimic:
    """Mimic joint constraint. Makes this joint's position track another joint's position via a linear relationship: position = multiplier * leader_position + offset. Used for mechanically coupled joints (e.g., parallel gripper fingers, coupled four-bar linkages)."""
    joint: Optional[Any] = None
    multiplier: Optional[Any] = None
    offset: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.joint = el.get("joint")
        self.multiplier = el.get("multiplier")
        self.offset = el.get("offset")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.joint is not None:
            el.set("joint", self.joint)
        if self.multiplier is not None:
            el.set("multiplier", self.multiplier)
        if self.offset is not None:
            el.set("offset", self.offset)
        return el

@dataclass
class ModelRef:
    """Reference to an external 3D model file (GLB or glTF) for high-fidelity visual representation. Preferred over geometry primitives when available."""
    uri: Optional[Any] = None
    sha: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.uri = el.get("uri")
        self.sha = el.get("sha")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.uri is not None:
            el.set("uri", self.uri)
        if self.sha is not None:
            el.set("sha", self.sha)
        return el

@dataclass
class Motor:
    """Motor or actuator with electrical, mechanical, and thermal parameters. Voltage and current use rated_value type with nominal/continuous/peak/max ratings. Connected to joints via transmission elements."""
    pose: Optional[Any] = None
    voltage: Optional[Any] = None
    current: Optional[Any] = None
    resistance: Optional[Any] = None
    torque_constant: Optional[Any] = None
    velocity_constant: Optional[Any] = None
    max_speed: Optional[Any] = None
    stall_torque: Optional[Any] = None
    thermal_resistance: Optional[Any] = None
    max_temperature: Optional[Any] = None
    thrust_axis: Optional[Any] = None
    max_thrust: Optional[Any] = None
    pole_pairs: Optional[Any] = None
    displacement: Optional[Any] = None
    cylinders: Optional[Any] = None
    stroke: Optional[Any] = None
    control_modes: Optional[Any] = None
    no_load_speed: Optional[Any] = None
    no_load_current: Optional[Any] = None
    max_power_out: Optional[Any] = None
    max_efficiency: Optional[Any] = None
    name: Optional[Any] = None
    type: Optional[Any] = None
    construction: Optional[Any] = None
    visual: Optional[Any] = None
    mesh: Optional[Any] = None
    encoder: Optional[Any] = None
    hall: Optional[Any] = None
    thermistor: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "voltage"), None)
        self.voltage = RatedValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "current"), None)
        self.current = RatedValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resistance"), None)
        self.resistance = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "torque-constant"), None)
        self.torque_constant = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "velocity-constant"), None)
        self.velocity_constant = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-speed"), None)
        self.max_speed = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "stall-torque"), None)
        self.stall_torque = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "thermal-resistance"), None)
        self.thermal_resistance = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-temperature"), None)
        self.max_temperature = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "thrust-axis"), None)
        self.thrust_axis = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-thrust"), None)
        self.max_thrust = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pole-pairs"), None)
        self.pole_pairs = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "displacement"), None)
        self.displacement = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cylinders"), None)
        self.cylinders = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "stroke"), None)
        self.stroke = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "control-modes"), None)
        self.control_modes = ControlModes.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "no-load-speed"), None)
        self.no_load_speed = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "no-load-current"), None)
        self.no_load_current = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-power-out"), None)
        self.max_power_out = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-efficiency"), None)
        self.max_efficiency = MeasuredValue.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        _v = el.get("type")
        self.type = MotorType(_v) if _v is not None else None
        self.construction = el.get("construction")
        self.visual = el.get("visual")
        self.mesh = el.get("mesh")
        self.encoder = el.get("encoder")
        self.hall = el.get("hall")
        self.thermistor = el.get("thermistor")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.construction is not None:
            el.set("construction", self.construction)
        if self.visual is not None:
            el.set("visual", self.visual)
        if self.mesh is not None:
            el.set("mesh", self.mesh)
        if self.encoder is not None:
            el.set("encoder", self.encoder)
        if self.hall is not None:
            el.set("hall", self.hall)
        if self.thermistor is not None:
            el.set("thermistor", self.thermistor)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.voltage is not None:
            el.append(self.voltage.to_xml("voltage"))
        if self.current is not None:
            el.append(self.current.to_xml("current"))
        if self.resistance is not None:
            el.append(self.resistance.to_xml("resistance"))
        if self.torque_constant is not None:
            el.append(self.torque_constant.to_xml("torque-constant"))
        if self.velocity_constant is not None:
            el.append(self.velocity_constant.to_xml("velocity-constant"))
        if self.max_speed is not None:
            el.append(self.max_speed.to_xml("max-speed"))
        if self.stall_torque is not None:
            el.append(self.stall_torque.to_xml("stall-torque"))
        if self.thermal_resistance is not None:
            el.append(self.thermal_resistance.to_xml("thermal-resistance"))
        if self.max_temperature is not None:
            el.append(self.max_temperature.to_xml("max-temperature"))
        if self.thrust_axis is not None:
            _e = etree.SubElement(el, "thrust-axis"); _e.text = self.thrust_axis
        if self.max_thrust is not None:
            el.append(self.max_thrust.to_xml("max-thrust"))
        if self.pole_pairs is not None:
            _e = etree.SubElement(el, "pole-pairs"); _e.text = self.pole_pairs
        if self.displacement is not None:
            el.append(self.displacement.to_xml("displacement"))
        if self.cylinders is not None:
            _e = etree.SubElement(el, "cylinders"); _e.text = self.cylinders
        if self.stroke is not None:
            el.append(self.stroke.to_xml("stroke"))
        if self.control_modes is not None:
            el.append(self.control_modes.to_xml("control-modes"))
        if self.no_load_speed is not None:
            el.append(self.no_load_speed.to_xml("no-load-speed"))
        if self.no_load_current is not None:
            el.append(self.no_load_current.to_xml("no-load-current"))
        if self.max_power_out is not None:
            el.append(self.max_power_out.to_xml("max-power-out"))
        if self.max_efficiency is not None:
            el.append(self.max_efficiency.to_xml("max-efficiency"))
        return el

@dataclass
class Network:
    """Unified network description grouping physical topology (links, buses, chains, meshes) and network configuration (gPTP clock sync, TSN traffic classes, Qbv schedules, MACsec security, EEE policy, stream profiles). A robot can have multiple networks for independent domains (e.g., separate arm and base networks, a Thread sensor mesh)."""
    description: Optional[Any] = None
    link: List[Any] = field(default_factory=list)
    bus: List[Any] = field(default_factory=list)
    chain: List[Any] = field(default_factory=list)
    mesh: List[Any] = field(default_factory=list)
    gptp: Optional[Any] = None
    traffic_classes: Optional[Any] = None
    schedule: List[Any] = field(default_factory=list)
    schedule_assignment: Optional[Any] = None
    stream_profile: List[Any] = field(default_factory=list)
    macsec: Optional[Any] = None
    eee_policy: Optional[Any] = None
    name: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        self.link = [Link.from_xml(_c) for _c in _children(el) if _ln(_c) == "link"]
        self.bus = [Bus.from_xml(_c) for _c in _children(el) if _ln(_c) == "bus"]
        self.chain = [Chain.from_xml(_c) for _c in _children(el) if _ln(_c) == "chain"]
        self.mesh = [WirelessMesh.from_xml(_c) for _c in _children(el) if _ln(_c) == "mesh"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "gptp"), None)
        self.gptp = Gptp.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "traffic-classes"), None)
        self.traffic_classes = TrafficClasses.from_xml(_c) if _c is not None else None
        self.schedule = [Schedule.from_xml(_c) for _c in _children(el) if _ln(_c) == "schedule"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "schedule-assignment"), None)
        self.schedule_assignment = ScheduleAssignment.from_xml(_c) if _c is not None else None
        self.stream_profile = [StreamProfileRef.from_xml(_c) for _c in _children(el) if _ln(_c) == "stream-profile"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "macsec"), None)
        self.macsec = MacsecConfig.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "eee-policy"), None)
        self.eee_policy = EeePolicyValue(_c.text) if _c is not None else None
        self.name = el.get("name")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        for _i in self.link:
            el.append(_i.to_xml("link"))
        for _i in self.bus:
            el.append(_i.to_xml("bus"))
        for _i in self.chain:
            el.append(_i.to_xml("chain"))
        for _i in self.mesh:
            el.append(_i.to_xml("mesh"))
        if self.gptp is not None:
            el.append(self.gptp.to_xml("gptp"))
        if self.traffic_classes is not None:
            el.append(self.traffic_classes.to_xml("traffic-classes"))
        for _i in self.schedule:
            el.append(_i.to_xml("schedule"))
        if self.schedule_assignment is not None:
            el.append(self.schedule_assignment.to_xml("schedule-assignment"))
        for _i in self.stream_profile:
            el.append(_i.to_xml("stream-profile"))
        if self.macsec is not None:
            el.append(self.macsec.to_xml("macsec"))
        if self.eee_policy is not None:
            _e = etree.SubElement(el, "eee-policy"); _e.text = self.eee_policy.value
        return el

@dataclass
class Noise:
    """Sensor noise model for simulation. Specifies the statistical distribution and parameters applied to ideal sensor readings. For gaussian type: output = ideal + N(mean, stddev) + bias where bias ~ N(bias-mean, bias-stddev). For uniform type: output = ideal + U(mean - stddev, mean + stddev)."""
    mean: Optional[Any] = None
    stddev: Optional[Any] = None
    bias_mean: Optional[Any] = None
    bias_stddev: Optional[Any] = None
    precision: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "mean"), None)
        self.mean = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "stddev"), None)
        self.stddev = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "bias-mean"), None)
        self.bias_mean = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "bias-stddev"), None)
        self.bias_stddev = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "precision"), None)
        self.precision = _c.text if _c is not None else None
        _v = el.get("type")
        self.type = NoiseType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.mean is not None:
            _e = etree.SubElement(el, "mean"); _e.text = self.mean
        if self.stddev is not None:
            _e = etree.SubElement(el, "stddev"); _e.text = self.stddev
        if self.bias_mean is not None:
            _e = etree.SubElement(el, "bias-mean"); _e.text = self.bias_mean
        if self.bias_stddev is not None:
            _e = etree.SubElement(el, "bias-stddev"); _e.text = self.bias_stddev
        if self.precision is not None:
            _e = etree.SubElement(el, "precision"); _e.text = self.precision
        return el

@dataclass
class OpticalSensor:
    """Optical sensor (camera, lidar, ToF, optical flow). Contains one or more field-of-view definitions, each with its own intrinsics, distortion, and noise model."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    geometry: Optional[Any] = None
    fov: List[Any] = field(default_factory=list)
    data_output: Optional[Any] = None
    camera_params: Optional[Any] = None
    lidar_params: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        self.fov = [SensorFov.from_xml(_c) for _c in _children(el) if _ln(_c) == "fov"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "camera-params"), None)
        self.camera_params = CameraParams.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "lidar-params"), None)
        self.lidar_params = LidarParams.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = OpticalSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        for _i in self.fov:
            el.append(_i.to_xml("fov"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.camera_params is not None:
            el.append(self.camera_params.to_xml("camera-params"))
        if self.lidar_params is not None:
            el.append(self.lidar_params.to_xml("lidar-params"))
        return el

@dataclass
class PhyConfig:
    """Physical layer configuration for an Ethernet port. Required for single-pair Ethernet links (100BASE-T1, 1000BASE-T1, 802.3dm) where PHY master/slave role must be explicitly configured. Semantic rule: each point-to-point T1 link requires exactly one master and one slave."""
    role: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _v = el.get("role")
        self.role = PhyRole(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.role is not None:
            el.set("role", self.role.value)
        return el

@dataclass
class PlcaCapability:
    """IEEE 802.3cg PLCA (Physical Layer Collision Avoidance) capability for 10BASE-T1S multi-drop Ethernet. PLCA provides deterministic medium access by assigning time slots to each node."""
    max_nodes: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.max_nodes = el.get("max-nodes")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.max_nodes is not None:
            el.set("max-nodes", self.max_nodes)
        return el

@dataclass
class PlcaConfig:
    """IEEE 802.3cg PLCA configuration for 10BASE-T1S multi-drop Ethernet buses. Defines the coordinator, node count, and burst parameters for deterministic medium access."""
    coordinator: Optional[Any] = None
    node_count: Optional[Any] = None
    burst_count: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "coordinator"), None)
        self.coordinator = PlcaCoordinator.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "node-count"), None)
        self.node_count = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "burst-count"), None)
        self.burst_count = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.coordinator is not None:
            el.append(self.coordinator.to_xml("coordinator"))
        if self.node_count is not None:
            _e = etree.SubElement(el, "node-count"); _e.text = self.node_count
        if self.burst_count is not None:
            _e = etree.SubElement(el, "burst-count"); _e.text = self.burst_count
        return el

@dataclass
class PlcaCoordinator:
    """PLCA coordinator designation for IEEE 802.3cg 10BASE-T1S multi-drop Ethernet. The coordinator provides the beacon that synchronizes time slot allocation for all nodes on the segment."""
    device: Optional[Any] = None
    port: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.device = el.get("device")
        self.port = el.get("port")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.device is not None:
            el.set("device", self.device)
        if self.port is not None:
            el.set("port", self.port)
        return el

@dataclass
class Port:
    """Physical wired connection interface on a component or switch. The name attribute is the HCDF logical identifier used in link/chain references. The iface attribute is the OS-level interface name (e.g., eth0, swp0, can0) as seen by the device for agent configuration. Contains capabilities describing electrical, protocol, TSN, and power characteristics."""
    capabilities: Optional[Any] = None
    pose: Optional[Any] = None
    geometry: Optional[Any] = None
    name: Optional[Any] = None
    iface: Optional[Any] = None
    type: Optional[Any] = None
    visual: Optional[Any] = None
    mesh: Optional[Any] = None
    electrical: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "capabilities"), None)
        self.capabilities = PortCapabilities.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        self.iface = el.get("iface")
        _v = el.get("type")
        self.type = PortType(_v) if _v is not None else None
        self.visual = el.get("visual")
        self.mesh = el.get("mesh")
        self.electrical = el.get("electrical")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.iface is not None:
            el.set("iface", self.iface)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.visual is not None:
            el.set("visual", self.visual)
        if self.mesh is not None:
            el.set("mesh", self.mesh)
        if self.electrical is not None:
            el.set("electrical", self.electrical)
        if self.capabilities is not None:
            el.append(self.capabilities.to_xml("capabilities"))
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class PortCapabilities:
    """Hardware capabilities of a physical port. Declares data rates, PHY configuration, TSN features, MACsec support, and power delivery characteristics. Elements must appear in order: data, PHY, TSN, power. Semantic rule: use either speed OR speed-tx + speed-rx, not both."""
    speed: Optional[Any] = None
    speed_tx: Optional[Any] = None
    speed_rx: Optional[Any] = None
    bitrate: Optional[Any] = None
    baud: Optional[Any] = None
    standard: Optional[Any] = None
    protocol: List[Any] = field(default_factory=list)
    phy: Optional[Any] = None
    tsn: Optional[Any] = None
    macsec: Optional[Any] = None
    lldp: Optional[Any] = None
    plca: Optional[Any] = None
    eee: Optional[Any] = None
    voltage: Optional[Any] = None
    current: Optional[Any] = None
    power: Optional[Any] = None
    typical_power: Optional[Any] = None
    connector: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "speed"), None)
        self.speed = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "speed-tx"), None)
        self.speed_tx = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "speed-rx"), None)
        self.speed_rx = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "bitrate"), None)
        self.bitrate = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "baud"), None)
        self.baud = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "standard"), None)
        self.standard = _c.text if _c is not None else None
        self.protocol = [_c.text for _c in _children(el) if _ln(_c) == "protocol"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "phy"), None)
        self.phy = PhyConfig.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "tsn"), None)
        self.tsn = TsnPortCapabilities.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "macsec"), None)
        self.macsec = MacsecCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "lldp"), None)
        self.lldp = LldpCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "plca"), None)
        self.plca = PlcaCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "eee"), None)
        self.eee = EeeCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "voltage"), None)
        self.voltage = VoltageCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "current"), None)
        self.current = CurrentCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "power"), None)
        self.power = PowerCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "typical-power"), None)
        self.typical_power = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "connector"), None)
        self.connector = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.speed is not None:
            el.append(self.speed.to_xml("speed"))
        if self.speed_tx is not None:
            el.append(self.speed_tx.to_xml("speed-tx"))
        if self.speed_rx is not None:
            el.append(self.speed_rx.to_xml("speed-rx"))
        if self.bitrate is not None:
            el.append(self.bitrate.to_xml("bitrate"))
        if self.baud is not None:
            el.append(self.baud.to_xml("baud"))
        if self.standard is not None:
            _e = etree.SubElement(el, "standard"); _e.text = self.standard
        for _i in self.protocol:
            _e = etree.SubElement(el, "protocol")
            if _i is not None: _e.text = _i
        if self.phy is not None:
            el.append(self.phy.to_xml("phy"))
        if self.tsn is not None:
            el.append(self.tsn.to_xml("tsn"))
        if self.macsec is not None:
            el.append(self.macsec.to_xml("macsec"))
        if self.lldp is not None:
            el.append(self.lldp.to_xml("lldp"))
        if self.plca is not None:
            el.append(self.plca.to_xml("plca"))
        if self.eee is not None:
            el.append(self.eee.to_xml("eee"))
        if self.voltage is not None:
            el.append(self.voltage.to_xml("voltage"))
        if self.current is not None:
            el.append(self.current.to_xml("current"))
        if self.power is not None:
            el.append(self.power.to_xml("power"))
        if self.typical_power is not None:
            el.append(self.typical_power.to_xml("typical-power"))
        if self.connector is not None:
            _e = etree.SubElement(el, "connector"); _e.text = self.connector
        return el

@dataclass
class Pose:
    """6-DOF pose (position + orientation) of an element relative to its parent frame. Specify position via xyz and orientation via either rpy (Euler angles) or quat (quaternion). If both rpy and quat are given, quat takes precedence."""
    xyz: Optional[Any] = None
    rpy: Optional[Any] = None
    quat: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.xyz = el.get("xyz")
        self.rpy = el.get("rpy")
        self.quat = el.get("quat")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.xyz is not None:
            el.set("xyz", self.xyz)
        if self.rpy is not None:
            el.set("rpy", self.rpy)
        if self.quat is not None:
            el.set("quat", self.quat)
        return el

@dataclass
class PowerCapability:
    """Port power capability. Text content is the rated power value. Used for total power budget calculations."""
    text: Optional[str] = None
    unit: Optional[Any] = None
    max: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.text = el.text
        self.unit = el.get("unit")
        self.max = el.get("max")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.unit is not None:
            el.set("unit", self.unit)
        if self.max is not None:
            el.set("max", self.max)
        if self.text is not None:
            el.text = self.text
        return el

@dataclass
class PowerSource:
    """Energy source on a component. Contains one or more source types (battery, tank, fuel-cell, solar, supercapacitor). An agent computes runtime, power budget, and weight impact from this data. The component's POWER port defines the electrical output; the power-source defines the stored or generated energy."""
    battery: Optional[Any] = None
    tank: Optional[Any] = None
    fuel_cell: Optional[Any] = None
    solar: Optional[Any] = None
    supercapacitor: Optional[Any] = None
    name: Optional[Any] = None
    role: Optional[Any] = None
    group: Optional[Any] = None
    visual: Optional[Any] = None
    mesh: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "battery"), None)
        self.battery = BatterySource.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "tank"), None)
        self.tank = TankSource.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "fuel-cell"), None)
        self.fuel_cell = FuelCell.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "solar"), None)
        self.solar = SolarSource.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "supercapacitor"), None)
        self.supercapacitor = SupercapacitorSource.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        self.role = el.get("role")
        self.group = el.get("group")
        self.visual = el.get("visual")
        self.mesh = el.get("mesh")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.role is not None:
            el.set("role", self.role)
        if self.group is not None:
            el.set("group", self.group)
        if self.visual is not None:
            el.set("visual", self.visual)
        if self.mesh is not None:
            el.set("mesh", self.mesh)
        if self.battery is not None:
            el.append(self.battery.to_xml("battery"))
        if self.tank is not None:
            el.append(self.tank.to_xml("tank"))
        if self.fuel_cell is not None:
            el.append(self.fuel_cell.to_xml("fuel-cell"))
        if self.solar is not None:
            el.append(self.solar.to_xml("solar"))
        if self.supercapacitor is not None:
            el.append(self.supercapacitor.to_xml("supercapacitor"))
        return el

@dataclass
class PropSurface:
    """Propeller surface — generates thrust from rotation. Used on multirotors, fixed-wing aircraft, boats, and underwater vehicles. The associated motor provides torque; the propeller converts it to thrust based on diameter, pitch, and blade geometry."""
    diameter: Optional[Any] = None
    pitch: Optional[Any] = None
    blades: Optional[Any] = None
    direction: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "diameter"), None)
        self.diameter = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pitch"), None)
        self.pitch = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "blades"), None)
        self.blades = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "direction"), None)
        self.direction = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.diameter is not None:
            el.append(self.diameter.to_xml("diameter"))
        if self.pitch is not None:
            el.append(self.pitch.to_xml("pitch"))
        if self.blades is not None:
            _e = etree.SubElement(el, "blades"); _e.text = self.blades
        if self.direction is not None:
            _e = etree.SubElement(el, "direction"); _e.text = self.direction
        return el

@dataclass
class RadarParams:
    """Radar-specific parameters for 4D imaging radar and distance/velocity sensing. Applies to rf sensors with type="radar"."""
    frequency: Optional[Any] = None
    modulation: Optional[Any] = None
    azimuth_fov: Optional[Any] = None
    elevation_fov: Optional[Any] = None
    azimuth_resolution: Optional[Any] = None
    elevation_resolution: Optional[Any] = None
    range_resolution: Optional[Any] = None
    velocity_range: Optional[Any] = None
    velocity_resolution: Optional[Any] = None
    max_detections: Optional[Any] = None
    mimo: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "frequency"), None)
        self.frequency = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "modulation"), None)
        self.modulation = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "azimuth-fov"), None)
        self.azimuth_fov = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "elevation-fov"), None)
        self.elevation_fov = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "azimuth-resolution"), None)
        self.azimuth_resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "elevation-resolution"), None)
        self.elevation_resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range-resolution"), None)
        self.range_resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "velocity-range"), None)
        self.velocity_range = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "velocity-resolution"), None)
        self.velocity_resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-detections"), None)
        self.max_detections = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "mimo"), None)
        self.mimo = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.frequency is not None:
            el.append(self.frequency.to_xml("frequency"))
        if self.modulation is not None:
            _e = etree.SubElement(el, "modulation"); _e.text = self.modulation
        if self.azimuth_fov is not None:
            el.append(self.azimuth_fov.to_xml("azimuth-fov"))
        if self.elevation_fov is not None:
            el.append(self.elevation_fov.to_xml("elevation-fov"))
        if self.azimuth_resolution is not None:
            el.append(self.azimuth_resolution.to_xml("azimuth-resolution"))
        if self.elevation_resolution is not None:
            el.append(self.elevation_resolution.to_xml("elevation-resolution"))
        if self.range_resolution is not None:
            el.append(self.range_resolution.to_xml("range-resolution"))
        if self.velocity_range is not None:
            el.append(self.velocity_range.to_xml("velocity-range"))
        if self.velocity_resolution is not None:
            el.append(self.velocity_resolution.to_xml("velocity-resolution"))
        if self.max_detections is not None:
            _e = etree.SubElement(el, "max-detections"); _e.text = self.max_detections
        if self.mimo is not None:
            _e = etree.SubElement(el, "mimo"); _e.text = self.mimo
        return el

@dataclass
class RadiationSensor:
    """Radiation sensor for detecting ionizing radiation. Used on nuclear inspection robots, space robots, and environmental monitoring platforms. Extends sensor_base with radiation type attribute."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = RadiationSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class RangeValue:
    """A value with minimum and maximum bounds and an optional unit. The text content is the nominal value. Used for port voltage ranges, temperature operating ranges, and similar bounded quantities. SI defaults apply when unit is omitted (V for voltage, degC for temperature, etc.)."""
    text: Optional[str] = None
    unit: Optional[Any] = None
    min: Optional[Any] = None
    max: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.text = el.text
        self.unit = el.get("unit")
        self.min = el.get("min")
        self.max = el.get("max")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.unit is not None:
            el.set("unit", self.unit)
        if self.min is not None:
            el.set("min", self.min)
        if self.max is not None:
            el.set("max", self.max)
        if self.text is not None:
            el.text = self.text
        return el

@dataclass
class RatedValue:
    """A value with multiple rating levels for motor electrical parameters. Text content is the primary value. Used for motor voltage (default unit: V) and current (default unit: A) where different operating regimes apply (nominal steady-state, continuous thermal limit, short-term peak, absolute maximum). SI defaults apply when unit is omitted."""
    text: Optional[str] = None
    unit: Optional[Any] = None
    nominal: Optional[Any] = None
    continuous: Optional[Any] = None
    peak: Optional[Any] = None
    max: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.text = el.text
        self.unit = el.get("unit")
        self.nominal = el.get("nominal")
        self.continuous = el.get("continuous")
        self.peak = el.get("peak")
        self.max = el.get("max")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.unit is not None:
            el.set("unit", self.unit)
        if self.nominal is not None:
            el.set("nominal", self.nominal)
        if self.continuous is not None:
            el.set("continuous", self.continuous)
        if self.peak is not None:
            el.set("peak", self.peak)
        if self.max is not None:
            el.set("max", self.max)
        if self.text is not None:
            el.text = self.text
        return el

@dataclass
class RfSensor:
    """Radio-frequency sensor (GNSS receiver, UWB ranger, radar, radio altimeter). Extends sensor_base with type-specific parameter blocks for radar and GNSS."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    radar_params: Optional[Any] = None
    gnss_params: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "radar-params"), None)
        self.radar_params = RadarParams.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "gnss-params"), None)
        self.gnss_params = GnssParams.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = RfSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        if self.radar_params is not None:
            el.append(self.radar_params.to_xml("radar-params"))
        if self.gnss_params is not None:
            el.append(self.gnss_params.to_xml("gnss-params"))
        return el

@dataclass
class SafetyController:
    """URDF joint safety_controller (soft-limit controller). Zero cyber-physical meaning; carried for lossless URDF round-trip only. Mirrors urdf.xsd safety_controller (k_velocity required)."""
    soft_lower_limit: Optional[Any] = None
    soft_upper_limit: Optional[Any] = None
    k_position: Optional[Any] = None
    k_velocity: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.soft_lower_limit = el.get("soft_lower_limit")
        self.soft_upper_limit = el.get("soft_upper_limit")
        self.k_position = el.get("k_position")
        self.k_velocity = el.get("k_velocity")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.soft_lower_limit is not None:
            el.set("soft_lower_limit", self.soft_lower_limit)
        if self.soft_upper_limit is not None:
            el.set("soft_upper_limit", self.soft_upper_limit)
        if self.k_position is not None:
            el.set("k_position", self.k_position)
        if self.k_velocity is not None:
            el.set("k_velocity", self.k_velocity)
        return el

@dataclass
class Schedule:
    """IEEE 802.1Qbv Time-Aware Shaper gate control list template. Defines a repeating cycle of gate states that open/close transmission gates per traffic class. Each gate entry specifies which traffic classes can transmit and for how long. Semantic rule: gate durations must sum to cycle-time-us."""
    gate: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    cycle_time_us: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.gate = [Gate.from_xml(_c) for _c in _children(el) if _ln(_c) == "gate"]
        self.name = el.get("name")
        self.cycle_time_us = el.get("cycle-time-us")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.cycle_time_us is not None:
            el.set("cycle-time-us", self.cycle_time_us)
        for _i in self.gate:
            el.append(_i.to_xml("gate"))
        return el

@dataclass
class ScheduleAssign:
    """Assignment of a named schedule template to specific ports and/or chains. Binds a Qbv gate control list to the hardware ports that should execute it."""
    schedule: Optional[Any] = None
    ports: Optional[Any] = None
    chains: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.schedule = el.get("schedule")
        self.ports = el.get("ports")
        self.chains = el.get("chains")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.schedule is not None:
            el.set("schedule", self.schedule)
        if self.ports is not None:
            el.set("ports", self.ports)
        if self.chains is not None:
            el.set("chains", self.chains)
        return el

@dataclass
class ScheduleAssignment:
    """Container for all schedule-to-port/chain assignments in the network. Groups schedule_assign entries that bind Qbv templates to physical ports."""
    assign: List[Any] = field(default_factory=list)

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.assign = [ScheduleAssign.from_xml(_c) for _c in _children(el) if _ln(_c) == "assign"]
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        for _i in self.assign:
            el.append(_i.to_xml("assign"))
        return el

@dataclass
class SelfCollisionDisable:
    """List of component pairs to skip for self-collision checking. Dramatically improves motion planning performance — typically 80%+ of all link pairs can be skipped. Equivalent to SRDF disable_collisions."""
    pair: List[Any] = field(default_factory=list)

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.pair = [CollisionPair.from_xml(_c) for _c in _children(el) if _ln(_c) == "pair"]
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        for _i in self.pair:
            el.append(_i.to_xml("pair"))
        return el

@dataclass
class Sensor:
    """Sensor hardware container grouped by physical measurement category. Contains sub-elements for inertial (accel, gyro), electromagnetic (mag), optical (camera, lidar, tof, optical flow), RF (gnss, uwb, radar), chemical (gas, ph, humidity), force (strain, pressure, torque, load cell), encoder (position/velocity feedback), temperature (thermistor, RTD, thermocouple, IR), radiation (geiger, scintillation, neutron, dosimeter), audio (microphone, ultrasonic, sonar, hydrophone), and tactile (capacitive,"""
    inertial: List[Any] = field(default_factory=list)
    em: List[Any] = field(default_factory=list)
    optical: List[Any] = field(default_factory=list)
    rf: List[Any] = field(default_factory=list)
    chemical: List[Any] = field(default_factory=list)
    force: List[Any] = field(default_factory=list)
    encoder: List[Any] = field(default_factory=list)
    temperature: List[Any] = field(default_factory=list)
    radiation: List[Any] = field(default_factory=list)
    audio: List[Any] = field(default_factory=list)
    tactile: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    update_rate: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.inertial = [InertialSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "inertial"]
        self.em = [EmSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "em"]
        self.optical = [OpticalSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "optical"]
        self.rf = [RfSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "rf"]
        self.chemical = [ChemicalSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "chemical"]
        self.force = [ForceSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "force"]
        self.encoder = [EncoderSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "encoder"]
        self.temperature = [TemperatureSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "temperature"]
        self.radiation = [RadiationSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "radiation"]
        self.audio = [AudioSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "audio"]
        self.tactile = [TactileSensor.from_xml(_c) for _c in _children(el) if _ln(_c) == "tactile"]
        self.name = el.get("name")
        self.update_rate = el.get("update-rate")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.update_rate is not None:
            el.set("update-rate", self.update_rate)
        for _i in self.inertial:
            el.append(_i.to_xml("inertial"))
        for _i in self.em:
            el.append(_i.to_xml("em"))
        for _i in self.optical:
            el.append(_i.to_xml("optical"))
        for _i in self.rf:
            el.append(_i.to_xml("rf"))
        for _i in self.chemical:
            el.append(_i.to_xml("chemical"))
        for _i in self.force:
            el.append(_i.to_xml("force"))
        for _i in self.encoder:
            el.append(_i.to_xml("encoder"))
        for _i in self.temperature:
            el.append(_i.to_xml("temperature"))
        for _i in self.radiation:
            el.append(_i.to_xml("radiation"))
        for _i in self.audio:
            el.append(_i.to_xml("audio"))
        for _i in self.tactile:
            el.append(_i.to_xml("tactile"))
        return el

@dataclass
class SensorBase:
    """Base sensor type with elements common to ALL sensor subtypes: pose, driver, range, resolution, noise, data output, and geometry. Extended by em, rf, chemical, force, and encoder sensor types. ODR/bandwidth/FIFO are NOT included here — those are specific to high-rate inertial sensors and live in sensor_params (accel/gyro blocks)."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class SensorDriver:
    """Sensor driver identification and configuration. Maps the physical sensor to a software driver by name and provides axis alignment correction for the driver's coordinate frame."""
    axis_align: Optional[Any] = None
    name: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "axis-align"), None)
        self.axis_align = AxisAlign.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.axis_align is not None:
            el.append(self.axis_align.to_xml("axis-align"))
        return el

@dataclass
class SensorFov:
    """Field-of-view definition for an optical sensor. Each FoV represents a distinct sensing cone/pyramid (e.g., a stereo camera has two FoVs). Contains the FoV geometry, design-spec intrinsics, distortion model, calibration results, and noise parameters. The intrinsics/distortion elements hold nominal datasheet values; the calibration element holds measured values from an actual calibration on this specific unit. If calibration is present, agents should use it over the design specs."""
    description: Optional[Any] = None
    pose: Optional[Any] = None
    geometry: Optional[Any] = None
    intrinsics: Optional[Any] = None
    distortion: Optional[Any] = None
    calibration: Optional[Any] = None
    noise: Optional[Any] = None
    name: Optional[Any] = None
    color: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "intrinsics"), None)
        self.intrinsics = CameraIntrinsics.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "distortion"), None)
        self.distortion = CameraDistortion.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "calibration"), None)
        self.calibration = CameraCalibration.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        self.color = el.get("color")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.color is not None:
            el.set("color", self.color)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        if self.intrinsics is not None:
            el.append(self.intrinsics.to_xml("intrinsics"))
        if self.distortion is not None:
            el.append(self.distortion.to_xml("distortion"))
        if self.calibration is not None:
            el.append(self.calibration.to_xml("calibration"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        return el

@dataclass
class SensorParams:
    """Measurement parameters for a sensor axis or channel. Defines the operating range, resolution, sampling rate (ODR), anti-aliasing filter bandwidth, FIFO configuration, and noise model. ODR and bandwidth are distinct: ODR is the hardware sampling rate, bandwidth is the LPF cutoff frequency. The effective processing rate depends on ODR and FIFO watermark."""
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    odr: Optional[Any] = None
    bandwidth: Optional[Any] = None
    fifo: Optional[Any] = None
    noise: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "odr"), None)
        self.odr = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "bandwidth"), None)
        self.bandwidth = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "fifo"), None)
        self.fifo = FifoConfig.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.odr is not None:
            el.append(self.odr.to_xml("odr"))
        if self.bandwidth is not None:
            el.append(self.bandwidth.to_xml("bandwidth"))
        if self.fifo is not None:
            el.append(self.fifo.to_xml("fifo"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        return el

@dataclass
class Software:
    """Firmware or software identity for a component. Used for firmware manifest tracking, version management, and over-the-air update planning."""
    version: Optional[Any] = None
    hash: Optional[Any] = None
    firmware_manifest_uri: Optional[Any] = None
    params: Optional[Any] = None
    name: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "version"), None)
        self.version = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "hash"), None)
        self.hash = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "firmware-manifest-uri"), None)
        self.firmware_manifest_uri = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "params"), None)
        self.params = _c.text if _c is not None else None
        self.name = el.get("name")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.version is not None:
            _e = etree.SubElement(el, "version"); _e.text = self.version
        if self.hash is not None:
            _e = etree.SubElement(el, "hash"); _e.text = self.hash
        if self.firmware_manifest_uri is not None:
            _e = etree.SubElement(el, "firmware-manifest-uri"); _e.text = self.firmware_manifest_uri
        if self.params is not None:
            _e = etree.SubElement(el, "params"); _e.text = self.params
        return el

@dataclass
class SolarSource:
    """Solar panel energy source — converts sunlight to electricity via photovoltaic cells. Used on agricultural robots, exploration rovers, long-endurance UAS, outdoor monitoring platforms, and space systems. Solar provides continuous power during daylight but output varies with irradiance, temperature, panel orientation, and shading. An agent combines solar peak-power with battery capacity to estimate sustained runtime: during daylight the panel charges the battery and powers the system, at night the"""
    cell_type: Optional[Any] = None
    peak_power: Optional[Any] = None
    area: Optional[Any] = None
    efficiency: Optional[Any] = None
    voltage_mpp: Optional[Any] = None
    current_mpp: Optional[Any] = None
    open_circuit_voltage: Optional[Any] = None
    mppt: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "cell-type"), None)
        self.cell_type = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "peak-power"), None)
        self.peak_power = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "area"), None)
        self.area = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "efficiency"), None)
        self.efficiency = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "voltage-mpp"), None)
        self.voltage_mpp = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "current-mpp"), None)
        self.current_mpp = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "open-circuit-voltage"), None)
        self.open_circuit_voltage = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "mppt"), None)
        self.mppt = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.cell_type is not None:
            _e = etree.SubElement(el, "cell-type"); _e.text = self.cell_type
        if self.peak_power is not None:
            el.append(self.peak_power.to_xml("peak-power"))
        if self.area is not None:
            el.append(self.area.to_xml("area"))
        if self.efficiency is not None:
            _e = etree.SubElement(el, "efficiency"); _e.text = self.efficiency
        if self.voltage_mpp is not None:
            el.append(self.voltage_mpp.to_xml("voltage-mpp"))
        if self.current_mpp is not None:
            el.append(self.current_mpp.to_xml("current-mpp"))
        if self.open_circuit_voltage is not None:
            el.append(self.open_circuit_voltage.to_xml("open-circuit-voltage"))
        if self.mppt is not None:
            _e = etree.SubElement(el, "mppt"); _e.text = self.mppt
        return el

@dataclass
class Sphere:
    """Sphere geometry primitive centered at the parent pose origin."""
    radius: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "radius"), None)
        self.radius = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.radius is not None:
            _e = etree.SubElement(el, "radius"); _e.text = self.radius
        return el

@dataclass
class Spur:
    """Spur connection from a chain hop to a device not in the main chain path. Represents a branch connection where a chain hop's switch port bridges to an external device (e.g., a sensor or actuator hanging off a chain node via an additional Ethernet port)."""
    description: Optional[Any] = None
    vlan: Optional[Any] = None
    device: Optional[Any] = None
    port: Optional[Any] = None
    via: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "vlan"), None)
        self.vlan = Vlan.from_xml(_c) if _c is not None else None
        self.device = el.get("device")
        self.port = el.get("port")
        self.via = el.get("via")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.device is not None:
            el.set("device", self.device)
        if self.port is not None:
            el.set("port", self.port)
        if self.via is not None:
            el.set("via", self.via)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.vlan is not None:
            el.append(self.vlan.to_xml("vlan"))
        return el

@dataclass
class StreamProfileRef:
    """Reference to an external stream profile definition file (.streams.xml). Stream profiles define individual TSN streams (talker, listener, bandwidth, latency requirements) separately from the HCDF topology to allow independent versioning and reuse across robots."""
    uri: Optional[Any] = None
    sha: Optional[Any] = None
    active: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.uri = el.get("uri")
        self.sha = el.get("sha")
        self.active = el.get("active")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.uri is not None:
            el.set("uri", self.uri)
        if self.sha is not None:
            el.set("sha", self.sha)
        if self.active is not None:
            el.set("active", self.active)
        return el

@dataclass
class SupercapacitorSource:
    """Supercapacitor (ultracapacitor) energy source — stores energy electrostatically rather than chemically. Compared to batteries: much higher power density (fast charge/discharge in seconds), much lower energy density (stores less total energy), and near-unlimited cycle life (millions of cycles vs hundreds for batteries). Used for peak power buffering (absorbs regenerative braking energy, provides burst current for motor acceleration), bridge power during battery hot-swap, and smoothing pulsed load"""
    capacitance: Optional[Any] = None
    max_voltage: Optional[Any] = None
    esr: Optional[Any] = None
    max_current: Optional[Any] = None
    peak_current: Optional[Any] = None
    energy: Optional[Any] = None
    cells_series: Optional[Any] = None
    cycle_life: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "capacitance"), None)
        self.capacitance = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-voltage"), None)
        self.max_voltage = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "esr"), None)
        self.esr = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-current"), None)
        self.max_current = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "peak-current"), None)
        self.peak_current = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "energy"), None)
        self.energy = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cells-series"), None)
        self.cells_series = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cycle-life"), None)
        self.cycle_life = _c.text if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.capacitance is not None:
            el.append(self.capacitance.to_xml("capacitance"))
        if self.max_voltage is not None:
            el.append(self.max_voltage.to_xml("max-voltage"))
        if self.esr is not None:
            el.append(self.esr.to_xml("esr"))
        if self.max_current is not None:
            el.append(self.max_current.to_xml("max-current"))
        if self.peak_current is not None:
            el.append(self.peak_current.to_xml("peak-current"))
        if self.energy is not None:
            el.append(self.energy.to_xml("energy"))
        if self.cells_series is not None:
            _e = etree.SubElement(el, "cells-series"); _e.text = self.cells_series
        if self.cycle_life is not None:
            _e = etree.SubElement(el, "cycle-life"); _e.text = self.cycle_life
        return el

@dataclass
class Surface:
    """Inline contact physics for a collision shape: friction, restitution (bounciness), and contact dynamics used by physics simulators. Appears only under <collision>. Inline-only (no named contact-material library) -- matching URDF (per-link <gazebo> mu/kp) and SDF (per-collision <surface>), neither of which has a reusable contact handle. Not to be confused with <dynamic-surface> (functional aero/wheel/gripper surfaces on a comp)."""
    friction: Optional[Any] = None
    restitution: Optional[Any] = None
    contact: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "friction"), None)
        self.friction = Friction.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "restitution"), None)
        self.restitution = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "contact"), None)
        self.contact = Contact.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.friction is not None:
            el.append(self.friction.to_xml("friction"))
        if self.restitution is not None:
            _e = etree.SubElement(el, "restitution"); _e.text = self.restitution
        if self.contact is not None:
            el.append(self.contact.to_xml("contact"))
        return el

@dataclass
class Switch:
    """Ethernet switching fabric that bridges ports together. Ports inside switch are connected by the switching ASIC and can forward frames between each other. Ports directly on comp (outside switch) are independent. The chip attribute identifies the switch IC (e.g., sja1110, s32n79-integrated). Omit chip for software bridges."""
    port: List[Any] = field(default_factory=list)
    tsn: Optional[Any] = None
    macsec: Optional[Any] = None
    chip: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.port = [Port.from_xml(_c) for _c in _children(el) if _ln(_c) == "port"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "tsn"), None)
        self.tsn = SwitchTsn.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "macsec"), None)
        self.macsec = SwitchMacsec.from_xml(_c) if _c is not None else None
        self.chip = el.get("chip")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.chip is not None:
            el.set("chip", self.chip)
        for _i in self.port:
            el.append(_i.to_xml("port"))
        if self.tsn is not None:
            el.append(self.tsn.to_xml("tsn"))
        if self.macsec is not None:
            el.append(self.macsec.to_xml("macsec"))
        return el

@dataclass
class SwitchMacsec:
    """MACsec hardware capabilities of a switch fabric. Lists cipher suites supported by the switch ASIC's MACsec engine for all bridged ports."""
    cipher: List[Any] = field(default_factory=list)

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.cipher = [MacsecCipher(_c.text) for _c in _children(el) if _ln(_c) == "cipher"]
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        for _i in self.cipher:
            _e = etree.SubElement(el, "cipher"); _e.text = _i.value
        return el

@dataclass
class SwitchTsn:
    """TSN capabilities of a switch fabric (as opposed to per-port capabilities). Declares which TSN sub-standards the switch ASIC supports globally across all its bridged ports."""
    qbv: Optional[Any] = None
    cbs: Optional[Any] = None
    psfp: Optional[Any] = None
    frer: Optional[Any] = None
    preemption: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "qbv"), None)
        self.qbv = TsnQbvCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cbs"), None)
        self.cbs = TsnCbsCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "psfp"), None)
        self.psfp = TsnPsfpCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "frer"), None)
        self.frer = TsnFrerCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "preemption"), None)
        self.preemption = TsnPreemptionCapability.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.qbv is not None:
            el.append(self.qbv.to_xml("qbv"))
        if self.cbs is not None:
            el.append(self.cbs.to_xml("cbs"))
        if self.psfp is not None:
            el.append(self.psfp.to_xml("psfp"))
        if self.frer is not None:
            el.append(self.frer.to_xml("frer"))
        if self.preemption is not None:
            el.append(self.preemption.to_xml("preemption"))
        return el

@dataclass
class TactileSensor:
    """Tactile sensor for distributed contact/pressure measurement across a surface. Unlike force sensors (single-point measurement), tactile sensors provide spatially distributed pressure data across an array of sensing elements (taxels). Used on robot fingertips, palms, grippers, and body skin for grasp planning, slip detection, object recognition by touch, and safe physical interaction. Extends sensor_base with tactile-specific parameters: array dimensions, taxel count, spatial resolution, and press"""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    rows: Optional[Any] = None
    cols: Optional[Any] = None
    taxels: Optional[Any] = None
    spatial_resolution: Optional[Any] = None
    pressure_range: Optional[Any] = None
    sensing_area: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "rows"), None)
        self.rows = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cols"), None)
        self.cols = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "taxels"), None)
        self.taxels = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "spatial-resolution"), None)
        self.spatial_resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pressure-range"), None)
        self.pressure_range = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "sensing-area"), None)
        self.sensing_area = MeasuredValue.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = TactileSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        if self.rows is not None:
            _e = etree.SubElement(el, "rows"); _e.text = self.rows
        if self.cols is not None:
            _e = etree.SubElement(el, "cols"); _e.text = self.cols
        if self.taxels is not None:
            _e = etree.SubElement(el, "taxels"); _e.text = self.taxels
        if self.spatial_resolution is not None:
            el.append(self.spatial_resolution.to_xml("spatial-resolution"))
        if self.pressure_range is not None:
            el.append(self.pressure_range.to_xml("pressure-range"))
        if self.sensing_area is not None:
            el.append(self.sensing_area.to_xml("sensing-area"))
        return el

@dataclass
class TankSource:
    """Fuel/gas tank energy source. For combustion engines, hydrogen fuel cells, compressed air systems, or pneumatic actuators. Defines fuel type, volume, pressure, energy content, and flow characterization (how fluid exits and at what rate). An agent needs both capacity (how much energy is stored) and flow (how fast it can be delivered) to compute runtime and power capability."""
    fuel: Optional[Any] = None
    volume: Optional[Any] = None
    pressure: Optional[Any] = None
    energy: Optional[Any] = None
    flow: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "fuel"), None)
        self.fuel = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "volume"), None)
        self.volume = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "pressure"), None)
        self.pressure = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "energy"), None)
        self.energy = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "flow"), None)
        self.flow = FlowCharacterization.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.fuel is not None:
            _e = etree.SubElement(el, "fuel"); _e.text = self.fuel
        if self.volume is not None:
            el.append(self.volume.to_xml("volume"))
        if self.pressure is not None:
            el.append(self.pressure.to_xml("pressure"))
        if self.energy is not None:
            el.append(self.energy.to_xml("energy"))
        if self.flow is not None:
            el.append(self.flow.to_xml("flow"))
        return el

@dataclass
class TemperatureSensor:
    """Temperature sensor — thermistor, RTD, thermocouple, or IR. Common use: motor winding temperature monitoring (NTC thermistor in motor signal connector), ambient temperature, battery cell temperature. Extends sensor_base with temperature sensor type attribute."""
    pose: Optional[Any] = None
    driver: Optional[Any] = None
    range: Optional[Any] = None
    resolution: Optional[Any] = None
    noise: Optional[Any] = None
    data_output: Optional[Any] = None
    geometry: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "driver"), None)
        self.driver = SensorDriver.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "range"), None)
        self.range = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "resolution"), None)
        self.resolution = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "noise"), None)
        self.noise = Noise.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "data-output"), None)
        self.data_output = DataOutput.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = Geometry.from_xml(_c) if _c is not None else None
        _v = el.get("type")
        self.type = TemperatureSensorType(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.driver is not None:
            el.append(self.driver.to_xml("driver"))
        if self.range is not None:
            el.append(self.range.to_xml("range"))
        if self.resolution is not None:
            el.append(self.resolution.to_xml("resolution"))
        if self.noise is not None:
            el.append(self.noise.to_xml("noise"))
        if self.data_output is not None:
            el.append(self.data_output.to_xml("data-output"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        return el

@dataclass
class TrackSurface:
    """Tracked vehicle surface (tank treads, rubber tracks). Defines the track geometry and ground contact properties. Used for high-traction vehicles on rough terrain."""
    width: Optional[Any] = None
    length: Optional[Any] = None
    ground_pressure: Optional[Any] = None
    friction: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "width"), None)
        self.width = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "length"), None)
        self.length = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "ground-pressure"), None)
        self.ground_pressure = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "friction"), None)
        self.friction = Friction.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.width is not None:
            el.append(self.width.to_xml("width"))
        if self.length is not None:
            el.append(self.length.to_xml("length"))
        if self.ground_pressure is not None:
            el.append(self.ground_pressure.to_xml("ground-pressure"))
        if self.friction is not None:
            el.append(self.friction.to_xml("friction"))
        return el

@dataclass
class TrafficClass:
    """Mapping between a traffic class number, a PCP value, and its QoS properties. Defines how frames are classified into priority queues for Qbv scheduling and CBS shaping."""
    tc: Optional[Any] = None
    pcp: Optional[Any] = None
    name: Optional[Any] = None
    description: Optional[Any] = None
    preemption: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.tc = el.get("tc")
        self.pcp = el.get("pcp")
        self.name = el.get("name")
        self.description = el.get("description")
        _v = el.get("preemption")
        self.preemption = PreemptionClass(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.tc is not None:
            el.set("tc", self.tc)
        if self.pcp is not None:
            el.set("pcp", self.pcp)
        if self.name is not None:
            el.set("name", self.name)
        if self.description is not None:
            el.set("description", self.description)
        if self.preemption is not None:
            el.set("preemption", self.preemption.value)
        return el

@dataclass
class TrafficClasses:
    """Traffic class definitions for the network. Maps PCP values to traffic class numbers and optionally assigns preemption behavior. Used by Qbv schedules and CBS shapers to determine queue assignments."""
    class_: List[Any] = field(default_factory=list)
    count: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.class_ = [TrafficClass.from_xml(_c) for _c in _children(el) if _ln(_c) == "class"]
        self.count = el.get("count")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.count is not None:
            el.set("count", self.count)
        for _i in self.class_:
            el.append(_i.to_xml("class"))
        return el

@dataclass
class Transmission:
    """Mechanical power transmission connecting a motor to a joint. Defines the gear reduction ratio, efficiency, backlash, and optional compliant elements (springs) for the mechanical coupling between motor output and joint input. A transmission without spring elements is a rigid actuator. Adding a series spring creates a Series Elastic Actuator (SEA). Adding a parallel spring creates a Parallel Elastic Actuator (PEA). Both can be combined. Advanced variants (CPEA, AE-PEA, VSA) are expressed through s"""
    motor: Optional[Any] = None
    joint: Optional[Any] = None
    reduction: Optional[Any] = None
    efficiency: Optional[Any] = None
    backlash: Optional[Any] = None
    spring: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    type: Optional[Any] = None
    encoder: Optional[Any] = None
    visual: Optional[Any] = None
    mesh: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "motor"), None)
        self.motor = TransmissionEndpoint.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "joint"), None)
        self.joint = TransmissionEndpoint.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "reduction"), None)
        self.reduction = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "efficiency"), None)
        self.efficiency = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "backlash"), None)
        self.backlash = MeasuredValue.from_xml(_c) if _c is not None else None
        self.spring = [TransmissionSpring.from_xml(_c) for _c in _children(el) if _ln(_c) == "spring"]
        self.name = el.get("name")
        _v = el.get("type")
        self.type = TransmissionType(_v) if _v is not None else None
        self.encoder = el.get("encoder")
        self.visual = el.get("visual")
        self.mesh = el.get("mesh")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.type is not None:
            el.set("type", self.type.value)
        if self.encoder is not None:
            el.set("encoder", self.encoder)
        if self.visual is not None:
            el.set("visual", self.visual)
        if self.mesh is not None:
            el.set("mesh", self.mesh)
        if self.motor is not None:
            el.append(self.motor.to_xml("motor"))
        if self.joint is not None:
            el.append(self.joint.to_xml("joint"))
        if self.reduction is not None:
            _e = etree.SubElement(el, "reduction"); _e.text = self.reduction
        if self.efficiency is not None:
            _e = etree.SubElement(el, "efficiency"); _e.text = self.efficiency
        if self.backlash is not None:
            el.append(self.backlash.to_xml("backlash"))
        for _i in self.spring:
            el.append(_i.to_xml("spring"))
        return el

@dataclass
class TransmissionEndpoint:
    """Reference to a motor or joint endpoint in a transmission. Uses a dotted path "comp.element" to identify the target across components."""
    ref: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.ref = el.get("ref")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.ref is not None:
            el.set("ref", self.ref)
        return el

@dataclass
class TransmissionSpring:
    """Compliant element (spring) in a transmission drivetrain. The presence of a spring element is what distinguishes compliant actuators from rigid ones. A spring in series between the gearbox and the joint output creates a Series Elastic Actuator (SEA) — enabling torque sensing via deflection measurement, impact absorption, backdrivability through high-ratio gearboxes, and inherent safety for human interaction. A spring in parallel with the motor creates a Parallel Elastic Actuator (PEA) — providing"""
    stiffness: Optional[Any] = None
    max_deflection: Optional[Any] = None
    damping: Optional[Any] = None
    equilibrium: Optional[Any] = None
    equilibrium_range: Optional[Any] = None
    clutch_power: Optional[Any] = None
    engage_time: Optional[Any] = None
    placement: Optional[Any] = None
    type: Optional[Any] = None
    torque_sensing: Optional[Any] = None
    clutch: Optional[Any] = None
    equilibrium_actuator: Optional[Any] = None
    stiffness_actuator: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "stiffness"), None)
        self.stiffness = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "max-deflection"), None)
        self.max_deflection = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "damping"), None)
        self.damping = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "equilibrium"), None)
        self.equilibrium = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "equilibrium-range"), None)
        self.equilibrium_range = RangeValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "clutch-power"), None)
        self.clutch_power = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "engage-time"), None)
        self.engage_time = MeasuredValue.from_xml(_c) if _c is not None else None
        self.placement = el.get("placement")
        self.type = el.get("type")
        self.torque_sensing = el.get("torque-sensing")
        self.clutch = el.get("clutch")
        self.equilibrium_actuator = el.get("equilibrium-actuator")
        self.stiffness_actuator = el.get("stiffness-actuator")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.placement is not None:
            el.set("placement", self.placement)
        if self.type is not None:
            el.set("type", self.type)
        if self.torque_sensing is not None:
            el.set("torque-sensing", self.torque_sensing)
        if self.clutch is not None:
            el.set("clutch", self.clutch)
        if self.equilibrium_actuator is not None:
            el.set("equilibrium-actuator", self.equilibrium_actuator)
        if self.stiffness_actuator is not None:
            el.set("stiffness-actuator", self.stiffness_actuator)
        if self.stiffness is not None:
            el.append(self.stiffness.to_xml("stiffness"))
        if self.max_deflection is not None:
            el.append(self.max_deflection.to_xml("max-deflection"))
        if self.damping is not None:
            el.append(self.damping.to_xml("damping"))
        if self.equilibrium is not None:
            el.append(self.equilibrium.to_xml("equilibrium"))
        if self.equilibrium_range is not None:
            el.append(self.equilibrium_range.to_xml("equilibrium-range"))
        if self.clutch_power is not None:
            el.append(self.clutch_power.to_xml("clutch-power"))
        if self.engage_time is not None:
            el.append(self.engage_time.to_xml("engage-time"))
        return el

@dataclass
class TsnAtsCapability:
    """IEEE 802.1Qcr Asynchronous Traffic Shaping capability. Provides per-stream shaping without requiring time synchronization, useful for non-gPTP-capable devices."""
    max_groups: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.max_groups = el.get("max-groups")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.max_groups is not None:
            el.set("max-groups", self.max_groups)
        return el

@dataclass
class TsnCbsCapability:
    """IEEE 802.1Qav Credit-Based Shaper capability. Provides bandwidth reservation for AVB/TSN streams by limiting burst size via credit accumulation. Presence indicates CBS support. The max-sr-classes attribute specifies how many SR (Stream Reservation) classes the port hardware supports."""
    max_sr_classes: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.max_sr_classes = el.get("max-sr-classes")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.max_sr_classes is not None:
            el.set("max-sr-classes", self.max_sr_classes)
        return el

@dataclass
class TsnFrerCapability:
    """IEEE 802.1CB Frame Replication and Elimination for Reliability capability. Provides seamless redundancy by duplicating streams over disjoint paths and eliminating duplicates at the receiver."""
    seq_encoding: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.seq_encoding = el.get("seq-encoding")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.seq_encoding is not None:
            el.set("seq-encoding", self.seq_encoding)
        return el

@dataclass
class TsnGptpCapability:
    """IEEE 802.1AS gPTP capability of a port. Indicates whether the port supports hardware timestamping and grandmaster election."""
    hw_timestamping: Optional[Any] = None
    gm_capable: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.hw_timestamping = el.get("hw-timestamping")
        self.gm_capable = el.get("gm-capable")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.hw_timestamping is not None:
            el.set("hw-timestamping", self.hw_timestamping)
        if self.gm_capable is not None:
            el.set("gm-capable", self.gm_capable)
        return el

@dataclass
class TsnPortCapabilities:
    """TSN (Time-Sensitive Networking) capabilities of a port. Declares which IEEE 802.1 TSN sub-standards the port hardware supports. Used by network planners to determine valid schedule and stream placements."""
    gptp: Optional[Any] = None
    qbv: Optional[Any] = None
    cbs: Optional[Any] = None
    preemption: Optional[Any] = None
    psfp: Optional[Any] = None
    frer: Optional[Any] = None
    ats: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "gptp"), None)
        self.gptp = TsnGptpCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "qbv"), None)
        self.qbv = TsnQbvCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cbs"), None)
        self.cbs = TsnCbsCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "preemption"), None)
        self.preemption = TsnPreemptionCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "psfp"), None)
        self.psfp = TsnPsfpCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "frer"), None)
        self.frer = TsnFrerCapability.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "ats"), None)
        self.ats = TsnAtsCapability.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.gptp is not None:
            el.append(self.gptp.to_xml("gptp"))
        if self.qbv is not None:
            el.append(self.qbv.to_xml("qbv"))
        if self.cbs is not None:
            el.append(self.cbs.to_xml("cbs"))
        if self.preemption is not None:
            el.append(self.preemption.to_xml("preemption"))
        if self.psfp is not None:
            el.append(self.psfp.to_xml("psfp"))
        if self.frer is not None:
            el.append(self.frer.to_xml("frer"))
        if self.ats is not None:
            el.append(self.ats.to_xml("ats"))
        return el

@dataclass
class TsnPreemptionCapability:
    """IEEE 802.1Qbu/802.3br frame preemption capability. Allows express traffic to interrupt preemptable frames mid-transmission, reducing worst-case latency for high-priority traffic."""
    min_fragment_size: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.min_fragment_size = el.get("min-fragment-size")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.min_fragment_size is not None:
            el.set("min-fragment-size", self.min_fragment_size)
        return el

@dataclass
class TsnPsfpCapability:
    """IEEE 802.1Qci Per-Stream Filtering and Policing capability. Provides ingress filtering, rate policing, and stream identification for protecting against misbehaving talkers."""
    max_filters: Optional[Any] = None
    max_gates: Optional[Any] = None
    max_meters: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.max_filters = el.get("max-filters")
        self.max_gates = el.get("max-gates")
        self.max_meters = el.get("max-meters")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.max_filters is not None:
            el.set("max-filters", self.max_filters)
        if self.max_gates is not None:
            el.set("max-gates", self.max_gates)
        if self.max_meters is not None:
            el.set("max-meters", self.max_meters)
        return el

@dataclass
class TsnQbvCapability:
    """IEEE 802.1Qbv Time-Aware Shaper capability of a port. Defines the port's gate scheduling hardware resources."""
    traffic_classes: Optional[Any] = None
    hw_queues: Optional[Any] = None
    max_gcl_entries: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.traffic_classes = el.get("traffic-classes")
        self.hw_queues = el.get("hw-queues")
        self.max_gcl_entries = el.get("max-gcl-entries")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.traffic_classes is not None:
            el.set("traffic-classes", self.traffic_classes)
        if self.hw_queues is not None:
            el.set("hw-queues", self.hw_queues)
        if self.max_gcl_entries is not None:
            el.set("max-gcl-entries", self.max_gcl_entries)
        return el

@dataclass
class Tunnel:
    """Tunnel configuration for carrying one chain's traffic encapsulated over another chain or link. Used for EtherCAT-over-TSN (IEEE 1722 encapsulation) or EtherCAT-over-EtherCAT (ETG.1700) scenarios."""
    chain: Optional[Any] = None
    hop: Optional[Any] = None
    protocol: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.chain = el.get("chain")
        self.hop = el.get("hop")
        _v = el.get("protocol")
        self.protocol = TunnelProtocol(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.chain is not None:
            el.set("chain", self.chain)
        if self.hop is not None:
            el.set("hop", self.hop)
        if self.protocol is not None:
            el.set("protocol", self.protocol.value)
        return el

@dataclass
class UrdfCompatComp:
    """Container for URDF-only, zero-CPS fields on a component, kept out of the clean core. Optional; omit for native HCDF authoring."""
    link_type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.link_type = el.get("link-type")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.link_type is not None:
            el.set("link-type", self.link_type)
        return el

@dataclass
class UrdfCompatJoint:
    """Container for URDF-only, zero-CPS fields on a joint, kept out of the clean core. Optional; omit for native HCDF authoring."""
    safety_controller: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "safety_controller"), None)
        self.safety_controller = SafetyController.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.safety_controller is not None:
            el.append(self.safety_controller.to_xml("safety_controller"))
        return el

@dataclass
class Visual:
    """Visual representation of a component for rendering. Contains either a high-fidelity 3D model reference or a geometry primitive fallback (or both, where model is preferred and geometry is the fallback). Multiple visuals per component are allowed for multi-part rendering."""
    pose: Optional[Any] = None
    model: Optional[Any] = None
    geometry: Optional[Any] = None
    color: Optional[Any] = None
    name: Optional[Any] = None
    toggle: Optional[Any] = None
    name_origin: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "pose"), None)
        self.pose = Pose.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "model"), None)
        self.model = ModelRef.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "geometry"), None)
        self.geometry = VisualGeometry.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "color"), None)
        self.color = Color.from_xml(_c) if _c is not None else None
        self.name = el.get("name")
        self.toggle = el.get("toggle")
        _v = el.get("name-origin")
        self.name_origin = NameOrigin(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.toggle is not None:
            el.set("toggle", self.toggle)
        if self.name_origin is not None:
            el.set("name-origin", self.name_origin.value)
        if self.pose is not None:
            el.append(self.pose.to_xml("pose"))
        if self.model is not None:
            el.append(self.model.to_xml("model"))
        if self.geometry is not None:
            el.append(self.geometry.to_xml("geometry"))
        if self.color is not None:
            el.append(self.color.to_xml("color"))
        return el

@dataclass
class VisualGeometry:
    """Visual geometry: primitive shapes ONLY (box, cylinder, sphere, capsule, cone, ellipsoid). There is deliberately NO <mesh> here -- a *visual* mesh is the self-contained GLB/glTF <model> (the rich-appearance arm of <visual>), never a bare geometry mesh. So "a visual mesh is GLB-only" is enforced by the grammar itself. Frustum is excluded (sensor-FOV only). Used in <visual>'s flat/primitive arm with an optional flat <material> color."""
    box: Optional[Any] = None
    cylinder: Optional[Any] = None
    sphere: Optional[Any] = None
    capsule: Optional[Any] = None
    cone: Optional[Any] = None
    ellipsoid: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "box"), None)
        self.box = Box.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cylinder"), None)
        self.cylinder = Cylinder.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "sphere"), None)
        self.sphere = Sphere.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "capsule"), None)
        self.capsule = Capsule.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "cone"), None)
        self.cone = Cone.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "ellipsoid"), None)
        self.ellipsoid = Ellipsoid.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.box is not None:
            el.append(self.box.to_xml("box"))
        if self.cylinder is not None:
            el.append(self.cylinder.to_xml("cylinder"))
        if self.sphere is not None:
            el.append(self.sphere.to_xml("sphere"))
        if self.capsule is not None:
            el.append(self.capsule.to_xml("capsule"))
        if self.cone is not None:
            el.append(self.cone.to_xml("cone"))
        if self.ellipsoid is not None:
            el.append(self.ellipsoid.to_xml("ellipsoid"))
        return el

@dataclass
class Vlan:
    """IEEE 802.1Q VLAN tag assignment for a link, chain, or spur. Segments network traffic into virtual LANs for isolation and QoS mapping."""
    id: Optional[Any] = None
    pcp: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.id = el.get("id")
        self.pcp = el.get("pcp")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.id is not None:
            el.set("id", self.id)
        if self.pcp is not None:
            el.set("pcp", self.pcp)
        return el

@dataclass
class VoltageCapability:
    """Port voltage capability. Text content is the nominal voltage. Attributes define the acceptable operating range. Used for power ports and powered Ethernet (PoDL)."""
    text: Optional[str] = None
    unit: Optional[Any] = None
    min: Optional[Any] = None
    max: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.text = el.text
        self.unit = el.get("unit")
        self.min = el.get("min")
        self.max = el.get("max")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.unit is not None:
            el.set("unit", self.unit)
        if self.min is not None:
            el.set("min", self.min)
        if self.max is not None:
            el.set("max", self.max)
        if self.text is not None:
            el.text = self.text
        return el

@dataclass
class WheelSurface:
    """Wheel ground contact surface. Defines the tire/wheel geometry and friction properties for ground vehicles. The motor driving this wheel is defined separately; the wheel surface describes the contact interface with the ground."""
    radius: Optional[Any] = None
    width: Optional[Any] = None
    friction: Optional[Any] = None
    type: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "radius"), None)
        self.radius = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "width"), None)
        self.width = MeasuredValue.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "friction"), None)
        self.friction = Friction.from_xml(_c) if _c is not None else None
        self.type = el.get("type")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.type is not None:
            el.set("type", self.type)
        if self.radius is not None:
            el.append(self.radius.to_xml("radius"))
        if self.width is not None:
            el.append(self.width.to_xml("width"))
        if self.friction is not None:
            el.append(self.friction.to_xml("friction"))
        return el

@dataclass
class Wired:
    """Wired point-to-point link physical layer definition. Contains exactly two port references (the two endpoints), plus optional VLAN, MACsec, and IP configuration."""
    port: List[Any] = field(default_factory=list)
    vlan: Optional[Any] = None
    macsec: Optional[Any] = None
    ip: Optional[Any] = None
    standard: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.port = [_c.text for _c in _children(el) if _ln(_c) == "port"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "vlan"), None)
        self.vlan = Vlan.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "macsec"), None)
        self.macsec = MacsecPolicy_.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "ip"), None)
        self.ip = _c.text if _c is not None else None
        self.standard = el.get("standard")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.standard is not None:
            el.set("standard", self.standard)
        for _i in self.port:
            _e = etree.SubElement(el, "port")
            if _i is not None: _e.text = _i
        if self.vlan is not None:
            el.append(self.vlan.to_xml("vlan"))
        if self.macsec is not None:
            el.append(self.macsec.to_xml("macsec"))
        if self.ip is not None:
            _e = etree.SubElement(el, "ip"); _e.text = self.ip
        return el

@dataclass
class Wireless:
    """Wireless link physical layer definition. Contains exactly two antenna references (the two endpoints) and an optional SSID for WiFi links."""
    antenna: List[Any] = field(default_factory=list)
    ssid: Optional[Any] = None
    standard: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        self.antenna = [_c.text for _c in _children(el) if _ln(_c) == "antenna"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "ssid"), None)
        self.ssid = _c.text if _c is not None else None
        self.standard = el.get("standard")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.standard is not None:
            el.set("standard", self.standard)
        for _i in self.antenna:
            _e = etree.SubElement(el, "antenna")
            if _i is not None: _e.text = _i
        if self.ssid is not None:
            _e = etree.SubElement(el, "ssid"); _e.text = self.ssid
        return el

@dataclass
class WirelessMesh:
    """Wireless mesh network where nodes self-organize routing topology. Unlike buses (shared medium, no routing) and chains (fixed forwarding order), mesh networks discover paths dynamically and can self-heal when nodes join or leave. Use for Thread/802.15.4 sensor meshes, BLE mesh sensor arrays, UWB positioning networks, and WiFi mesh (802.11s). The HCDF declares mesh membership and protocol — the routing protocol handles topology autonomously at runtime."""
    description: Optional[Any] = None
    node: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    standard: Optional[Any] = None
    protocol: Optional[Any] = None
    channel: Optional[Any] = None
    pan_id: Optional[Any] = None
    network_key_ref: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        self.node = [MeshNode.from_xml(_c) for _c in _children(el) if _ln(_c) == "node"]
        self.name = el.get("name")
        self.standard = el.get("standard")
        self.protocol = el.get("protocol")
        self.channel = el.get("channel")
        self.pan_id = el.get("pan-id")
        self.network_key_ref = el.get("network-key-ref")
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.standard is not None:
            el.set("standard", self.standard)
        if self.protocol is not None:
            el.set("protocol", self.protocol)
        if self.channel is not None:
            el.set("channel", self.channel)
        if self.pan_id is not None:
            el.set("pan-id", self.pan_id)
        if self.network_key_ref is not None:
            el.set("network-key-ref", self.network_key_ref)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        for _i in self.node:
            el.append(_i.to_xml("node"))
        return el

@dataclass
class Hcdf:
    """Root element of an HCDF cyber-physical robot description. Every HCDF file is a self-describing module: standalone robot descriptions and reusable sub-assemblies (arms, sensor heads, wheel modules) all use the same root element with the same metadata. When files are composed via include, each file retains its own name, author, and license, allowing tooling to trace provenance per sub-assembly and generate accurate bills of materials. Contains metadata, components (rigid bodies), joints (kinematic"""
    description: Optional[Any] = None
    author: Optional[Any] = None
    license: Optional[Any] = None
    url: Optional[Any] = None
    comp: List[Any] = field(default_factory=list)
    joint: List[Any] = field(default_factory=list)
    group: List[Any] = field(default_factory=list)
    state: List[Any] = field(default_factory=list)
    self_collision_disable: Optional[Any] = None
    network: List[Any] = field(default_factory=list)
    transmission: List[Any] = field(default_factory=list)
    color: List[Any] = field(default_factory=list)
    include: List[Any] = field(default_factory=list)
    extension: List[Any] = field(default_factory=list)
    name: Optional[Any] = None
    version: Optional[Any] = None
    body_frame: Optional[Any] = None
    world_frame: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "description"), None)
        self.description = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "author"), None)
        self.author = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "license"), None)
        self.license = _c.text if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "url"), None)
        self.url = _c.text if _c is not None else None
        self.comp = [Comp.from_xml(_c) for _c in _children(el) if _ln(_c) == "comp"]
        self.joint = [Joint.from_xml(_c) for _c in _children(el) if _ln(_c) == "joint"]
        self.group = [JointGroup.from_xml(_c) for _c in _children(el) if _ln(_c) == "group"]
        self.state = [KinematicState.from_xml(_c) for _c in _children(el) if _ln(_c) == "state"]
        _c = next((_c for _c in _children(el) if _ln(_c) == "self-collision-disable"), None)
        self.self_collision_disable = SelfCollisionDisable.from_xml(_c) if _c is not None else None
        self.network = [Network.from_xml(_c) for _c in _children(el) if _ln(_c) == "network"]
        self.transmission = [Transmission.from_xml(_c) for _c in _children(el) if _ln(_c) == "transmission"]
        self.color = [Color.from_xml(_c) for _c in _children(el) if _ln(_c) == "color"]
        self.include = [Include.from_xml(_c) for _c in _children(el) if _ln(_c) == "include"]
        self.extension = [Extension.from_xml(_c) for _c in _children(el) if _ln(_c) == "extension"]
        self.name = el.get("name")
        self.version = el.get("version")
        _v = el.get("body-frame")
        self.body_frame = BodyFrame(_v) if _v is not None else None
        _v = el.get("world-frame")
        self.world_frame = WorldFrame(_v) if _v is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.name is not None:
            el.set("name", self.name)
        if self.version is not None:
            el.set("version", self.version)
        if self.body_frame is not None:
            el.set("body-frame", self.body_frame.value)
        if self.world_frame is not None:
            el.set("world-frame", self.world_frame.value)
        if self.description is not None:
            _e = etree.SubElement(el, "description"); _e.text = self.description
        if self.author is not None:
            _e = etree.SubElement(el, "author"); _e.text = self.author
        if self.license is not None:
            _e = etree.SubElement(el, "license"); _e.text = self.license
        if self.url is not None:
            _e = etree.SubElement(el, "url"); _e.text = self.url
        for _i in self.comp:
            el.append(_i.to_xml("comp"))
        for _i in self.joint:
            el.append(_i.to_xml("joint"))
        for _i in self.group:
            el.append(_i.to_xml("group"))
        for _i in self.state:
            el.append(_i.to_xml("state"))
        if self.self_collision_disable is not None:
            el.append(self.self_collision_disable.to_xml("self-collision-disable"))
        for _i in self.network:
            el.append(_i.to_xml("network"))
        for _i in self.transmission:
            el.append(_i.to_xml("transmission"))
        for _i in self.color:
            el.append(_i.to_xml("color"))
        for _i in self.include:
            el.append(_i.to_xml("include"))
        for _i in self.extension:
            el.append(_i.to_xml("extension"))
        return el

@dataclass
class LidarParamsScanPattern:
    horizontal: Optional[Any] = None
    vertical: Optional[Any] = None

    @classmethod
    def from_xml(cls, el):
        self = cls()
        _c = next((_c for _c in _children(el) if _ln(_c) == "horizontal"), None)
        self.horizontal = LidarScanAxis.from_xml(_c) if _c is not None else None
        _c = next((_c for _c in _children(el) if _ln(_c) == "vertical"), None)
        self.vertical = LidarScanAxis.from_xml(_c) if _c is not None else None
        return self

    def to_xml(self, tag):
        el = etree.Element(tag)
        if self.horizontal is not None:
            el.append(self.horizontal.to_xml("horizontal"))
        if self.vertical is not None:
            el.append(self.vertical.to_xml("vertical"))
        return el



__all__ = [

    "AerofoilSurface",

    "Antenna",

    "AntennaCapabilities",

    "AntennaType",

    "AudioSensor",

    "AudioSensorType",

    "Axis",

    "AxisAlign",

    "AxisValue",

    "BatterySource",

    "BodyFrame",

    "Box",

    "Bus",

    "BusParticipant",

    "BusParticipantRole",

    "BusTopology",

    "BusType",

    "CameraCalibration",

    "CameraDistortion",

    "CameraIntrinsics",

    "CameraParams",

    "Capsule",

    "Chain",

    "ChainMode",

    "ChainProtocol",

    "ChainTopology",

    "ChemicalSensor",

    "ChemicalSensorType",

    "Collision",

    "CollisionGeometry",

    "CollisionPair",

    "Color",

    "Comp",

    "CompRole",

    "Cone",

    "ConfidentialityOffset",

    "Contact",

    "ControlModes",

    "ControlSurfaceDef",

    "CurrentCapability",

    "Cylinder",

    "DataOutput",

    "DcChain",

    "DcHop",

    "DcMode",

    "Discovered",

    "DynamicSurface",

    "EeeCapability",

    "EeePolicyValue",

    "Ellipsoid",

    "EmSensor",

    "EmSensorType",

    "EncoderPrinciple",

    "EncoderSensor",

    "EncoderSensorType",

    "EthercatIdentity",

    "Extension",

    "FifoConfig",

    "FlowCharacterization",

    "ForceSensor",

    "ForceSensorType",

    "Frame",

    "Friction",

    "Frustum",

    "FrustumShape",

    "FuelCell",

    "Gate",

    "Geometry",

    "GnssParams",

    "Gptp",

    "GptpClock",

    "GptpPortDefaults",

    "GripperSurface",

    "GroupJointRef",

    "GroupRef",

    "Hcdf",

    "HmiElement",

    "HmiType",

    "Hop",

    "HopRole",

    "HydrofoilSurface",

    "Include",

    "InertialProperties",

    "InertialSensor",

    "InertialSensorType",

    "Joint",

    "JointCalibration",

    "JointChild",

    "JointDynamics",

    "JointGroup",

    "JointLimit",

    "JointParent",

    "JointPosition",

    "JointType",

    "KeyAgreement",

    "KeyStore",

    "KeyStoreType",

    "KinematicState",

    "LidarParams",

    "LidarParamsScanPattern",

    "LidarScanAxis",

    "Link",

    "LldpCapability",

    "LoopClosure",

    "MacsecCapability",

    "MacsecCipher",

    "MacsecConfig",

    "MacsecPolicy",

    "MacsecPolicy_",

    "MeasuredValue",

    "Mesh",

    "MeshNode",

    "MeshRole",

    "Mimic",

    "ModelRef",

    "Motor",

    "MotorType",

    "NameOrigin",

    "Network",

    "Noise",

    "NoiseType",

    "OpticalSensor",

    "OpticalSensorType",

    "PhyConfig",

    "PhyRole",

    "PlcaCapability",

    "PlcaConfig",

    "PlcaCoordinator",

    "Port",

    "PortCapabilities",

    "PortType",

    "Pose",

    "PowerCapability",

    "PowerSource",

    "PreemptionClass",

    "PropSurface",

    "RadarParams",

    "RadiationSensor",

    "RadiationSensorType",

    "RangeValue",

    "RatedValue",

    "RfSensor",

    "RfSensorType",

    "SafetyController",

    "Schedule",

    "ScheduleAssign",

    "ScheduleAssignment",

    "SelfCollisionDisable",

    "Sensor",

    "SensorBase",

    "SensorDriver",

    "SensorFov",

    "SensorParams",

    "Software",

    "SolarSource",

    "Sphere",

    "Spur",

    "StreamProfileRef",

    "SupercapacitorSource",

    "Surface",

    "Switch",

    "SwitchMacsec",

    "SwitchTsn",

    "TactileSensor",

    "TactileSensorType",

    "TankSource",

    "TemperatureSensor",

    "TemperatureSensorType",

    "TrackSurface",

    "TrafficClass",

    "TrafficClasses",

    "Transmission",

    "TransmissionEndpoint",

    "TransmissionSpring",

    "TransmissionType",

    "TsnAtsCapability",

    "TsnCbsCapability",

    "TsnFrerCapability",

    "TsnGptpCapability",

    "TsnPortCapabilities",

    "TsnPreemptionCapability",

    "TsnPsfpCapability",

    "TsnQbvCapability",

    "Tunnel",

    "TunnelProtocol",

    "UrdfCompatComp",

    "UrdfCompatJoint",

    "Visual",

    "VisualGeometry",

    "Vlan",

    "VoltageCapability",

    "WheelSurface",

    "Wired",

    "Wireless",

    "WirelessMesh",

    "WorldFrame",

]
