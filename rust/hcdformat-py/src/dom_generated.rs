// @generated by hcdformat-domgen. DO NOT EDIT.
// Regenerate with: scripts/generate_dom_mirrors.sh (drift-checked in CI).
//
// One mirror per reachable model struct or choice; the `hcdformat-pyderive` attribute macros
// expand each into a write-through Python DOM handle. Each mirror's site list is every
// parent that references the type (the locator-enum variants); shared types (Pose, Color,
// geometry, …) therefore carry several sites. See hcdformat-pyderive/src/lib.rs.

use super::{err, resolve_index, stale, PyVisualAppearance, Resolve};
use hcdformat::model::connectivity::{
    Carrier, EeeMode, Fidelity, GptpClockKind, MacsecEnforcement, Purpose, TerminationMounting,
    TrafficPreemption,
};
use hcdformat::model::connectivity_xml::{
    ModelPartRepresentation, ModelRepresentation, NamedQuantity, Quantity, QuantityRange,
    SelectedProfile, SelectionQuantity, SelectionQuantityChoice, SelectionRange,
};
use hcdformat::model::enums::*;
use hcdformat::model::*;
use hcdformat_pyderive::{pychoice, pydom};
use pyo3::prelude::*;

pub(super) trait DomEnum: Sized {
    fn dom_value(&self) -> &'static str;
    fn from_dom_value(value: &str) -> Option<Self>;
}

impl DomEnum for AudioSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Microphone => "microphone",
            Self::Ultrasonic => "ultrasonic",
            Self::Sonar => "sonar",
            Self::Hydrophone => "hydrophone",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "microphone" => Some(Self::Microphone),
            "ultrasonic" => Some(Self::Ultrasonic),
            "sonar" => Some(Self::Sonar),
            "hydrophone" => Some(Self::Hydrophone),
            _ => None,
        }
    }
}

impl DomEnum for AxisValue {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::X => "X",
            Self::NegX => "-X",
            Self::Y => "Y",
            Self::NegY => "-Y",
            Self::Z => "Z",
            Self::NegZ => "-Z",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "X" => Some(Self::X),
            "-X" => Some(Self::NegX),
            "Y" => Some(Self::Y),
            "-Y" => Some(Self::NegY),
            "Z" => Some(Self::Z),
            "-Z" => Some(Self::NegZ),
            _ => None,
        }
    }
}

impl DomEnum for BodyFrame {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::FLU => "FLU",
            Self::FRD => "FRD",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "FLU" => Some(Self::FLU),
            "FRD" => Some(Self::FRD),
            _ => None,
        }
    }
}

impl DomEnum for CameraDistortionModel {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::PlumbBob => "plumb_bob",
            Self::RationalPolynomial => "rational_polynomial",
            Self::Equidistant => "equidistant",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "plumb_bob" => Some(Self::PlumbBob),
            "rational_polynomial" => Some(Self::RationalPolynomial),
            "equidistant" => Some(Self::Equidistant),
            _ => None,
        }
    }
}

impl DomEnum for CameraProjectionType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Pinhole => "pinhole",
            Self::Stereographic => "stereographic",
            Self::Equidistant => "equidistant",
            Self::Equisolid => "equisolid",
            Self::Orthographic => "orthographic",
            Self::Custom => "custom",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "pinhole" => Some(Self::Pinhole),
            "stereographic" => Some(Self::Stereographic),
            "equidistant" => Some(Self::Equidistant),
            "equisolid" => Some(Self::Equisolid),
            "orthographic" => Some(Self::Orthographic),
            "custom" => Some(Self::Custom),
            _ => None,
        }
    }
}

impl DomEnum for Carrier {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Electrical => "electrical",
            Self::GuidedOptical => "guided-optical",
            Self::ConductedRf => "conducted-rf",
            Self::RadiatedRf => "radiated-rf",
            Self::Liquid => "liquid",
            Self::Gas => "gas",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "electrical" => Some(Self::Electrical),
            "guided-optical" => Some(Self::GuidedOptical),
            "conducted-rf" => Some(Self::ConductedRf),
            "radiated-rf" => Some(Self::RadiatedRf),
            "liquid" => Some(Self::Liquid),
            "gas" => Some(Self::Gas),
            _ => None,
        }
    }
}

impl DomEnum for ChemicalSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Gas => "gas",
            Self::Ph => "ph",
            Self::Humidity => "humidity",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "gas" => Some(Self::Gas),
            "ph" => Some(Self::Ph),
            "humidity" => Some(Self::Humidity),
            _ => None,
        }
    }
}

impl DomEnum for CompRole {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Sensor => "sensor",
            Self::Compute => "compute",
            Self::Actuator => "actuator",
            Self::Parent => "parent",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "sensor" => Some(Self::Sensor),
            "compute" => Some(Self::Compute),
            "actuator" => Some(Self::Actuator),
            "parent" => Some(Self::Parent),
            _ => None,
        }
    }
}

impl DomEnum for EeeMode {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Enabled => "enabled",
            Self::Disabled => "disabled",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "enabled" => Some(Self::Enabled),
            "disabled" => Some(Self::Disabled),
            _ => None,
        }
    }
}

impl DomEnum for EmSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Mag => "mag",
            Self::MetalDetector => "metal_detector",
            Self::EddyCurrent => "eddy_current",
            Self::Emf => "emf",
            Self::HallEffect => "hall-effect",
            Self::Fluxgate => "fluxgate",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "mag" => Some(Self::Mag),
            "metal_detector" => Some(Self::MetalDetector),
            "eddy_current" => Some(Self::EddyCurrent),
            "emf" => Some(Self::Emf),
            "hall-effect" => Some(Self::HallEffect),
            "fluxgate" => Some(Self::Fluxgate),
            _ => None,
        }
    }
}

impl DomEnum for EncoderPrinciple {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Optical => "optical",
            Self::Magnetic => "magnetic",
            Self::Inductive => "inductive",
            Self::Capacitive => "capacitive",
            Self::Resistive => "resistive",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "optical" => Some(Self::Optical),
            "magnetic" => Some(Self::Magnetic),
            "inductive" => Some(Self::Inductive),
            "capacitive" => Some(Self::Capacitive),
            "resistive" => Some(Self::Resistive),
            _ => None,
        }
    }
}

impl DomEnum for EncoderSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Incremental => "incremental",
            Self::Absolute => "absolute",
            Self::Linear => "linear",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "incremental" => Some(Self::Incremental),
            "absolute" => Some(Self::Absolute),
            "linear" => Some(Self::Linear),
            _ => None,
        }
    }
}

impl DomEnum for Fidelity {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Functional => "functional",
            Self::Presented => "presented",
            Self::Exact => "exact",
            Self::Quantified => "quantified",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "functional" => Some(Self::Functional),
            "presented" => Some(Self::Presented),
            "exact" => Some(Self::Exact),
            "quantified" => Some(Self::Quantified),
            _ => None,
        }
    }
}

impl DomEnum for FluidSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Barometer => "barometer",
            Self::Airspeed => "airspeed",
            Self::Depth => "depth",
            Self::Flow => "flow",
            Self::Level => "level",
            Self::Differential => "differential",
            Self::Gauge => "gauge",
            Self::Absolute => "absolute",
            Self::Sealed => "sealed",
            Self::Vacuum => "vacuum",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "barometer" => Some(Self::Barometer),
            "airspeed" => Some(Self::Airspeed),
            "depth" => Some(Self::Depth),
            "flow" => Some(Self::Flow),
            "level" => Some(Self::Level),
            "differential" => Some(Self::Differential),
            "gauge" => Some(Self::Gauge),
            "absolute" => Some(Self::Absolute),
            "sealed" => Some(Self::Sealed),
            "vacuum" => Some(Self::Vacuum),
            _ => None,
        }
    }
}

impl DomEnum for ForceFrame {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Parent => "parent",
            Self::Child => "child",
            Self::Sensor => "sensor",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "parent" => Some(Self::Parent),
            "child" => Some(Self::Child),
            "sensor" => Some(Self::Sensor),
            _ => None,
        }
    }
}

impl DomEnum for ForceSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Strain => "strain",
            Self::Pressure => "pressure",
            Self::Torque => "torque",
            Self::LoadCell => "load_cell",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "strain" => Some(Self::Strain),
            "pressure" => Some(Self::Pressure),
            "torque" => Some(Self::Torque),
            "load_cell" => Some(Self::LoadCell),
            _ => None,
        }
    }
}

impl DomEnum for FrerSequenceEncoding {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::RTag => "r-tag",
            Self::Hsr => "hsr",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "r-tag" => Some(Self::RTag),
            "hsr" => Some(Self::Hsr),
            _ => None,
        }
    }
}

impl DomEnum for FrustumShape {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Conical => "conical",
            Self::Pyramidal => "pyramidal",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "conical" => Some(Self::Conical),
            "pyramidal" => Some(Self::Pyramidal),
            _ => None,
        }
    }
}

impl DomEnum for GptpClockKind {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Ordinary => "ordinary",
            Self::Boundary => "boundary",
            Self::Transparent => "transparent",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "ordinary" => Some(Self::Ordinary),
            "boundary" => Some(Self::Boundary),
            "transparent" => Some(Self::Transparent),
            _ => None,
        }
    }
}

impl DomEnum for Handedness {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Right => "right",
            Self::Left => "left",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "right" => Some(Self::Right),
            "left" => Some(Self::Left),
            _ => None,
        }
    }
}

impl DomEnum for HmiType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Display => "display",
            Self::Speaker => "speaker",
            Self::Buzzer => "buzzer",
            Self::LedStatus => "led-status",
            Self::LedIllumination => "led-illumination",
            Self::Button => "button",
            Self::Touchscreen => "touchscreen",
            Self::Indicator => "indicator",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "display" => Some(Self::Display),
            "speaker" => Some(Self::Speaker),
            "buzzer" => Some(Self::Buzzer),
            "led-status" => Some(Self::LedStatus),
            "led-illumination" => Some(Self::LedIllumination),
            "button" => Some(Self::Button),
            "touchscreen" => Some(Self::Touchscreen),
            "indicator" => Some(Self::Indicator),
            _ => None,
        }
    }
}

impl DomEnum for InertialSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Accel => "accel",
            Self::Gyro => "gyro",
            Self::AccelGyro => "accel_gyro",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "accel" => Some(Self::Accel),
            "gyro" => Some(Self::Gyro),
            "accel_gyro" => Some(Self::AccelGyro),
            _ => None,
        }
    }
}

impl DomEnum for JointType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Revolute => "revolute",
            Self::Continuous => "continuous",
            Self::Prismatic => "prismatic",
            Self::Fixed => "fixed",
            Self::Ball => "ball",
            Self::Universal => "universal",
            Self::Planar => "planar",
            Self::Screw => "screw",
            Self::Cylindrical => "cylindrical",
            Self::Free => "free",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "revolute" => Some(Self::Revolute),
            "continuous" => Some(Self::Continuous),
            "prismatic" => Some(Self::Prismatic),
            "fixed" => Some(Self::Fixed),
            "ball" => Some(Self::Ball),
            "universal" => Some(Self::Universal),
            "planar" => Some(Self::Planar),
            "screw" => Some(Self::Screw),
            "cylindrical" => Some(Self::Cylindrical),
            "free" => Some(Self::Free),
            _ => None,
        }
    }
}

impl DomEnum for MacsecEnforcement {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::MustSecure => "must-secure",
            Self::ShouldSecure => "should-secure",
            Self::IntegrityOnly => "integrity-only",
            Self::Disabled => "disabled",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "must-secure" => Some(Self::MustSecure),
            "should-secure" => Some(Self::ShouldSecure),
            "integrity-only" => Some(Self::IntegrityOnly),
            "disabled" => Some(Self::Disabled),
            _ => None,
        }
    }
}

impl DomEnum for MeasureDirection {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::ParentToChild => "parent_to_child",
            Self::ChildToParent => "child_to_parent",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "parent_to_child" => Some(Self::ParentToChild),
            "child_to_parent" => Some(Self::ChildToParent),
            _ => None,
        }
    }
}

impl DomEnum for MotorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Bldc => "bldc",
            Self::Brushed => "brushed",
            Self::Stepper => "stepper",
            Self::Servo => "servo",
            Self::Linear => "linear",
            Self::Solenoid => "solenoid",
            Self::Hydraulic => "hydraulic",
            Self::Pneumatic => "pneumatic",
            Self::Thrust => "thrust",
            Self::Ice => "ice",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "bldc" => Some(Self::Bldc),
            "brushed" => Some(Self::Brushed),
            "stepper" => Some(Self::Stepper),
            "servo" => Some(Self::Servo),
            "linear" => Some(Self::Linear),
            "solenoid" => Some(Self::Solenoid),
            "hydraulic" => Some(Self::Hydraulic),
            "pneumatic" => Some(Self::Pneumatic),
            "thrust" => Some(Self::Thrust),
            "ice" => Some(Self::Ice),
            _ => None,
        }
    }
}

impl DomEnum for NameOrigin {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Authored => "authored",
            Self::Synthesized => "synthesized",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "authored" => Some(Self::Authored),
            "synthesized" => Some(Self::Synthesized),
            _ => None,
        }
    }
}

impl DomEnum for NoiseType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Gaussian => "gaussian",
            Self::Uniform => "uniform",
            Self::None => "none",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "gaussian" => Some(Self::Gaussian),
            "uniform" => Some(Self::Uniform),
            "none" => Some(Self::None),
            _ => None,
        }
    }
}

impl DomEnum for OpticalSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Camera => "camera",
            Self::Thermal => "thermal",
            Self::Lidar => "lidar",
            Self::Tof => "tof",
            Self::OpticalFlow => "optical_flow",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "camera" => Some(Self::Camera),
            "thermal" => Some(Self::Thermal),
            "lidar" => Some(Self::Lidar),
            "tof" => Some(Self::Tof),
            "optical_flow" => Some(Self::OpticalFlow),
            _ => None,
        }
    }
}

impl DomEnum for PitchConvention {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::MPerRev => "m_per_rev",
            Self::RadPerM => "rad_per_m",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "m_per_rev" => Some(Self::MPerRev),
            "rad_per_m" => Some(Self::RadPerM),
            _ => None,
        }
    }
}

impl DomEnum for ProbeType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Pitot => "pitot",
            Self::PitotStatic => "pitot_static",
            Self::Multihole => "multihole",
            Self::Hotwire => "hotwire",
            Self::Venturi => "venturi",
            Self::Orifice => "orifice",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "pitot" => Some(Self::Pitot),
            "pitot_static" => Some(Self::PitotStatic),
            "multihole" => Some(Self::Multihole),
            "hotwire" => Some(Self::Hotwire),
            "venturi" => Some(Self::Venturi),
            "orifice" => Some(Self::Orifice),
            _ => None,
        }
    }
}

impl DomEnum for Purpose {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Communication => "communication",
            Self::PowerDelivery => "power-delivery",
            Self::MaterialTransfer => "material-transfer",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "communication" => Some(Self::Communication),
            "power-delivery" => Some(Self::PowerDelivery),
            "material-transfer" => Some(Self::MaterialTransfer),
            _ => None,
        }
    }
}

impl DomEnum for RadiationSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Geiger => "geiger",
            Self::Scintillation => "scintillation",
            Self::Neutron => "neutron",
            Self::Dosimeter => "dosimeter",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "geiger" => Some(Self::Geiger),
            "scintillation" => Some(Self::Scintillation),
            "neutron" => Some(Self::Neutron),
            "dosimeter" => Some(Self::Dosimeter),
            _ => None,
        }
    }
}

impl DomEnum for RfSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Gnss => "gnss",
            Self::Uwb => "uwb",
            Self::Radar => "radar",
            Self::RadioAltimeter => "radio_altimeter",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "gnss" => Some(Self::Gnss),
            "uwb" => Some(Self::Uwb),
            "radar" => Some(Self::Radar),
            "radio_altimeter" => Some(Self::RadioAltimeter),
            _ => None,
        }
    }
}

impl DomEnum for RoleType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Reference => "reference",
            Self::Driven => "driven",
            Self::Input => "input",
            Self::Output => "output",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "reference" => Some(Self::Reference),
            "driven" => Some(Self::Driven),
            "input" => Some(Self::Input),
            "output" => Some(Self::Output),
            _ => None,
        }
    }
}

impl DomEnum for StreamProfileSelectionRole {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Default => "default",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "default" => Some(Self::Default),
            _ => None,
        }
    }
}

impl DomEnum for TactileSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Capacitive => "capacitive",
            Self::Resistive => "resistive",
            Self::Piezoelectric => "piezoelectric",
            Self::Barometric => "barometric",
            Self::Optical => "optical",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "capacitive" => Some(Self::Capacitive),
            "resistive" => Some(Self::Resistive),
            "piezoelectric" => Some(Self::Piezoelectric),
            "barometric" => Some(Self::Barometric),
            "optical" => Some(Self::Optical),
            _ => None,
        }
    }
}

impl DomEnum for TemperatureSensorType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Thermistor => "thermistor",
            Self::Rtd => "rtd",
            Self::Thermocouple => "thermocouple",
            Self::Ir => "ir",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "thermistor" => Some(Self::Thermistor),
            "rtd" => Some(Self::Rtd),
            "thermocouple" => Some(Self::Thermocouple),
            "ir" => Some(Self::Ir),
            _ => None,
        }
    }
}

impl DomEnum for TerminationMounting {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Endpoint => "endpoint",
            Self::Inline => "inline",
            Self::Branch => "branch",
            Self::Closure => "closure",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "endpoint" => Some(Self::Endpoint),
            "inline" => Some(Self::Inline),
            "branch" => Some(Self::Branch),
            "closure" => Some(Self::Closure),
            _ => None,
        }
    }
}

impl DomEnum for TrafficPreemption {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Express => "express",
            Self::Preemptable => "preemptable",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "express" => Some(Self::Express),
            "preemptable" => Some(Self::Preemptable),
            _ => None,
        }
    }
}

impl DomEnum for TransmissionType {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::Simple => "simple",
            Self::Differential => "differential",
            Self::Belt => "belt",
            Self::Gear => "gear",
            Self::Planetary => "planetary",
            Self::Cycloidal => "cycloidal",
            Self::Harmonic => "harmonic",
            Self::StrainWave => "strain-wave",
            Self::Worm => "worm",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "simple" => Some(Self::Simple),
            "differential" => Some(Self::Differential),
            "belt" => Some(Self::Belt),
            "gear" => Some(Self::Gear),
            "planetary" => Some(Self::Planetary),
            "cycloidal" => Some(Self::Cycloidal),
            "harmonic" => Some(Self::Harmonic),
            "strain-wave" => Some(Self::StrainWave),
            "worm" => Some(Self::Worm),
            _ => None,
        }
    }
}

impl DomEnum for WorldFrame {
    fn dom_value(&self) -> &'static str {
        match self {
            Self::ENU => "ENU",
            Self::NED => "NED",
        }
    }
    fn from_dom_value(value: &str) -> Option<Self> {
        match value {
            "ENU" => Some(Self::ENU),
            "NED" => Some(Self::NED),
            _ => None,
        }
    }
}

#[pydom(py = PyAerofoilSurface, target = AerofoilSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDynamicSurface, ptype = DynamicSurface, field = aerofoil, card = opt),
)]
struct AerofoilSurfaceDom {
    #[pydom(nested = PyMeasuredValue)]
    span: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    chord: Option<()>,
    profile: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    sweep: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    dihedral: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    area: Option<()>,
}

#[pydom(py = PyAntenna, target = Antenna, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = antenna, card = vec),
)]
struct AntennaDom {
    name: String,
    #[pydom(nested = PyPortRef, clone_nested)]
    conducted_port: Option<()>,
    #[pydom(required_nested = PyPortRef)]
    radiated_port: (),
    #[pydom(nested = PyRepresentation, clone_nested)]
    representation: Option<()>,
}

#[pydom(py = PyAssembly, target = Assembly, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = cable, card = vec),
    site(parent = PyHcdf, ptype = Hcdf, field = harness, card = vec),
    site(parent = PyHcdf, ptype = Hcdf, field = plumbing, card = vec),
    site(parent = PyHcdf, ptype = Hcdf, field = umbilical, card = vec),
)]
struct AssemblyDom {
    name: String,
    #[pydom(list = PyConnector, name_field = req)]
    connector: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    wire: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    conductor: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    cable_member: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    fiber: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    coax: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    waveguide: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    feed: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    hose: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    pipe: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    passage: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    splice: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    tee: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    manifold: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    busbar: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    optical_splitter: Vec<()>,
    #[pydom(list = PyTermination, name_field = req, clone_list)]
    termination: Vec<()>,
    #[pydom(nested = PyRepresentation, clone_nested)]
    representation: Option<()>,
}

#[pydom(py = PyAssemblyModelRoot, target = AssemblyModelRoot, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyModelRootChoice, ptype = ModelRootChoice, variant = AssemblyModel),
)]
struct AssemblyModelRootDom {
    assembly: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyAssemblyRef, target = AssemblyRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyPhysicalOwnerChoice, ptype = PhysicalOwnerChoice, variant = Assembly),
)]
struct AssemblyRefDom {
    assembly: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyAxis, target = Axis, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = axis, card = opt),
    site(parent = PyJoint, ptype = Joint, field = axis2, card = opt),
)]
struct AxisDom {
    xyz: Option<String>,
}

#[pydom(py = PyAxisAlign, target = AxisAlign, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorDriver, ptype = SensorDriver, field = axis_align, card = opt),
)]
struct AxisAlignDom {
    #[pydom(mapped_enum)]
    x: Option<()>,
    #[pydom(mapped_enum)]
    y: Option<()>,
    #[pydom(mapped_enum)]
    z: Option<()>,
}

#[pydom(py = PyAxisNoise, target = AxisNoise, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyForceAxisNoise, ptype = ForceAxisNoise, field = force, card = opt),
    site(parent = PyForceAxisNoise, ptype = ForceAxisNoise, field = torque, card = opt),
    site(parent = PySensorParams, ptype = SensorParams, field = axis_noise, card = opt),
)]
struct AxisNoiseDom {
    #[pydom(nested = PyNoise)]
    x: Option<()>,
    #[pydom(nested = PyNoise)]
    y: Option<()>,
    #[pydom(nested = PyNoise)]
    z: Option<()>,
}

#[pydom(py = PyBatterySource, target = BatterySource, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPowerSource, ptype = PowerSource, field = battery, card = opt),
)]
struct BatterySourceDom {
    chemistry: Option<String>,
    cells_series: Option<String>,
    cells_parallel: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    nominal_voltage: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    capacity: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    energy: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_discharge: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    min_voltage: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_voltage: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_charge_rate: Option<()>,
    charge_protocol: Option<String>,
    cycle_life: Option<String>,
}

#[pydom(py = PyBinding, target = Binding, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = binding, card = vec),
)]
struct BindingDom {
    name: String,
    #[pydom(mapped_enum)]
    fidelity: (),
    #[pydom(required_nested = PyFunctionalEndpointRef)]
    functional: (),
    #[pydom(required_nested = PyPhysicalEndpointRef)]
    physical: (),
}

#[pydom(py = PyBox, target = Box_, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollisionGeometry, ptype = CollisionGeometry, field = box_, card = opt),
    site(parent = PyGeometry, ptype = Geometry, field = box_, card = opt),
)]
struct Box_Dom {
    size: Option<String>,
}

#[pydom(py = PyBus, target = Bus, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = bus, card = vec),
)]
struct BusDom {
    name: String,
    description: Option<String>,
    #[pydom(nested = PyNetworkSelection, clone_nested)]
    selected: Option<()>,
    #[pydom(nested = PyNetworkConfiguration)]
    configuration: Option<()>,
    #[pydom(list = PyParticipant, name_field = req, clone_list)]
    participant: Vec<()>,
}

#[pydom(py = PyCameraBinning, target = CameraBinning, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCameraMatrix, ptype = CameraMatrix, field = binning, card = opt),
)]
struct CameraBinningDom {
    x: Option<String>,
    y: Option<String>,
}

#[pydom(py = PyCameraCalibration, target = CameraCalibration, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorFov, ptype = SensorFov, field = calibration, card = opt),
)]
struct CameraCalibrationDom {
    fx: Option<String>,
    fy: Option<String>,
    cx: Option<String>,
    cy: Option<String>,
    k1: Option<String>,
    k2: Option<String>,
    k3: Option<String>,
    p1: Option<String>,
    p2: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    reprojection_error: Option<()>,
    date: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    temperature: Option<()>,
    method: Option<String>,
    images: Option<String>,
}

#[pydom(py = PyCameraDistortion, target = CameraDistortion, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorFov, ptype = SensorFov, field = distortion, card = opt),
)]
struct CameraDistortionDom {
    k1: Option<String>,
    k2: Option<String>,
    k3: Option<String>,
    p1: Option<String>,
    p2: Option<String>,
    k4: Option<String>,
    k5: Option<String>,
    k6: Option<String>,
    #[pydom(mapped_enum)]
    model: Option<()>,
}

#[pydom(py = PyCameraIntrinsics, target = CameraIntrinsics, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorFov, ptype = SensorFov, field = intrinsics, card = opt),
)]
struct CameraIntrinsicsDom {
    width: Option<String>,
    height: Option<String>,
    format: Option<String>,
    fx: Option<String>,
    fy: Option<String>,
    cx: Option<String>,
    cy: Option<String>,
    s: Option<String>,
}

#[pydom(py = PyCameraLens, target = CameraLens, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorFov, ptype = SensorFov, field = lens, card = opt),
)]
struct CameraLensDom {
    type_: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    cutoff_angle: Option<()>,
    scale_to_hfov: Option<String>,
    #[pydom(nested = PyCameraLensCustomFunction)]
    custom_function: Option<()>,
}

#[pydom(py = PyCameraLensCustomFunction, target = CameraLensCustomFunction, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCameraLens, ptype = CameraLens, field = custom_function, card = opt),
)]
struct CameraLensCustomFunctionDom {
    c1: Option<String>,
    c2: Option<String>,
    c3: Option<String>,
    f: Option<String>,
    fun: Option<String>,
}

#[pydom(py = PyCameraMatrix, target = CameraMatrix, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorFov, ptype = SensorFov, field = camera_matrix, card = opt),
)]
struct CameraMatrixDom {
    rectification: Option<String>,
    #[pydom(nested = PyCameraProjection)]
    projection: Option<()>,
    #[pydom(nested = PyCameraBinning)]
    binning: Option<()>,
    #[pydom(nested = PyCameraRoi)]
    roi: Option<()>,
}

#[pydom(py = PyCameraParams, target = CameraParams, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyOpticalSensor, ptype = OpticalSensor, field = camera_params, card = opt),
)]
struct CameraParamsDom {
    shutter: Option<String>,
    hdr: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    dynamic_range: Option<()>,
    #[pydom(nested = PyRangeValue)]
    depth_range: Option<()>,
    compression: Option<String>,
    trigger: Option<String>,
    ir_cut: Option<String>,
    spectral_band: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    netd: Option<()>,
    #[pydom(nested = PyRangeValue)]
    temperature_range: Option<()>,
}

#[pydom(py = PyCameraProjection, target = CameraProjection, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCameraMatrix, ptype = CameraMatrix, field = projection, card = opt),
)]
struct CameraProjectionDom {
    fx: Option<String>,
    fy: Option<String>,
    cx: Option<String>,
    cy: Option<String>,
    tx: Option<String>,
    ty: Option<String>,
}

#[pydom(py = PyCameraRoi, target = CameraRoi, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCameraMatrix, ptype = CameraMatrix, field = roi, card = opt),
)]
struct CameraRoiDom {
    x_offset: Option<String>,
    y_offset: Option<String>,
    width: Option<String>,
    height: Option<String>,
    rectify: Option<String>,
}

#[pydom(py = PyCapabilities, target = Capabilities, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyChannel, ptype = Channel, field = capabilities, card = opt),
    site(parent = PyPort, ptype = Port, field = capabilities, card = opt),
)]
struct CapabilitiesDom {
    #[pydom(list = PyPurposeCapability, name_field = none, clone_list)]
    purpose: Vec<()>,
    #[pydom(list = PyCarrierCapability, name_field = none, clone_list)]
    carrier: Vec<()>,
    #[pydom(list = PyProfileCapability, name_field = none, clone_list)]
    profile: Vec<()>,
    #[pydom(nested = PyQuantityRange)]
    rate: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    voltage: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    current: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    power: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    impedance: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    frequency: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    bandwidth: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    pressure: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    flow: Option<()>,
    #[pydom(nested = PyQuantityRange)]
    temperature: Option<()>,
}

#[pydom(py = PyCapsule, target = Capsule, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollisionGeometry, ptype = CollisionGeometry, field = capsule, card = opt),
    site(parent = PyGeometry, ptype = Geometry, field = capsule, card = opt),
)]
struct CapsuleDom {
    radius: Option<String>,
    length: Option<String>,
}

#[pydom(py = PyCarrierCapability, target = CarrierCapability, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCapabilities, ptype = Capabilities, field = carrier, card = vec),
)]
struct CarrierCapabilityDom {
    #[pydom(mapped_enum)]
    value: (),
}

#[pydom(py = PyChain, target = Chain, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = chain, card = vec),
)]
struct ChainDom {
    name: String,
    description: Option<String>,
    #[pydom(nested = PyNetworkSelection, clone_nested)]
    selected: Option<()>,
    #[pydom(nested = PyNetworkConfiguration)]
    configuration: Option<()>,
    #[pydom(list = PyParticipant, name_field = req, clone_list)]
    participant: Vec<()>,
    #[pydom(list = PyHop, name_field = req, clone_list)]
    hop: Vec<()>,
    #[pydom(list = PyLeg, name_field = req, clone_list)]
    leg: Vec<()>,
}

#[pydom(py = PyChannel, target = Channel, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPort, ptype = Port, field = channel, card = vec),
)]
struct ChannelDom {
    name: String,
    role: Option<String>,
    local_group: Option<String>,
    #[pydom(nested = PyCapabilities)]
    capabilities: Option<()>,
}

#[pydom(py = PyChannelRef, target = ChannelRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyFunctionalEndpointChoice, ptype = FunctionalEndpointChoice, variant = Channel),
)]
struct ChannelRefDom {
    component: String,
    port: String,
    channel: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyCollision, target = Collision, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = collision, card = vec),
)]
struct CollisionDom {
    name: Option<String>,
    verbose: Option<String>,
    #[pydom(mapped_enum)]
    name_origin: Option<()>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PyCollisionGeometry)]
    geometry: Option<()>,
    #[pydom(nested = PySurface)]
    surface: Option<()>,
}

#[pydom(py = PyCollisionGeometry, target = CollisionGeometry, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollision, ptype = Collision, field = geometry, card = opt),
)]
struct CollisionGeometryDom {
    #[pydom(nested = PyBox)]
    box_: Option<()>,
    #[pydom(nested = PyCylinder)]
    cylinder: Option<()>,
    #[pydom(nested = PySphere)]
    sphere: Option<()>,
    #[pydom(nested = PyCapsule)]
    capsule: Option<()>,
    #[pydom(nested = PyCone)]
    cone: Option<()>,
    #[pydom(nested = PyEllipsoid)]
    ellipsoid: Option<()>,
    #[pydom(nested = PyMesh)]
    mesh: Option<()>,
}

#[pydom(py = PyCollisionPair, target = CollisionPair, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySelfCollisionDisable, ptype = SelfCollisionDisable, field = pair, card = vec),
)]
struct CollisionPairDom {
    comp1: Option<String>,
    comp2: Option<String>,
    reason: Option<String>,
}

#[pydom(py = PyColor, target = Color, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = color, card = vec),
)]
struct ColorDom {
    name: Option<String>,
    rgba: Option<String>,
    description: Option<String>,
}

#[pydom(py = PyComp, target = Comp, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = comp, card = vec),
)]
struct CompDom {
    name: String,
    #[pydom(mapped_enum)]
    role: Option<()>,
    hwid: Option<String>,
    struct_type: Option<String>,
    ip_rating: Option<String>,
    description: Option<String>,
    board: Option<String>,
    #[pydom(nested = PyRangeValue)]
    operating_temp: Option<()>,
    #[pydom(nested = PyInertial)]
    inertial: Option<()>,
    #[pydom(list = PyVisual, name_field = req)]
    visual: Vec<()>,
    #[pydom(list = PyCollision, name_field = opt)]
    collision: Vec<()>,
    #[pydom(list = PyFrame, name_field = req)]
    frame: Vec<()>,
    #[pydom(list = PyPort, name_field = req)]
    port: Vec<()>,
    #[pydom(list = PyConnector, name_field = req)]
    connector: Vec<()>,
    #[pydom(list = PyAntenna, name_field = req, clone_list)]
    antenna: Vec<()>,
    #[pydom(list = PyFunction, name_field = req)]
    switch: Vec<()>,
    #[pydom(list = PyFunction, name_field = req)]
    bridge: Vec<()>,
    #[pydom(list = PyFunction, name_field = req)]
    converter: Vec<()>,
    #[pydom(list = PyFunction, name_field = req)]
    transceiver: Vec<()>,
    #[pydom(list = PyFunction, name_field = req)]
    radio: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    wire: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    conductor: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    cable_member: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    fiber: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    coax: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    waveguide: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    feed: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    hose: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    pipe: Vec<()>,
    #[pydom(list = PyPath, name_field = req, clone_list)]
    passage: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    splice: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    tee: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    manifold: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    busbar: Vec<()>,
    #[pydom(list = PyJunction, name_field = req)]
    optical_splitter: Vec<()>,
    #[pydom(list = PyTermination, name_field = req, clone_list)]
    termination: Vec<()>,
    #[pydom(list = PySensor, name_field = opt)]
    sensor: Vec<()>,
    #[pydom(list = PyMotor, name_field = opt)]
    motor: Vec<()>,
    #[pydom(list = PyHmiElement, name_field = opt)]
    hmi: Vec<()>,
    #[pydom(list = PyDynamicSurface, name_field = opt)]
    dynamic_surface: Vec<()>,
    #[pydom(list = PyPowerSource, name_field = opt)]
    power_source: Vec<()>,
    #[pydom(nested = PySoftware)]
    software: Option<()>,
    #[pydom(nested = PyDiscovered)]
    discovered: Option<()>,
    #[pydom(nested = PyUrdfCompatComp)]
    urdf_compat: Option<()>,
    #[pydom(list = PyExtension, name_field = none)]
    extension: Vec<()>,
    #[pydom(skip)]
    comments: (),
}

#[pydom(py = PyComponentNamedFrame, target = ComponentNamedFrame, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRouteFrameChoice, ptype = RouteFrameChoice, variant = ComponentFrame),
)]
struct ComponentNamedFrameDom {
    component: String,
    frame: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyComponentOriginFrame, target = ComponentOriginFrame, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRouteFrameChoice, ptype = RouteFrameChoice, variant = ComponentOrigin),
)]
struct ComponentOriginFrameDom {
    component: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyComponentRef, target = ComponentRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyHopOwnerChoice, ptype = HopOwnerChoice, variant = Component),
    choice_site(parent = PyPhysicalOwnerChoice, ptype = PhysicalOwnerChoice, variant = Component),
)]
struct ComponentRefDom {
    component: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyComponentVisualRoot, target = ComponentVisualRoot, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyModelRootChoice, ptype = ModelRootChoice, variant = ComponentVisual),
)]
struct ComponentVisualRootDom {
    component: String,
    visual: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyCone, target = Cone, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollisionGeometry, ptype = CollisionGeometry, field = cone, card = opt),
    site(parent = PyGeometry, ptype = Geometry, field = cone, card = opt),
)]
struct ConeDom {
    radius: Option<String>,
    length: Option<String>,
}

#[pydom(py = PyConnectivityFunctionRef, target = ConnectivityFunctionRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyHopOwnerChoice, ptype = HopOwnerChoice, variant = Function),
)]
struct ConnectivityFunctionRefDom {
    component: String,
    function: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyConnectivityMesh, target = ConnectivityMesh, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = mesh, card = vec),
)]
struct ConnectivityMeshDom {
    name: String,
    description: Option<String>,
    #[pydom(nested = PyNetworkSelection, clone_nested)]
    selected: Option<()>,
    #[pydom(nested = PyNetworkConfiguration)]
    configuration: Option<()>,
    #[pydom(list = PyParticipant, name_field = req, clone_list)]
    participant: Vec<()>,
}

#[pydom(py = PyConnector, target = Connector, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAssembly, ptype = Assembly, field = connector, card = vec),
    site(parent = PyComp, ptype = Comp, field = connector, card = vec),
)]
struct ConnectorDom {
    name: String,
    family: Option<String>,
    #[pydom(list = PyPosition, name_field = req)]
    pin: Vec<()>,
    #[pydom(list = PyPosition, name_field = req)]
    socket: Vec<()>,
    #[pydom(list = PyPosition, name_field = req)]
    contact: Vec<()>,
    #[pydom(list = PyPosition, name_field = req)]
    fiber: Vec<()>,
    #[pydom(list = PyPosition, name_field = req)]
    passage: Vec<()>,
    #[pydom(list = PyPosition, name_field = req)]
    feed: Vec<()>,
    #[pydom(list = PyPosition, name_field = req)]
    waveguide_opening: Vec<()>,
    #[pydom(nested = PyRepresentation, clone_nested)]
    representation: Option<()>,
}

#[pydom(py = PyConnectorRef, target = ConnectorRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMate, ptype = Mate, field = first, card = req),
    site(parent = PyMate, ptype = Mate, field = second, card = req),
    choice_site(parent = PyPhysicalEndpointChoice, ptype = PhysicalEndpointChoice, variant = Connector),
)]
struct ConnectorRefDom {
    connector: String,
    #[pydom(choice = PyPhysicalOwnerChoice)]
    owner: (),
}

#[pydom(py = PyContact, target = Contact, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySurface, ptype = Surface, field = contact, card = opt),
)]
struct ContactDom {
    stiffness: Option<String>,
    damping: Option<String>,
}

#[pydom(py = PyControlModes, target = ControlModes, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMotor, ptype = Motor, field = control_modes, card = opt),
)]
struct ControlModesDom {
    #[pydom(strlist)]
    mode: Vec<String>,
}

#[pydom(py = PyControlSurface, target = ControlSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDynamicSurface, ptype = DynamicSurface, field = control_surface, card = opt),
)]
struct ControlSurfaceDom {
    type_: Option<String>,
    chord_ratio: Option<String>,
    span_fraction: Option<String>,
    #[pydom(nested = PyRangeValue)]
    deflection: Option<()>,
}

#[pydom(py = PyCylinder, target = Cylinder, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollisionGeometry, ptype = CollisionGeometry, field = cylinder, card = opt),
    site(parent = PyGeometry, ptype = Geometry, field = cylinder, card = opt),
)]
struct CylinderDom {
    radius: Option<String>,
    length: Option<String>,
}

#[pydom(py = PyDataOutput, target = DataOutput, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = data_output, card = opt),
    site(parent = PyOpticalSensor, ptype = OpticalSensor, field = data_output, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = data_output, card = opt),
)]
struct DataOutputDom {
    #[pydom(nested = PyMeasuredValue)]
    bandwidth: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    bandwidth_compressed: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    frame_size: Option<()>,
}

#[pydom(py = PyDerivedRouteRepresentation, target = DerivedRouteRepresentation, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRepresentationChoice, ptype = RepresentationChoice, variant = DerivedRoute),
)]
struct DerivedRouteRepresentationDom {
    #[pydom(choice = PyRouteSectionChoice)]
    section: Option<()>,
    #[pydom(list = PyRoutePoint, name_field = none, clone_list)]
    waypoint: Vec<()>,
}

#[pydom(py = PyDiscovered, target = Discovered, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = discovered, card = opt),
)]
struct DiscoveredDom {
    ip: Option<String>,
    port: Option<String>,
    last_seen: Option<String>,
}

#[pydom(py = PyDynamicSurface, target = DynamicSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = dynamic_surface, card = vec),
)]
struct DynamicSurfaceDom {
    name: Option<String>,
    visual: Option<String>,
    mesh: Option<String>,
    #[pydom(nested = PyPropSurface)]
    prop: Option<()>,
    #[pydom(nested = PyAerofoilSurface)]
    aerofoil: Option<()>,
    #[pydom(nested = PyHydrofoilSurface)]
    hydrofoil: Option<()>,
    #[pydom(nested = PyControlSurface)]
    control_surface: Option<()>,
    #[pydom(nested = PyWheelSurface)]
    wheel: Option<()>,
    #[pydom(nested = PyTrackSurface)]
    track: Option<()>,
    #[pydom(nested = PyGripperSurface)]
    gripper: Option<()>,
}

#[pydom(py = PyEeeConfiguration, target = EeeConfiguration, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkConfiguration, ptype = NetworkConfiguration, field = eee, card = opt),
)]
struct EeeConfigurationDom {
    #[pydom(mapped_enum)]
    default_mode: (),
    #[pydom(list = PyEeeOverride, name_field = none, clone_list)]
    override_: Vec<()>,
}

#[pydom(py = PyEeeOverride, target = EeeOverride, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyEeeConfiguration, ptype = EeeConfiguration, field = override_, card = vec),
)]
struct EeeOverrideDom {
    #[pydom(mapped_enum)]
    mode: (),
    #[pydom(required_nested = PyParticipantRef)]
    participant: (),
}

#[pydom(py = PyEllipsoid, target = Ellipsoid, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollisionGeometry, ptype = CollisionGeometry, field = ellipsoid, card = opt),
    site(parent = PyGeometry, ptype = Geometry, field = ellipsoid, card = opt),
)]
struct EllipsoidDom {
    radii: Option<String>,
}

#[pydom(py = PyEndpoint, target = Endpoint, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyTransmission, ptype = Transmission, field = joint, card = vec),
    site(parent = PyTransmission, ptype = Transmission, field = motor, card = vec),
)]
struct EndpointDom {
    ref_: Option<String>,
    role: Option<String>,
}

#[pydom(py = PyExtension, target = Extension, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = extension, card = vec),
    site(parent = PyHcdf, ptype = Hcdf, field = extension, card = vec),
    site(parent = PyJoint, ptype = Joint, field = extension, card = opt),
)]
struct ExtensionDom {
    domain: String,
    version: Option<String>,
    body: String,
}

#[pydom(py = PyFifoConfig, target = FifoConfig, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorParams, ptype = SensorParams, field = fifo, card = opt),
)]
struct FifoConfigDom {
    depth: Option<String>,
    watermark: Option<String>,
}

#[pydom(py = PyFlowCharacterization, target = FlowCharacterization, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyTankSource, ptype = TankSource, field = flow, card = opt),
)]
struct FlowCharacterizationDom {
    method: Option<String>,
    #[pydom(nested = PyRangeValue)]
    outlet_pressure: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_flow_rate: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    peak_flow_rate: Option<()>,
    regulator: Option<String>,
}

#[pydom(py = PyFluidProbe, target = FluidProbe, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = probe, card = opt),
)]
struct FluidProbeDom {
    type_: Option<String>,
    static_port: Option<String>,
    total_port: Option<String>,
    ports: Option<String>,
}

#[pydom(py = PyFluidSensor, target = FluidSensor, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensor, ptype = Sensor, field = fluid, card = vec),
)]
struct FluidSensorDom {
    type_: Option<String>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PySensorDriver)]
    driver: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    resolution: Option<()>,
    #[pydom(nested = PyNoise)]
    noise: Option<()>,
    #[pydom(nested = PyDataOutput)]
    data_output: Option<()>,
    #[pydom(nested = PyGeometry)]
    geometry: Option<()>,
    medium: Option<String>,
    #[pydom(nested = PyRangeValue)]
    pressure_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    accuracy: Option<()>,
    #[pydom(nested = PyRangeValue)]
    temperature_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    temperature_coefficient: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    reference_pressure: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    reference_density: Option<()>,
    #[pydom(nested = PyRangeValue)]
    altitude_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    altitude_resolution: Option<()>,
    #[pydom(nested = PyRangeValue)]
    airspeed_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    airspeed_resolution: Option<()>,
    #[pydom(strlist)]
    airspeed_output: Vec<String>,
    #[pydom(nested = PyRangeValue)]
    depth_range: Option<()>,
    #[pydom(nested = PyRangeValue)]
    level_range: Option<()>,
    #[pydom(nested = PyRangeValue)]
    flow_range: Option<()>,
    #[pydom(nested = PyFluidProbe)]
    probe: Option<()>,
}

#[pydom(py = PyForceAxisNoise, target = ForceAxisNoise, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorCategory, ptype = SensorCategory, field = axis_noise, card = opt),
)]
struct ForceAxisNoiseDom {
    #[pydom(nested = PyAxisNoise)]
    force: Option<()>,
    #[pydom(nested = PyAxisNoise)]
    torque: Option<()>,
}

#[pydom(py = PyFrame, target = Frame, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = frame, card = vec),
)]
struct FrameDom {
    name: String,
    relative_to: Option<String>,
    type_: Option<String>,
    description: Option<String>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
}

#[pydom(py = PyFriction, target = Friction, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyGripperSurface, ptype = GripperSurface, field = friction, card = opt),
    site(parent = PySurface, ptype = Surface, field = friction, card = opt),
    site(parent = PyTrackSurface, ptype = TrackSurface, field = friction, card = opt),
    site(parent = PyWheelSurface, ptype = WheelSurface, field = friction, card = opt),
)]
struct FrictionDom {
    static_: Option<String>,
    dynamic: Option<String>,
    #[pydom(nested = PyFrictionDirection)]
    friction_direction: Option<()>,
}

#[pydom(py = PyFrictionDirection, target = FrictionDirection, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyFriction, ptype = Friction, field = friction_direction, card = opt),
)]
struct FrictionDirectionDom {
    fdir1: Option<String>,
    mu2: Option<String>,
    slip1: Option<String>,
    slip2: Option<String>,
}

#[pydom(py = PyFrustum, target = Frustum, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyGeometry, ptype = Geometry, field = frustum, card = opt),
)]
struct FrustumDom {
    #[pydom(mapped_enum)]
    shape: Option<()>,
    near: Option<String>,
    far: Option<String>,
    fov: Option<String>,
    hfov: Option<String>,
    vfov: Option<String>,
}

#[pydom(py = PyFuelCell, target = FuelCell, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPowerSource, ptype = PowerSource, field = fuel_cell, card = opt),
)]
struct FuelCellDom {
    type_: Option<String>,
    fuel: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    rated_power: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    peak_power: Option<()>,
    efficiency: Option<String>,
    #[pydom(nested = PyRangeValue)]
    output_voltage: Option<()>,
}

#[pydom(py = PyFunction, target = Function, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = bridge, card = vec),
    site(parent = PyComp, ptype = Comp, field = converter, card = vec),
    site(parent = PyComp, ptype = Comp, field = radio, card = vec),
    site(parent = PyComp, ptype = Comp, field = switch, card = vec),
    site(parent = PyComp, ptype = Comp, field = transceiver, card = vec),
)]
struct FunctionDom {
    name: String,
    #[pydom(list = PyFunctionalEndpointRef, name_field = none, clone_list)]
    input: Vec<()>,
    #[pydom(list = PyFunctionalEndpointRef, name_field = none, clone_list)]
    output: Vec<()>,
    #[pydom(list = PyFunctionalEndpointRef, name_field = none, clone_list)]
    bidirectional: Vec<()>,
}

#[pydom(py = PyFunctionalEndpointRef, target = FunctionalEndpointRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyBinding, ptype = Binding, field = functional, card = req),
    site(parent = PyFunction, ptype = Function, field = bidirectional, card = vec),
    site(parent = PyFunction, ptype = Function, field = input, card = vec),
    site(parent = PyFunction, ptype = Function, field = output, card = vec),
    site(parent = PyParticipant, ptype = Participant, field = endpoint, card = req),
)]
struct FunctionalEndpointRefDom {
    #[pydom(choice = PyFunctionalEndpointChoice)]
    endpoint: (),
}

#[pydom(py = PyGateControlEntry, target = GateControlEntry, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyGateSchedule, ptype = GateSchedule, field = gate, card = vec),
)]
struct GateControlEntryDom {
    #[pydom(required_scalar)]
    duration_ns: u64,
    #[pydom(required_nested = PyOpenTrafficClasses)]
    open: (),
}

#[pydom(py = PyGateSchedule, target = GateSchedule, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkConfiguration, ptype = NetworkConfiguration, field = gate_schedule, card = vec),
)]
struct GateScheduleDom {
    name: String,
    #[pydom(required_scalar)]
    cycle_time_ns: u64,
    #[pydom(list = PyGateControlEntry, name_field = none, clone_list)]
    gate: Vec<()>,
}

#[pydom(py = PyGeometry, target = Geometry, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = geometry, card = opt),
    site(parent = PyHmiElement, ptype = HmiElement, field = geometry, card = opt),
    site(parent = PyInertialSensor, ptype = InertialSensor, field = geometry, card = opt),
    site(parent = PyOpticalSensor, ptype = OpticalSensor, field = geometry, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = geometry, card = opt),
    site(parent = PySensorFov, ptype = SensorFov, field = geometry, card = opt),
)]
struct GeometryDom {
    #[pydom(nested = PyBox)]
    box_: Option<()>,
    #[pydom(nested = PyCylinder)]
    cylinder: Option<()>,
    #[pydom(nested = PySphere)]
    sphere: Option<()>,
    #[pydom(nested = PyCapsule)]
    capsule: Option<()>,
    #[pydom(nested = PyCone)]
    cone: Option<()>,
    #[pydom(nested = PyEllipsoid)]
    ellipsoid: Option<()>,
    #[pydom(nested = PyMesh)]
    mesh: Option<()>,
    #[pydom(nested = PyFrustum)]
    frustum: Option<()>,
}

#[pydom(py = PyGnssParams, target = GnssParams, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorCategory, ptype = SensorCategory, field = gnss_params, card = opt),
)]
struct GnssParamsDom {
    #[pydom(strlist)]
    constellation: Vec<String>,
    #[pydom(strlist)]
    frequency: Vec<String>,
    rtk: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    accuracy_standalone: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    accuracy_rtk: Option<()>,
    pps: Option<String>,
    dead_reckoning: Option<String>,
    anti_spoofing: Option<String>,
}

#[pydom(py = PyGptpClock, target = GptpClock, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyGptpDomain, ptype = GptpDomain, field = clock, card = vec),
)]
struct GptpClockDom {
    name: String,
    #[pydom(mapped_enum)]
    kind: (),
    #[pydom(required_scalar)]
    gm_capable: bool,
    priority1: Option<u8>,
    priority2: Option<u8>,
    clock_class: Option<u8>,
    clock_accuracy: Option<u8>,
    #[pydom(required_nested = PyParticipantRef)]
    participant: (),
}

#[pydom(py = PyGptpDomain, target = GptpDomain, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkConfiguration, ptype = NetworkConfiguration, field = gptp_domain, card = vec),
)]
struct GptpDomainDom {
    name: String,
    #[pydom(required_scalar)]
    number: u8,
    #[pydom(list = PyGptpClock, name_field = req, clone_list)]
    clock: Vec<()>,
    #[pydom(nested = PyGptpPortDefaults)]
    port_defaults: Option<()>,
}

#[pydom(py = PyGptpPortDefaults, target = GptpPortDefaults, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyGptpDomain, ptype = GptpDomain, field = port_defaults, card = opt),
)]
struct GptpPortDefaultsDom {
    log_sync_interval: Option<i8>,
    log_announce_interval: Option<i8>,
    log_pdelay_req_interval: Option<i8>,
    announce_receipt_timeout: Option<u8>,
    neighbor_prop_delay_threshold_ns: Option<u64>,
}

#[pydom(py = PyGripperSurface, target = GripperSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDynamicSurface, ptype = DynamicSurface, field = gripper, card = opt),
)]
struct GripperSurfaceDom {
    type_: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    grip_force: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    contact_area: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    payload: Option<()>,
    shore_hardness: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    vacuum_level: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    magnetic_force: Option<()>,
    #[pydom(nested = PyFriction)]
    friction: Option<()>,
}

#[pydom(py = PyHcdf, target = Hcdf, owner(target = Hcdf, py = PyHcdf), root)]
struct HcdfDom {
    name: String,
    version: String,
    #[pydom(mapped_enum)]
    body_frame: Option<()>,
    #[pydom(mapped_enum)]
    world_frame: Option<()>,
    description: Option<String>,
    author: Option<String>,
    license: Option<String>,
    url: Option<String>,
    #[pydom(list = PyComp, name_field = req)]
    comp: Vec<()>,
    #[pydom(list = PyJoint, name_field = opt)]
    joint: Vec<()>,
    #[pydom(list = PyJointGroup, name_field = opt)]
    group: Vec<()>,
    #[pydom(list = PyKinematicState, name_field = opt)]
    state: Vec<()>,
    #[pydom(nested = PySelfCollisionDisable)]
    self_collision_disable: Option<()>,
    #[pydom(list = PyAssembly, name_field = req)]
    harness: Vec<()>,
    #[pydom(list = PyAssembly, name_field = req)]
    cable: Vec<()>,
    #[pydom(list = PyAssembly, name_field = req)]
    plumbing: Vec<()>,
    #[pydom(list = PyAssembly, name_field = req)]
    umbilical: Vec<()>,
    #[pydom(list = PyBinding, name_field = req, clone_list)]
    binding: Vec<()>,
    #[pydom(list = PyMate, name_field = req, clone_list)]
    mate: Vec<()>,
    #[pydom(list = PyLink, name_field = req, clone_list)]
    link: Vec<()>,
    #[pydom(list = PyBus, name_field = req, clone_list)]
    bus: Vec<()>,
    #[pydom(list = PyChain, name_field = req, clone_list)]
    chain: Vec<()>,
    #[pydom(list = PyStar, name_field = req, clone_list)]
    star: Vec<()>,
    #[pydom(list = PyRing, name_field = req, clone_list)]
    ring: Vec<()>,
    #[pydom(list = PyConnectivityMesh, name_field = req, clone_list)]
    mesh: Vec<()>,
    #[pydom(list = PyTree, name_field = req, clone_list)]
    tree: Vec<()>,
    #[pydom(list = PyStreamProfileResource, name_field = none, clone_list)]
    stream_profile: Vec<()>,
    #[pydom(list = PyTransmission, name_field = opt)]
    transmission: Vec<()>,
    #[pydom(list = PyColor, name_field = opt)]
    color: Vec<()>,
    #[pydom(list = PyInclude, name_field = opt)]
    include: Vec<()>,
    #[pydom(list = PyExtension, name_field = none)]
    extension: Vec<()>,
    #[pydom(skip)]
    comments: (),
}

#[pydom(py = PyHmiElement, target = HmiElement, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = hmi, card = vec),
)]
struct HmiElementDom {
    name: Option<String>,
    #[pydom(mapped_enum)]
    type_: Option<()>,
    visual: Option<String>,
    mesh: Option<String>,
    description: Option<String>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PyGeometry)]
    geometry: Option<()>,
    #[pydom(nested = PyLedIllumination)]
    illumination: Option<()>,
}

#[pydom(py = PyHop, target = Hop, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyChain, ptype = Chain, field = hop, card = vec),
    site(parent = PyRing, ptype = Ring, field = hop, card = vec),
    site(parent = PyTree, ptype = Tree, field = hop, card = vec),
)]
struct HopDom {
    name: String,
    role: Option<String>,
    processing_delay_ns: Option<u64>,
    description: Option<String>,
    #[pydom(required_nested = PyHopOwnerRef)]
    owner: (),
}

#[pydom(py = PyHopOwnerRef, target = HopOwnerRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHop, ptype = Hop, field = owner, card = req),
)]
struct HopOwnerRefDom {
    #[pydom(choice = PyHopOwnerChoice)]
    owner: (),
}

#[pydom(py = PyHopRef, target = HopRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHopReference, ptype = HopReference, field = hop, card = req),
    site(parent = PyLegEnd, ptype = LegEnd, field = hop, card = req),
)]
struct HopRefDom {
    network: String,
    hop: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyHopReference, target = HopReference, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyTree, ptype = Tree, field = root, card = req),
)]
struct HopReferenceDom {
    #[pydom(required_nested = PyHopRef)]
    hop: (),
}

#[pydom(py = PyHydrofoilSurface, target = HydrofoilSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDynamicSurface, ptype = DynamicSurface, field = hydrofoil, card = opt),
)]
struct HydrofoilSurfaceDom {
    #[pydom(nested = PyMeasuredValue)]
    span: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    chord: Option<()>,
    profile: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    area: Option<()>,
}

#[pydom(py = PyInclude, target = Include, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = include, card = vec),
)]
struct IncludeDom {
    uri: Option<String>,
    sha: Option<String>,
    name: Option<String>,
    pose: Option<String>,
    static_: Option<String>,
    placement_frame: Option<String>,
}

#[pydom(py = PyInertial, target = Inertial, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = inertial, card = opt),
)]
struct InertialDom {
    mass: Option<String>,
    #[pydom(nested = PyPose)]
    inertia_origin: Option<()>,
    inertia: Option<String>,
}

#[pydom(py = PyInertialSensor, target = InertialSensor, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensor, ptype = Sensor, field = inertial, card = vec),
)]
struct InertialSensorDom {
    #[pydom(mapped_enum)]
    type_: Option<()>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PySensorDriver)]
    driver: Option<()>,
    #[pydom(nested = PySensorParams)]
    accel: Option<()>,
    #[pydom(nested = PySensorParams)]
    gyro: Option<()>,
    #[pydom(nested = PyGeometry)]
    geometry: Option<()>,
}

#[pydom(py = PyInstanceRef, target = InstanceRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAssemblyModelRoot, ptype = AssemblyModelRoot, field = instance, card = opt),
    site(parent = PyAssemblyRef, ptype = AssemblyRef, field = instance, card = opt),
    site(parent = PyChannelRef, ptype = ChannelRef, field = instance, card = opt),
    site(parent = PyComponentNamedFrame, ptype = ComponentNamedFrame, field = instance, card = opt),
    site(parent = PyComponentOriginFrame, ptype = ComponentOriginFrame, field = instance, card = opt),
    site(parent = PyComponentRef, ptype = ComponentRef, field = instance, card = opt),
    site(parent = PyComponentVisualRoot, ptype = ComponentVisualRoot, field = instance, card = opt),
    site(parent = PyConnectivityFunctionRef, ptype = ConnectivityFunctionRef, field = instance, card = opt),
    site(parent = PyHopRef, ptype = HopRef, field = instance, card = opt),
    site(parent = PyLegRef, ptype = LegRef, field = instance, card = opt),
    site(parent = PyMacsecPolicyRef, ptype = MacsecPolicyRef, field = instance, card = opt),
    site(parent = PyNetworkRef, ptype = NetworkRef, field = instance, card = opt),
    site(parent = PyParticipantRef, ptype = ParticipantRef, field = instance, card = opt),
    site(parent = PyPortRef, ptype = PortRef, field = instance, card = opt),
    site(parent = PyScheduleRef, ptype = ScheduleRef, field = instance, card = opt),
    site(parent = PyTrafficClassRef, ptype = TrafficClassRef, field = instance, card = opt),
)]
struct InstanceRefDom {
    #[pydom(list = PyInstanceSegment, name_field = opt, clone_list)]
    segment: Vec<()>,
}

#[pydom(py = PyInstanceSegment, target = InstanceSegment, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyInstanceRef, ptype = InstanceRef, field = segment, card = vec),
)]
struct InstanceSegmentDom {
    name: Option<String>,
    #[pydom(required_scalar)]
    occurrence: u32,
}

#[pydom(py = PyJoint, target = Joint, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = joint, card = vec),
)]
struct JointDom {
    name: Option<String>,
    #[pydom(mapped_enum)]
    type_: Option<()>,
    thread_pitch: Option<String>,
    #[pydom(mapped_enum)]
    pitch_convention: Option<()>,
    #[pydom(mapped_enum)]
    handedness: Option<()>,
    description: Option<String>,
    #[pydom(nested = PyJointEndpoint)]
    parent: Option<()>,
    #[pydom(nested = PyJointEndpoint)]
    child: Option<()>,
    #[pydom(nested = PyPose)]
    origin: Option<()>,
    #[pydom(nested = PyAxis)]
    axis: Option<()>,
    #[pydom(nested = PyAxis)]
    axis2: Option<()>,
    #[pydom(nested = PyJointLimit)]
    limit2: Option<()>,
    #[pydom(nested = PyJointDynamics)]
    dynamics2: Option<()>,
    #[pydom(nested = PyJointLimit)]
    limit: Option<()>,
    #[pydom(nested = PySwingLimit)]
    swing_limit: Option<()>,
    #[pydom(nested = PyJointLimit)]
    twist_limit: Option<()>,
    #[pydom(nested = PyJointDynamics)]
    dynamics: Option<()>,
    #[pydom(nested = PyJointCalibration)]
    calibration: Option<()>,
    #[pydom(nested = PyMimic)]
    mimic: Option<()>,
    #[pydom(nested = PyLoopClosure)]
    loop_: Option<()>,
    #[pydom(nested = PyExtension)]
    extension: Option<()>,
    #[pydom(nested = PyUrdfCompatJoint)]
    urdf_compat: Option<()>,
    #[pydom(skip)]
    comments: (),
}

#[pydom(py = PyJointCalibration, target = JointCalibration, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = calibration, card = opt),
)]
struct JointCalibrationDom {
    reference_position: Option<String>,
    rising: Option<String>,
    falling: Option<String>,
}

#[pydom(py = PyJointDynamics, target = JointDynamics, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = dynamics, card = opt),
    site(parent = PyJoint, ptype = Joint, field = dynamics2, card = opt),
)]
struct JointDynamicsDom {
    damping: Option<String>,
    friction: Option<String>,
    spring_stiffness: Option<String>,
    spring_reference: Option<String>,
}

#[pydom(py = PyJointEndpoint, target = JointEndpoint, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = child, card = opt),
    site(parent = PyJoint, ptype = Joint, field = parent, card = opt),
)]
struct JointEndpointDom {
    comp: Option<String>,
}

#[pydom(py = PyJointGroup, target = JointGroup, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = group, card = vec),
)]
struct JointGroupDom {
    name: Option<String>,
    type_: Option<String>,
    tip_comp: Option<String>,
    tip_frame: Option<String>,
    description: Option<String>,
    #[pydom(list = PyRef, name_field = none)]
    joint: Vec<()>,
    #[pydom(list = PyRef, name_field = none)]
    group: Vec<()>,
}

#[pydom(py = PyJointLimit, target = JointLimit, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = limit, card = opt),
    site(parent = PyJoint, ptype = Joint, field = limit2, card = opt),
    site(parent = PyJoint, ptype = Joint, field = twist_limit, card = opt),
)]
struct JointLimitDom {
    lower: Option<String>,
    upper: Option<String>,
    effort: Option<String>,
    velocity: Option<String>,
    acceleration: Option<String>,
    jerk: Option<String>,
    deceleration: Option<String>,
}

#[pydom(py = PyJointPosition, target = JointPosition, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyKinematicState, ptype = KinematicState, field = joint_position, card = vec),
)]
struct JointPositionDom {
    joint: Option<String>,
    value: Option<String>,
}

#[pydom(py = PyJunction, target = Junction, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAssembly, ptype = Assembly, field = busbar, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = manifold, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = optical_splitter, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = splice, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = tee, card = vec),
    site(parent = PyComp, ptype = Comp, field = busbar, card = vec),
    site(parent = PyComp, ptype = Comp, field = manifold, card = vec),
    site(parent = PyComp, ptype = Comp, field = optical_splitter, card = vec),
    site(parent = PyComp, ptype = Comp, field = splice, card = vec),
    site(parent = PyComp, ptype = Comp, field = tee, card = vec),
)]
struct JunctionDom {
    name: String,
    #[pydom(mapped_enum)]
    fidelity: (),
    #[pydom(list = PyPhysicalEndpointRef, name_field = none, clone_list)]
    attachment: Vec<()>,
    #[pydom(nested = PyRepresentation, clone_nested)]
    representation: Option<()>,
}

#[pydom(py = PyJunctionRef, target = JunctionRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyPhysicalEndpointChoice, ptype = PhysicalEndpointChoice, variant = Junction),
)]
struct JunctionRefDom {
    junction: String,
    #[pydom(choice = PyPhysicalOwnerChoice)]
    owner: (),
}

#[pydom(py = PyKinematicState, target = KinematicState, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = state, card = vec),
)]
struct KinematicStateDom {
    name: Option<String>,
    default: Option<String>,
    description: Option<String>,
    #[pydom(list = PyJointPosition, name_field = none)]
    joint_position: Vec<()>,
}

#[pydom(py = PyLedIllumination, target = LedIllumination, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHmiElement, ptype = HmiElement, field = illumination, card = opt),
)]
struct LedIlluminationDom {
    light_type: Option<String>,
    cast_shadows: Option<String>,
    intensity: Option<String>,
    diffuse: Option<String>,
    specular: Option<String>,
    direction: Option<String>,
    #[pydom(nested = PyLightAttenuation)]
    attenuation: Option<()>,
}

#[pydom(py = PyLeg, target = Leg, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyChain, ptype = Chain, field = leg, card = vec),
    site(parent = PyRing, ptype = Ring, field = leg, card = vec),
    site(parent = PyTree, ptype = Tree, field = leg, card = vec),
)]
struct LegDom {
    name: String,
    #[pydom(name = "from_")]
    #[pydom(required_nested = PyLegEnd)]
    from: (),
    #[pydom(required_nested = PyLegEnd)]
    to: (),
}

#[pydom(py = PyLegEnd, target = LegEnd, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyLeg, ptype = Leg, field = from, card = req),
    site(parent = PyLeg, ptype = Leg, field = to, card = req),
)]
struct LegEndDom {
    #[pydom(required_nested = PyHopRef)]
    hop: (),
    #[pydom(required_nested = PyParticipantRef)]
    participant: (),
}

#[pydom(py = PyLegRef, target = LegRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyTopologySegmentChoice, ptype = TopologySegmentChoice, variant = Leg),
)]
struct LegRefDom {
    network: String,
    leg: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyLidarParams, target = LidarParams, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyOpticalSensor, ptype = OpticalSensor, field = lidar_params, card = opt),
)]
struct LidarParamsDom {
    scan_type: Option<String>,
    channels: Option<String>,
    points_per_second: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    scan_rate: Option<()>,
    returns: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    wavelength: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    horizontal_fov: Option<()>,
    #[pydom(nested = PyRangeValue)]
    vertical_fov: Option<()>,
    #[pydom(nested = PyLidarRange)]
    range: Option<()>,
    #[pydom(nested = PyLidarScanPattern)]
    scan_pattern: Option<()>,
    #[pydom(nested = PyNoise)]
    noise: Option<()>,
}

#[pydom(py = PyLidarRange, target = LidarRange, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyLidarParams, ptype = LidarParams, field = range, card = opt),
)]
struct LidarRangeDom {
    #[pydom(nested = PyMeasuredValue)]
    min: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    resolution: Option<()>,
}

#[pydom(py = PyLidarScanAxis, target = LidarScanAxis, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyLidarScanPattern, ptype = LidarScanPattern, field = horizontal, card = opt),
    site(parent = PyLidarScanPattern, ptype = LidarScanPattern, field = vertical, card = opt),
)]
struct LidarScanAxisDom {
    samples: Option<String>,
    resolution: Option<String>,
    min_angle: Option<String>,
    max_angle: Option<String>,
}

#[pydom(py = PyLidarScanPattern, target = LidarScanPattern, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyLidarParams, ptype = LidarParams, field = scan_pattern, card = opt),
)]
struct LidarScanPatternDom {
    #[pydom(nested = PyLidarScanAxis)]
    horizontal: Option<()>,
    #[pydom(nested = PyLidarScanAxis)]
    vertical: Option<()>,
}

#[pydom(py = PyLightAttenuation, target = LightAttenuation, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyLedIllumination, ptype = LedIllumination, field = attenuation, card = opt),
)]
struct LightAttenuationDom {
    range: Option<String>,
    linear: Option<String>,
    constant: Option<String>,
    quadratic: Option<String>,
}

#[pydom(py = PyLink, target = Link, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = link, card = vec),
)]
struct LinkDom {
    name: String,
    description: Option<String>,
    #[pydom(nested = PyNetworkSelection, clone_nested)]
    selected: Option<()>,
    #[pydom(nested = PyNetworkConfiguration)]
    configuration: Option<()>,
    #[pydom(list = PyParticipant, name_field = req, clone_list)]
    participant: Vec<()>,
}

#[pydom(py = PyLoopClosure, target = LoopClosure, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = loop_, card = opt),
)]
struct LoopClosureDom {
    predecessor: Option<String>,
    successor: Option<String>,
    constraint_axes: Option<String>,
}

#[pydom(py = PyMacsecConfiguration, target = MacsecConfiguration, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkConfiguration, ptype = NetworkConfiguration, field = macsec, card = opt),
)]
struct MacsecConfigurationDom {
    #[pydom(list = PyMacsecPolicyDefinition, name_field = req, clone_list)]
    policy: Vec<()>,
    #[pydom(nested = PyMacsecDefaultPolicy, clone_nested)]
    default_policy: Option<()>,
    #[pydom(list = PyMacsecOverride, name_field = none, clone_list)]
    override_: Vec<()>,
}

#[pydom(py = PyMacsecDefaultPolicy, target = MacsecDefaultPolicy, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMacsecConfiguration, ptype = MacsecConfiguration, field = default_policy, card = opt),
)]
struct MacsecDefaultPolicyDom {
    #[pydom(required_nested = PyMacsecPolicyRef)]
    policy: (),
}

#[pydom(py = PyMacsecOverride, target = MacsecOverride, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMacsecConfiguration, ptype = MacsecConfiguration, field = override_, card = vec),
)]
struct MacsecOverrideDom {
    #[pydom(required_nested = PyTopologySegmentRef)]
    target: (),
    #[pydom(required_nested = PyMacsecPolicyRef)]
    policy: (),
}

#[pydom(py = PyMacsecPolicyDefinition, target = MacsecPolicyDefinition, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMacsecConfiguration, ptype = MacsecConfiguration, field = policy, card = vec),
)]
struct MacsecPolicyDefinitionDom {
    name: String,
    #[pydom(mapped_enum)]
    enforcement: (),
    cipher: Option<String>,
    key_agreement: Option<String>,
    confidentiality_offset: Option<u8>,
    rekey_interval_ns: Option<u64>,
    credential_store_ref: Option<String>,
}

#[pydom(py = PyMacsecPolicyRef, target = MacsecPolicyRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMacsecDefaultPolicy, ptype = MacsecDefaultPolicy, field = policy, card = req),
    site(parent = PyMacsecOverride, ptype = MacsecOverride, field = policy, card = req),
)]
struct MacsecPolicyRefDom {
    network: String,
    policy: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyMate, target = Mate, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = mate, card = vec),
)]
struct MateDom {
    name: String,
    #[pydom(mapped_enum)]
    fidelity: (),
    #[pydom(required_nested = PyConnectorRef)]
    first: (),
    #[pydom(required_nested = PyConnectorRef)]
    second: (),
    #[pydom(list = PyPositionMapping, name_field = none, clone_list)]
    position_mapping: Vec<()>,
}

#[pydom(py = PyMeasuredValue, target = MeasuredValue, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAerofoilSurface, ptype = AerofoilSurface, field = area, card = opt),
    site(parent = PyAerofoilSurface, ptype = AerofoilSurface, field = chord, card = opt),
    site(parent = PyAerofoilSurface, ptype = AerofoilSurface, field = dihedral, card = opt),
    site(parent = PyAerofoilSurface, ptype = AerofoilSurface, field = span, card = opt),
    site(parent = PyAerofoilSurface, ptype = AerofoilSurface, field = sweep, card = opt),
    site(parent = PyBatterySource, ptype = BatterySource, field = capacity, card = opt),
    site(parent = PyBatterySource, ptype = BatterySource, field = energy, card = opt),
    site(parent = PyBatterySource, ptype = BatterySource, field = max_charge_rate, card = opt),
    site(parent = PyBatterySource, ptype = BatterySource, field = max_discharge, card = opt),
    site(parent = PyBatterySource, ptype = BatterySource, field = max_voltage, card = opt),
    site(parent = PyBatterySource, ptype = BatterySource, field = min_voltage, card = opt),
    site(parent = PyBatterySource, ptype = BatterySource, field = nominal_voltage, card = opt),
    site(parent = PyCameraCalibration, ptype = CameraCalibration, field = reprojection_error, card = opt),
    site(parent = PyCameraCalibration, ptype = CameraCalibration, field = temperature, card = opt),
    site(parent = PyCameraLens, ptype = CameraLens, field = cutoff_angle, card = opt),
    site(parent = PyCameraParams, ptype = CameraParams, field = dynamic_range, card = opt),
    site(parent = PyCameraParams, ptype = CameraParams, field = netd, card = opt),
    site(parent = PyDataOutput, ptype = DataOutput, field = bandwidth, card = opt),
    site(parent = PyDataOutput, ptype = DataOutput, field = bandwidth_compressed, card = opt),
    site(parent = PyDataOutput, ptype = DataOutput, field = frame_size, card = opt),
    site(parent = PyFlowCharacterization, ptype = FlowCharacterization, field = max_flow_rate, card = opt),
    site(parent = PyFlowCharacterization, ptype = FlowCharacterization, field = peak_flow_rate, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = accuracy, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = airspeed_resolution, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = altitude_resolution, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = range, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = reference_density, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = reference_pressure, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = resolution, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = temperature_coefficient, card = opt),
    site(parent = PyFuelCell, ptype = FuelCell, field = peak_power, card = opt),
    site(parent = PyFuelCell, ptype = FuelCell, field = rated_power, card = opt),
    site(parent = PyGnssParams, ptype = GnssParams, field = accuracy_rtk, card = opt),
    site(parent = PyGnssParams, ptype = GnssParams, field = accuracy_standalone, card = opt),
    site(parent = PyGripperSurface, ptype = GripperSurface, field = contact_area, card = opt),
    site(parent = PyGripperSurface, ptype = GripperSurface, field = grip_force, card = opt),
    site(parent = PyGripperSurface, ptype = GripperSurface, field = magnetic_force, card = opt),
    site(parent = PyGripperSurface, ptype = GripperSurface, field = payload, card = opt),
    site(parent = PyGripperSurface, ptype = GripperSurface, field = vacuum_level, card = opt),
    site(parent = PyHydrofoilSurface, ptype = HydrofoilSurface, field = area, card = opt),
    site(parent = PyHydrofoilSurface, ptype = HydrofoilSurface, field = chord, card = opt),
    site(parent = PyHydrofoilSurface, ptype = HydrofoilSurface, field = span, card = opt),
    site(parent = PyLidarParams, ptype = LidarParams, field = horizontal_fov, card = opt),
    site(parent = PyLidarParams, ptype = LidarParams, field = scan_rate, card = opt),
    site(parent = PyLidarParams, ptype = LidarParams, field = wavelength, card = opt),
    site(parent = PyLidarRange, ptype = LidarRange, field = max, card = opt),
    site(parent = PyLidarRange, ptype = LidarRange, field = min, card = opt),
    site(parent = PyLidarRange, ptype = LidarRange, field = resolution, card = opt),
    site(parent = PyMotor, ptype = Motor, field = displacement, card = opt),
    site(parent = PyMotor, ptype = Motor, field = inductance, card = opt),
    site(parent = PyMotor, ptype = Motor, field = max_efficiency, card = opt),
    site(parent = PyMotor, ptype = Motor, field = max_power_out, card = opt),
    site(parent = PyMotor, ptype = Motor, field = max_speed, card = opt),
    site(parent = PyMotor, ptype = Motor, field = max_temperature, card = opt),
    site(parent = PyMotor, ptype = Motor, field = max_thrust, card = opt),
    site(parent = PyMotor, ptype = Motor, field = no_load_current, card = opt),
    site(parent = PyMotor, ptype = Motor, field = no_load_speed, card = opt),
    site(parent = PyMotor, ptype = Motor, field = resistance, card = opt),
    site(parent = PyMotor, ptype = Motor, field = rotor_inertia, card = opt),
    site(parent = PyMotor, ptype = Motor, field = stall_torque, card = opt),
    site(parent = PyMotor, ptype = Motor, field = stroke, card = opt),
    site(parent = PyMotor, ptype = Motor, field = thermal_resistance, card = opt),
    site(parent = PyMotor, ptype = Motor, field = torque_constant, card = opt),
    site(parent = PyMotor, ptype = Motor, field = velocity_constant, card = opt),
    site(parent = PyPropSurface, ptype = PropSurface, field = diameter, card = opt),
    site(parent = PyPropSurface, ptype = PropSurface, field = pitch, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = azimuth_fov, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = azimuth_resolution, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = elevation_fov, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = elevation_resolution, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = frequency, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = range_resolution, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = velocity_resolution, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = beam_width, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = range, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = resolution, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = sensing_area, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = sensitivity, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = spatial_resolution, card = opt),
    site(parent = PySensorParams, ptype = SensorParams, field = bandwidth, card = opt),
    site(parent = PySensorParams, ptype = SensorParams, field = odr, card = opt),
    site(parent = PySensorParams, ptype = SensorParams, field = range, card = opt),
    site(parent = PySensorParams, ptype = SensorParams, field = resolution, card = opt),
    site(parent = PySolarSource, ptype = SolarSource, field = area, card = opt),
    site(parent = PySolarSource, ptype = SolarSource, field = current_mpp, card = opt),
    site(parent = PySolarSource, ptype = SolarSource, field = open_circuit_voltage, card = opt),
    site(parent = PySolarSource, ptype = SolarSource, field = peak_power, card = opt),
    site(parent = PySolarSource, ptype = SolarSource, field = voltage_mpp, card = opt),
    site(parent = PySupercapacitorSource, ptype = SupercapacitorSource, field = capacitance, card = opt),
    site(parent = PySupercapacitorSource, ptype = SupercapacitorSource, field = energy, card = opt),
    site(parent = PySupercapacitorSource, ptype = SupercapacitorSource, field = esr, card = opt),
    site(parent = PySupercapacitorSource, ptype = SupercapacitorSource, field = max_current, card = opt),
    site(parent = PySupercapacitorSource, ptype = SupercapacitorSource, field = max_voltage, card = opt),
    site(parent = PySupercapacitorSource, ptype = SupercapacitorSource, field = peak_current, card = opt),
    site(parent = PyTankSource, ptype = TankSource, field = energy, card = opt),
    site(parent = PyTankSource, ptype = TankSource, field = pressure, card = opt),
    site(parent = PyTankSource, ptype = TankSource, field = volume, card = opt),
    site(parent = PyTrackSurface, ptype = TrackSurface, field = ground_pressure, card = opt),
    site(parent = PyTrackSurface, ptype = TrackSurface, field = length, card = opt),
    site(parent = PyTrackSurface, ptype = TrackSurface, field = width, card = opt),
    site(parent = PyTransmission, ptype = Transmission, field = backlash, card = opt),
    site(parent = PyTransmissionSpring, ptype = TransmissionSpring, field = clutch_power, card = opt),
    site(parent = PyTransmissionSpring, ptype = TransmissionSpring, field = damping, card = opt),
    site(parent = PyTransmissionSpring, ptype = TransmissionSpring, field = engage_time, card = opt),
    site(parent = PyTransmissionSpring, ptype = TransmissionSpring, field = equilibrium, card = opt),
    site(parent = PyTransmissionSpring, ptype = TransmissionSpring, field = max_deflection, card = opt),
    site(parent = PyWheelSurface, ptype = WheelSurface, field = radius, card = opt),
    site(parent = PyWheelSurface, ptype = WheelSurface, field = width, card = opt),
)]
struct MeasuredValueDom {
    unit: Option<String>,
    value: Option<String>,
}

#[pydom(py = PyMesh, target = Mesh, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollisionGeometry, ptype = CollisionGeometry, field = mesh, card = opt),
    site(parent = PyGeometry, ptype = Geometry, field = mesh, card = opt),
)]
struct MeshDom {
    uri: Option<String>,
    scale: Option<String>,
    sha: Option<String>,
    source_uri: Option<String>,
    #[pydom(nested = PySubmesh)]
    submesh: Option<()>,
}

#[pydom(py = PyMimic, target = Mimic, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = mimic, card = opt),
)]
struct MimicDom {
    joint: Option<String>,
    multiplier: Option<String>,
    offset: Option<String>,
}

#[pydom(py = PyModelPartRepresentation, target = ModelPartRepresentation, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRepresentationChoice, ptype = RepresentationChoice, variant = ModelPart),
)]
struct ModelPartRepresentationDom {
    node_path: String,
    submesh_fallback: Option<String>,
    #[pydom(required_nested = PyModelRoot)]
    model_root: (),
}

#[pydom(py = PyModelRepresentation, target = ModelRepresentation, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRepresentationChoice, ptype = RepresentationChoice, variant = Model),
)]
struct ModelRepresentationDom {
    uri: String,
    sha: Option<String>,
    node_path: Option<String>,
    #[pydom(required_nested = PyPlacement)]
    placement: (),
}

#[pydom(py = PyModelRoot, target = ModelRoot, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyModelPartRepresentation, ptype = ModelPartRepresentation, field = model_root, card = req),
)]
struct ModelRootDom {
    #[pydom(choice = PyModelRootChoice)]
    root: (),
}

#[pydom(py = PyMotor, target = Motor, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = motor, card = vec),
)]
struct MotorDom {
    name: Option<String>,
    #[pydom(mapped_enum)]
    type_: Option<()>,
    construction: Option<String>,
    visual: Option<String>,
    mesh: Option<String>,
    encoder: Option<String>,
    hall: Option<String>,
    thermistor: Option<String>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PyRatedValue)]
    voltage: Option<()>,
    #[pydom(nested = PyRatedValue)]
    current: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    resistance: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    inductance: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    torque_constant: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    velocity_constant: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    rotor_inertia: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_speed: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    stall_torque: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    thermal_resistance: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_temperature: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    no_load_speed: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    no_load_current: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_power_out: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_efficiency: Option<()>,
    thrust_axis: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    max_thrust: Option<()>,
    pole_pairs: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    displacement: Option<()>,
    cylinders: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    stroke: Option<()>,
    #[pydom(nested = PyControlModes)]
    control_modes: Option<()>,
}

#[pydom(py = PyNamedQuantity, target = NamedQuantity, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyTermination, ptype = Termination, field = quantity, card = vec),
)]
struct NamedQuantityDom {
    property: String,
    #[pydom(required_scalar)]
    value: f64,
    unit: String,
}

#[pydom(py = PyNetworkConfiguration, target = NetworkConfiguration, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyBus, ptype = Bus, field = configuration, card = opt),
    site(parent = PyChain, ptype = Chain, field = configuration, card = opt),
    site(parent = PyConnectivityMesh, ptype = ConnectivityMesh, field = configuration, card = opt),
    site(parent = PyLink, ptype = Link, field = configuration, card = opt),
    site(parent = PyRing, ptype = Ring, field = configuration, card = opt),
    site(parent = PyStar, ptype = Star, field = configuration, card = opt),
    site(parent = PyTree, ptype = Tree, field = configuration, card = opt),
)]
struct NetworkConfigurationDom {
    #[pydom(list = PyGptpDomain, name_field = req, clone_list)]
    gptp_domain: Vec<()>,
    #[pydom(list = PyTrafficClass, name_field = req, clone_list)]
    traffic_class: Vec<()>,
    #[pydom(list = PyGateSchedule, name_field = req, clone_list)]
    gate_schedule: Vec<()>,
    #[pydom(list = PyScheduleAssignment, name_field = req, clone_list)]
    schedule_assignment: Vec<()>,
    #[pydom(nested = PyPlcaConfiguration, clone_nested)]
    plca: Option<()>,
    #[pydom(nested = PyMacsecConfiguration, clone_nested)]
    macsec: Option<()>,
    #[pydom(nested = PyEeeConfiguration, clone_nested)]
    eee: Option<()>,
}

#[pydom(py = PyNetworkRef, target = NetworkRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyTopologySegmentChoice, ptype = TopologySegmentChoice, variant = Network),
)]
struct NetworkRefDom {
    network: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyNetworkSelection, target = NetworkSelection, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyBus, ptype = Bus, field = selected, card = opt),
    site(parent = PyChain, ptype = Chain, field = selected, card = opt),
    site(parent = PyConnectivityMesh, ptype = ConnectivityMesh, field = selected, card = opt),
    site(parent = PyLink, ptype = Link, field = selected, card = opt),
    site(parent = PyRing, ptype = Ring, field = selected, card = opt),
    site(parent = PyStar, ptype = Star, field = selected, card = opt),
    site(parent = PyTree, ptype = Tree, field = selected, card = opt),
)]
struct NetworkSelectionDom {
    #[pydom(mapped_enum)]
    purpose: (),
    #[pydom(mapped_enum)]
    carrier: (),
    #[pydom(list = PySelectedProfile, name_field = none, clone_list)]
    profile: Vec<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    rate: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    voltage: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    current: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    power: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    impedance: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    frequency: Option<()>,
    #[pydom(nested = PyRfSelection, clone_nested)]
    rf: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    pressure: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    flow: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    temperature: Option<()>,
}

#[pydom(py = PyNoise, target = Noise, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAxisNoise, ptype = AxisNoise, field = x, card = opt),
    site(parent = PyAxisNoise, ptype = AxisNoise, field = y, card = opt),
    site(parent = PyAxisNoise, ptype = AxisNoise, field = z, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = noise, card = opt),
    site(parent = PyLidarParams, ptype = LidarParams, field = noise, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = noise, card = opt),
    site(parent = PySensorFov, ptype = SensorFov, field = noise, card = opt),
    site(parent = PySensorParams, ptype = SensorParams, field = noise, card = opt),
)]
struct NoiseDom {
    #[pydom(mapped_enum)]
    type_: Option<()>,
    mean: Option<String>,
    stddev: Option<String>,
    bias_mean: Option<String>,
    bias_stddev: Option<String>,
    precision: Option<String>,
    dynamic_bias_stddev: Option<String>,
    dynamic_bias_correlation_time: Option<String>,
}

#[pydom(py = PyOpenTrafficClasses, target = OpenTrafficClasses, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyGateControlEntry, ptype = GateControlEntry, field = open, card = req),
)]
struct OpenTrafficClassesDom {
    #[pydom(list = PyTrafficClassRef, name_field = none, clone_list)]
    traffic_class: Vec<()>,
}

#[pydom(py = PyOpticalSensor, target = OpticalSensor, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensor, ptype = Sensor, field = optical, card = vec),
)]
struct OpticalSensorDom {
    #[pydom(mapped_enum)]
    type_: Option<()>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PySensorDriver)]
    driver: Option<()>,
    #[pydom(nested = PyGeometry)]
    geometry: Option<()>,
    #[pydom(list = PySensorFov, name_field = opt)]
    fov: Vec<()>,
    #[pydom(nested = PyDataOutput)]
    data_output: Option<()>,
    #[pydom(nested = PyCameraParams)]
    camera_params: Option<()>,
    #[pydom(nested = PyLidarParams)]
    lidar_params: Option<()>,
}

#[pydom(py = PyParticipant, target = Participant, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyBus, ptype = Bus, field = participant, card = vec),
    site(parent = PyChain, ptype = Chain, field = participant, card = vec),
    site(parent = PyConnectivityMesh, ptype = ConnectivityMesh, field = participant, card = vec),
    site(parent = PyLink, ptype = Link, field = participant, card = vec),
    site(parent = PyRing, ptype = Ring, field = participant, card = vec),
    site(parent = PyStar, ptype = Star, field = participant, card = vec),
    site(parent = PyTree, ptype = Tree, field = participant, card = vec),
)]
struct ParticipantDom {
    name: String,
    role: Option<String>,
    #[pydom(required_nested = PyFunctionalEndpointRef)]
    endpoint: (),
}

#[pydom(py = PyParticipantRef, target = ParticipantRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyEeeOverride, ptype = EeeOverride, field = participant, card = req),
    site(parent = PyGptpClock, ptype = GptpClock, field = participant, card = req),
    site(parent = PyLegEnd, ptype = LegEnd, field = participant, card = req),
    site(parent = PyParticipantReference, ptype = ParticipantReference, field = participant, card = req),
    site(parent = PyPlcaNode, ptype = PlcaNode, field = participant, card = req),
    site(parent = PyScheduleTarget, ptype = ScheduleTarget, field = participant, card = req),
)]
struct ParticipantRefDom {
    network: String,
    participant: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyParticipantReference, target = ParticipantReference, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyStar, ptype = Star, field = coordinator, card = req),
)]
struct ParticipantReferenceDom {
    #[pydom(required_nested = PyParticipantRef)]
    participant: (),
}

#[pydom(py = PyPath, target = Path, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAssembly, ptype = Assembly, field = cable_member, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = coax, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = conductor, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = feed, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = fiber, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = hose, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = passage, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = pipe, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = waveguide, card = vec),
    site(parent = PyAssembly, ptype = Assembly, field = wire, card = vec),
    site(parent = PyComp, ptype = Comp, field = cable_member, card = vec),
    site(parent = PyComp, ptype = Comp, field = coax, card = vec),
    site(parent = PyComp, ptype = Comp, field = conductor, card = vec),
    site(parent = PyComp, ptype = Comp, field = feed, card = vec),
    site(parent = PyComp, ptype = Comp, field = fiber, card = vec),
    site(parent = PyComp, ptype = Comp, field = hose, card = vec),
    site(parent = PyComp, ptype = Comp, field = passage, card = vec),
    site(parent = PyComp, ptype = Comp, field = pipe, card = vec),
    site(parent = PyComp, ptype = Comp, field = waveguide, card = vec),
    site(parent = PyComp, ptype = Comp, field = wire, card = vec),
)]
struct PathDom {
    name: String,
    #[pydom(mapped_enum)]
    fidelity: (),
    role: Option<String>,
    local_group: Option<String>,
    #[pydom(required_nested = PyPhysicalEndpointRef)]
    first: (),
    #[pydom(required_nested = PyPhysicalEndpointRef)]
    second: (),
    #[pydom(nested = PyRepresentation, clone_nested)]
    representation: Option<()>,
}

#[pydom(py = PyPcpValue, target = PcpValue, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyTrafficClass, ptype = TrafficClass, field = pcp, card = vec),
)]
struct PcpValueDom {
    #[pydom(required_scalar)]
    value: u8,
}

#[pydom(py = PyPhysicalEndpointRef, target = PhysicalEndpointRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyBinding, ptype = Binding, field = physical, card = req),
    site(parent = PyJunction, ptype = Junction, field = attachment, card = vec),
    site(parent = PyPath, ptype = Path, field = first, card = req),
    site(parent = PyPath, ptype = Path, field = second, card = req),
    site(parent = PyTermination, ptype = Termination, field = attachment, card = vec),
)]
struct PhysicalEndpointRefDom {
    #[pydom(choice = PyPhysicalEndpointChoice)]
    endpoint: (),
}

#[pydom(py = PyPlacement, target = Placement, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyModelRepresentation, ptype = ModelRepresentation, field = placement, card = req),
    site(parent = PyPrimitiveBox, ptype = PrimitiveBox, field = placement, card = req),
    site(parent = PyPrimitiveCylinder, ptype = PrimitiveCylinder, field = placement, card = req),
    site(parent = PyPrimitiveSphere, ptype = PrimitiveSphere, field = placement, card = req),
)]
struct PlacementDom {
    #[pydom(arr = 3)]
    xyz: [f64; 3],
    #[pydom(required_nested = PyRouteFrame)]
    frame: (),
    #[pydom(required_nested = PyPlacementRotation)]
    rotation: (),
}

#[pydom(py = PyPlacementRotation, target = PlacementRotation, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPlacement, ptype = Placement, field = rotation, card = req),
    site(parent = PyRoutePoint, ptype = RoutePoint, field = rotation, card = opt),
)]
struct PlacementRotationDom {
    #[pydom(choice = PyPlacementRotationChoice)]
    rotation: (),
}

#[pydom(py = PyPlcaConfiguration, target = PlcaConfiguration, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkConfiguration, ptype = NetworkConfiguration, field = plca, card = opt),
)]
struct PlcaConfigurationDom {
    #[pydom(required_scalar)]
    max_node_id: u8,
    #[pydom(required_scalar)]
    to_timer_bit_times: u16,
    #[pydom(list = PyPlcaNode, name_field = none, clone_list)]
    node: Vec<()>,
}

#[pydom(py = PyPlcaNode, target = PlcaNode, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPlcaConfiguration, ptype = PlcaConfiguration, field = node, card = vec),
)]
struct PlcaNodeDom {
    #[pydom(required_scalar)]
    id: u8,
    #[pydom(required_scalar)]
    burst_count: u8,
    #[pydom(required_scalar)]
    burst_timer_bit_times: u16,
    #[pydom(required_nested = PyParticipantRef)]
    participant: (),
}

#[pydom(py = PyPort, target = Port, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = port, card = vec),
)]
struct PortDom {
    name: String,
    #[pydom(nested = PyCapabilities)]
    capabilities: Option<()>,
    #[pydom(list = PyChannel, name_field = req)]
    channel: Vec<()>,
}

#[pydom(py = PyPortRef, target = PortRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAntenna, ptype = Antenna, field = conducted_port, card = opt),
    site(parent = PyAntenna, ptype = Antenna, field = radiated_port, card = req),
    choice_site(parent = PyFunctionalEndpointChoice, ptype = FunctionalEndpointChoice, variant = Port),
)]
struct PortRefDom {
    component: String,
    port: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyPose, target = Pose, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollision, ptype = Collision, field = pose, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = pose, card = opt),
    site(parent = PyFrame, ptype = Frame, field = pose, card = opt),
    site(parent = PyHmiElement, ptype = HmiElement, field = pose, card = opt),
    site(parent = PyInertial, ptype = Inertial, field = inertia_origin, card = opt),
    site(parent = PyInertialSensor, ptype = InertialSensor, field = pose, card = opt),
    site(parent = PyJoint, ptype = Joint, field = origin, card = opt),
    site(parent = PyMotor, ptype = Motor, field = pose, card = opt),
    site(parent = PyOpticalSensor, ptype = OpticalSensor, field = pose, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = pose, card = opt),
    site(parent = PySensorFov, ptype = SensorFov, field = pose, card = opt),
    site(parent = PyVisual, ptype = Visual, field = pose, card = opt),
)]
struct PoseDom {
    #[pydom(arr = 3)]
    xyz: Option<()>,
    #[pydom(arr = 3)]
    rpy: Option<()>,
    #[pydom(arr = 4)]
    quat: Option<()>,
}

#[pydom(py = PyPosition, target = Position, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyConnector, ptype = Connector, field = contact, card = vec),
    site(parent = PyConnector, ptype = Connector, field = feed, card = vec),
    site(parent = PyConnector, ptype = Connector, field = fiber, card = vec),
    site(parent = PyConnector, ptype = Connector, field = passage, card = vec),
    site(parent = PyConnector, ptype = Connector, field = pin, card = vec),
    site(parent = PyConnector, ptype = Connector, field = socket, card = vec),
    site(parent = PyConnector, ptype = Connector, field = waveguide_opening, card = vec),
)]
struct PositionDom {
    name: String,
    role: Option<String>,
    local_group: Option<String>,
    #[pydom(nested = PyRepresentation, clone_nested)]
    representation: Option<()>,
}

#[pydom(py = PyPositionMapping, target = PositionMapping, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMate, ptype = Mate, field = position_mapping, card = vec),
)]
struct PositionMappingDom {
    #[pydom(required_nested = PyPositionRef)]
    first: (),
    #[pydom(required_nested = PyPositionRef)]
    second: (),
}

#[pydom(py = PyPositionRef, target = PositionRef, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyPhysicalEndpointChoice, ptype = PhysicalEndpointChoice, variant = Position),
    site(parent = PyPositionMapping, ptype = PositionMapping, field = first, card = req),
    site(parent = PyPositionMapping, ptype = PositionMapping, field = second, card = req),
)]
struct PositionRefDom {
    connector: String,
    position: String,
    #[pydom(choice = PyPhysicalOwnerChoice)]
    owner: (),
}

#[pydom(py = PyPowerSource, target = PowerSource, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = power_source, card = vec),
)]
struct PowerSourceDom {
    name: Option<String>,
    role: Option<String>,
    group: Option<String>,
    visual: Option<String>,
    mesh: Option<String>,
    #[pydom(nested = PyBatterySource)]
    battery: Option<()>,
    #[pydom(nested = PyTankSource)]
    tank: Option<()>,
    #[pydom(nested = PyFuelCell)]
    fuel_cell: Option<()>,
    #[pydom(nested = PySolarSource)]
    solar: Option<()>,
    #[pydom(nested = PySupercapacitorSource)]
    supercapacitor: Option<()>,
}

#[pydom(py = PyPrimitiveBox, target = PrimitiveBox, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRepresentationChoice, ptype = RepresentationChoice, variant = Box),
)]
struct PrimitiveBoxDom {
    #[pydom(arr = 3)]
    size: [f64; 3],
    #[pydom(required_nested = PyPlacement)]
    placement: (),
}

#[pydom(py = PyPrimitiveCylinder, target = PrimitiveCylinder, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRepresentationChoice, ptype = RepresentationChoice, variant = Cylinder),
)]
struct PrimitiveCylinderDom {
    #[pydom(required_scalar)]
    radius: f64,
    #[pydom(required_scalar)]
    length: f64,
    #[pydom(required_nested = PyPlacement)]
    placement: (),
}

#[pydom(py = PyPrimitiveSphere, target = PrimitiveSphere, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRepresentationChoice, ptype = RepresentationChoice, variant = Sphere),
)]
struct PrimitiveSphereDom {
    #[pydom(required_scalar)]
    radius: f64,
    #[pydom(required_nested = PyPlacement)]
    placement: (),
}

#[pydom(py = PyProfileCapability, target = ProfileCapability, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCapabilities, ptype = Capabilities, field = profile, card = vec),
)]
struct ProfileCapabilityDom {
    id: String,
}

#[pydom(py = PyPropSurface, target = PropSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDynamicSurface, ptype = DynamicSurface, field = prop, card = opt),
)]
struct PropSurfaceDom {
    #[pydom(nested = PyMeasuredValue)]
    diameter: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    pitch: Option<()>,
    blades: Option<String>,
    direction: Option<String>,
}

#[pydom(py = PyPurposeCapability, target = PurposeCapability, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCapabilities, ptype = Capabilities, field = purpose, card = vec),
)]
struct PurposeCapabilityDom {
    #[pydom(mapped_enum)]
    value: (),
}

#[pydom(py = PyQuantity, target = Quantity, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PySelectionQuantityChoice, ptype = SelectionQuantityChoice, variant = Nominal),
)]
struct QuantityDom {
    #[pydom(required_scalar)]
    value: f64,
    unit: String,
}

#[pydom(py = PyQuantityRange, target = QuantityRange, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCapabilities, ptype = Capabilities, field = bandwidth, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = current, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = flow, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = frequency, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = impedance, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = power, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = pressure, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = rate, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = temperature, card = opt),
    site(parent = PyCapabilities, ptype = Capabilities, field = voltage, card = opt),
)]
struct QuantityRangeDom {
    minimum: Option<f64>,
    maximum: Option<f64>,
    nominal: Option<f64>,
    unit: String,
}

#[pydom(py = PyQuaternionRotation, target = QuaternionRotation, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyPlacementRotationChoice, ptype = PlacementRotationChoice, variant = Quaternion),
)]
struct QuaternionRotationDom {
    #[pydom(arr = 4)]
    value: [f64; 4],
}

#[pydom(py = PyRadarParams, target = RadarParams, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensorCategory, ptype = SensorCategory, field = radar_params, card = opt),
)]
struct RadarParamsDom {
    #[pydom(nested = PyMeasuredValue)]
    frequency: Option<()>,
    modulation: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    azimuth_fov: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    elevation_fov: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    azimuth_resolution: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    elevation_resolution: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    range_resolution: Option<()>,
    #[pydom(nested = PyRangeValue)]
    velocity_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    velocity_resolution: Option<()>,
    max_detections: Option<String>,
    mimo: Option<String>,
}

#[pydom(py = PyRangeValue, target = RangeValue, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCameraParams, ptype = CameraParams, field = depth_range, card = opt),
    site(parent = PyCameraParams, ptype = CameraParams, field = temperature_range, card = opt),
    site(parent = PyComp, ptype = Comp, field = operating_temp, card = opt),
    site(parent = PyControlSurface, ptype = ControlSurface, field = deflection, card = opt),
    site(parent = PyFlowCharacterization, ptype = FlowCharacterization, field = outlet_pressure, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = airspeed_range, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = altitude_range, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = depth_range, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = flow_range, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = level_range, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = pressure_range, card = opt),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = temperature_range, card = opt),
    site(parent = PyFuelCell, ptype = FuelCell, field = output_voltage, card = opt),
    site(parent = PyLidarParams, ptype = LidarParams, field = vertical_fov, card = opt),
    site(parent = PyRadarParams, ptype = RadarParams, field = velocity_range, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = frequency_range, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = pressure_range, card = opt),
    site(parent = PyTransmissionSpring, ptype = TransmissionSpring, field = equilibrium_range, card = opt),
    site(parent = PyTransmissionSpring, ptype = TransmissionSpring, field = stiffness, card = opt),
)]
struct RangeValueDom {
    unit: Option<String>,
    min: Option<String>,
    max: Option<String>,
    value: Option<String>,
}

#[pydom(py = PyRatedValue, target = RatedValue, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMotor, ptype = Motor, field = current, card = opt),
    site(parent = PyMotor, ptype = Motor, field = voltage, card = opt),
)]
struct RatedValueDom {
    unit: Option<String>,
    nominal: Option<String>,
    continuous: Option<String>,
    peak: Option<String>,
    max: Option<String>,
    value: Option<String>,
}

#[pydom(py = PyRectangularRouteSection, target = RectangularRouteSection, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRouteSectionChoice, ptype = RouteSectionChoice, variant = Rectangular),
)]
struct RectangularRouteSectionDom {
    #[pydom(required_scalar)]
    width: f64,
    #[pydom(required_scalar)]
    height: f64,
}

#[pydom(py = PyRef, target = Ref, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJointGroup, ptype = JointGroup, field = group, card = vec),
    site(parent = PyJointGroup, ptype = JointGroup, field = joint, card = vec),
)]
struct RefDom {
    ref_: Option<String>,
}

#[pydom(py = PyRepresentation, target = Representation, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAntenna, ptype = Antenna, field = representation, card = opt),
    site(parent = PyAssembly, ptype = Assembly, field = representation, card = opt),
    site(parent = PyConnector, ptype = Connector, field = representation, card = opt),
    site(parent = PyJunction, ptype = Junction, field = representation, card = opt),
    site(parent = PyPath, ptype = Path, field = representation, card = opt),
    site(parent = PyPosition, ptype = Position, field = representation, card = opt),
    site(parent = PyTermination, ptype = Termination, field = representation, card = opt),
)]
struct RepresentationDom {
    #[pydom(choice = PyRepresentationChoice)]
    variant: (),
}

#[pydom(py = PyRfChannelSelection, target = RfChannelSelection, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyRfSelection, ptype = RfSelection, field = channel, card = req),
)]
struct RfChannelSelectionDom {
    #[pydom(choice = PyRfChannelSelectionChoice)]
    selection: (),
}

#[pydom(py = PyRfFrequencyDefinedChannel, target = RfFrequencyDefinedChannel, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRfChannelSelectionChoice, ptype = RfChannelSelectionChoice, variant = FrequencyDefined),
)]
struct RfFrequencyDefinedChannelDom {
    #[pydom(required_nested = PySelectionQuantity)]
    center_frequency: (),
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    bandwidth: Option<()>,
}

#[pydom(py = PyRfNumberedChannel, target = RfNumberedChannel, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRfChannelSelectionChoice, ptype = RfChannelSelectionChoice, variant = Numbered),
)]
struct RfNumberedChannelDom {
    #[pydom(required_scalar)]
    number: u32,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    center_frequency: Option<()>,
    #[pydom(nested = PySelectionQuantity, clone_nested)]
    bandwidth: Option<()>,
}

#[pydom(py = PyRfSelection, target = RfSelection, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = rf, card = opt),
)]
struct RfSelectionDom {
    #[pydom(required_nested = PyRfChannelSelection)]
    channel: (),
}

#[pydom(py = PyRing, target = Ring, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = ring, card = vec),
)]
struct RingDom {
    name: String,
    description: Option<String>,
    #[pydom(nested = PyNetworkSelection, clone_nested)]
    selected: Option<()>,
    #[pydom(nested = PyNetworkConfiguration)]
    configuration: Option<()>,
    #[pydom(list = PyParticipant, name_field = req, clone_list)]
    participant: Vec<()>,
    #[pydom(list = PyHop, name_field = req, clone_list)]
    hop: Vec<()>,
    #[pydom(list = PyLeg, name_field = req, clone_list)]
    leg: Vec<()>,
}

#[pydom(py = PyRoundRouteSection, target = RoundRouteSection, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRouteSectionChoice, ptype = RouteSectionChoice, variant = Round),
)]
struct RoundRouteSectionDom {
    #[pydom(required_scalar)]
    diameter: f64,
}

#[pydom(py = PyRouteFrame, target = RouteFrame, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPlacement, ptype = Placement, field = frame, card = req),
    site(parent = PyRoutePoint, ptype = RoutePoint, field = frame, card = req),
)]
struct RouteFrameDom {
    #[pydom(choice = PyRouteFrameChoice)]
    frame: (),
}

#[pydom(py = PyRoutePoint, target = RoutePoint, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDerivedRouteRepresentation, ptype = DerivedRouteRepresentation, field = waypoint, card = vec),
)]
struct RoutePointDom {
    #[pydom(arr = 3)]
    xyz: [f64; 3],
    #[pydom(required_nested = PyRouteFrame)]
    frame: (),
    #[pydom(nested = PyPlacementRotation, clone_nested)]
    rotation: Option<()>,
}

#[pydom(py = PyRpyRotation, target = RpyRotation, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyPlacementRotationChoice, ptype = PlacementRotationChoice, variant = Rpy),
)]
struct RpyRotationDom {
    #[pydom(arr = 3)]
    value: [f64; 3],
}

#[pydom(py = PySafetyController, target = SafetyController, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyUrdfCompatJoint, ptype = UrdfCompatJoint, field = safety_controller, card = opt),
)]
struct SafetyControllerDom {
    soft_lower_limit: Option<String>,
    soft_upper_limit: Option<String>,
    k_position: Option<String>,
    k_velocity: Option<String>,
}

#[pydom(py = PyScheduleAssignment, target = ScheduleAssignment, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkConfiguration, ptype = NetworkConfiguration, field = schedule_assignment, card = vec),
)]
struct ScheduleAssignmentDom {
    name: String,
    #[pydom(required_nested = PyScheduleRef)]
    schedule: (),
    #[pydom(list = PyScheduleTarget, name_field = none, clone_list)]
    target: Vec<()>,
}

#[pydom(py = PyScheduleRef, target = ScheduleRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyScheduleAssignment, ptype = ScheduleAssignment, field = schedule, card = req),
)]
struct ScheduleRefDom {
    network: String,
    schedule: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyScheduleTarget, target = ScheduleTarget, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyScheduleAssignment, ptype = ScheduleAssignment, field = target, card = vec),
)]
struct ScheduleTargetDom {
    #[pydom(required_nested = PyParticipantRef)]
    participant: (),
}

#[pydom(py = PySelectedProfile, target = SelectedProfile, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = profile, card = vec),
)]
struct SelectedProfileDom {
    id: String,
}

#[pydom(py = PySelectionQuantity, target = SelectionQuantity, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = current, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = flow, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = frequency, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = impedance, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = power, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = pressure, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = rate, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = temperature, card = opt),
    site(parent = PyNetworkSelection, ptype = NetworkSelection, field = voltage, card = opt),
    site(parent = PyRfFrequencyDefinedChannel, ptype = RfFrequencyDefinedChannel, field = bandwidth, card = opt),
    site(parent = PyRfFrequencyDefinedChannel, ptype = RfFrequencyDefinedChannel, field = center_frequency, card = req),
    site(parent = PyRfNumberedChannel, ptype = RfNumberedChannel, field = bandwidth, card = opt),
    site(parent = PyRfNumberedChannel, ptype = RfNumberedChannel, field = center_frequency, card = opt),
)]
struct SelectionQuantityDom {
    #[pydom(choice = PySelectionQuantityChoice)]
    selection: (),
}

#[pydom(py = PySelectionRange, target = SelectionRange, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PySelectionQuantityChoice, ptype = SelectionQuantityChoice, variant = Range),
)]
struct SelectionRangeDom {
    #[pydom(required_scalar)]
    minimum: f64,
    #[pydom(required_scalar)]
    maximum: f64,
    nominal: Option<f64>,
    unit: String,
}

#[pydom(py = PySelfCollisionDisable, target = SelfCollisionDisable, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = self_collision_disable, card = opt),
)]
struct SelfCollisionDisableDom {
    #[pydom(list = PyCollisionPair, name_field = none)]
    pair: Vec<()>,
}

#[pydom(py = PySensor, target = Sensor, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = sensor, card = vec),
)]
struct SensorDom {
    name: Option<String>,
    update_rate: Option<String>,
    #[pydom(list = PyInertialSensor, name_field = none)]
    inertial: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    em: Vec<()>,
    #[pydom(list = PyOpticalSensor, name_field = none)]
    optical: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    rf: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    chemical: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    force: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    encoder: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    temperature: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    radiation: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    audio: Vec<()>,
    #[pydom(list = PySensorCategory, name_field = none)]
    tactile: Vec<()>,
    #[pydom(list = PyFluidSensor, name_field = none)]
    fluid: Vec<()>,
}

#[pydom(py = PySensorCategory, target = SensorCategory, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySensor, ptype = Sensor, field = audio, card = vec),
    site(parent = PySensor, ptype = Sensor, field = chemical, card = vec),
    site(parent = PySensor, ptype = Sensor, field = em, card = vec),
    site(parent = PySensor, ptype = Sensor, field = encoder, card = vec),
    site(parent = PySensor, ptype = Sensor, field = force, card = vec),
    site(parent = PySensor, ptype = Sensor, field = radiation, card = vec),
    site(parent = PySensor, ptype = Sensor, field = rf, card = vec),
    site(parent = PySensor, ptype = Sensor, field = tactile, card = vec),
    site(parent = PySensor, ptype = Sensor, field = temperature, card = vec),
)]
struct SensorCategoryDom {
    type_: Option<String>,
    #[pydom(mapped_enum)]
    principle: Option<()>,
    interface: Option<String>,
    active: Option<String>,
    #[pydom(mapped_enum)]
    frame: Option<()>,
    #[pydom(mapped_enum)]
    measure_direction: Option<()>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PySensorDriver)]
    driver: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    resolution: Option<()>,
    #[pydom(nested = PyNoise)]
    noise: Option<()>,
    #[pydom(nested = PyDataOutput)]
    data_output: Option<()>,
    #[pydom(nested = PyGeometry)]
    geometry: Option<()>,
    #[pydom(nested = PyRadarParams)]
    radar_params: Option<()>,
    #[pydom(nested = PyGnssParams)]
    gnss_params: Option<()>,
    #[pydom(nested = PyRangeValue)]
    frequency_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    sensitivity: Option<()>,
    channels: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    beam_width: Option<()>,
    rows: Option<String>,
    cols: Option<String>,
    taxels: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    spatial_resolution: Option<()>,
    #[pydom(nested = PyRangeValue)]
    pressure_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    sensing_area: Option<()>,
    collision: Option<String>,
    #[pydom(nested = PyForceAxisNoise)]
    axis_noise: Option<()>,
}

#[pydom(py = PySensorDriver, target = SensorDriver, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyFluidSensor, ptype = FluidSensor, field = driver, card = opt),
    site(parent = PyInertialSensor, ptype = InertialSensor, field = driver, card = opt),
    site(parent = PyOpticalSensor, ptype = OpticalSensor, field = driver, card = opt),
    site(parent = PySensorCategory, ptype = SensorCategory, field = driver, card = opt),
)]
struct SensorDriverDom {
    name: Option<String>,
    #[pydom(nested = PyAxisAlign)]
    axis_align: Option<()>,
}

#[pydom(py = PySensorFov, target = SensorFov, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyOpticalSensor, ptype = OpticalSensor, field = fov, card = vec),
)]
struct SensorFovDom {
    name: Option<String>,
    color: Option<String>,
    description: Option<String>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(nested = PyGeometry)]
    geometry: Option<()>,
    #[pydom(nested = PyCameraIntrinsics)]
    intrinsics: Option<()>,
    #[pydom(nested = PyCameraDistortion)]
    distortion: Option<()>,
    #[pydom(nested = PyCameraLens)]
    lens: Option<()>,
    #[pydom(nested = PyCameraCalibration)]
    calibration: Option<()>,
    #[pydom(nested = PyNoise)]
    noise: Option<()>,
    #[pydom(nested = PyCameraMatrix)]
    camera_matrix: Option<()>,
}

#[pydom(py = PySensorParams, target = SensorParams, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyInertialSensor, ptype = InertialSensor, field = accel, card = opt),
    site(parent = PyInertialSensor, ptype = InertialSensor, field = gyro, card = opt),
)]
struct SensorParamsDom {
    #[pydom(nested = PyMeasuredValue)]
    range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    resolution: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    odr: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    bandwidth: Option<()>,
    #[pydom(nested = PyFifoConfig)]
    fifo: Option<()>,
    #[pydom(nested = PyNoise)]
    noise: Option<()>,
    #[pydom(nested = PyAxisNoise)]
    axis_noise: Option<()>,
}

#[pydom(py = PySoftware, target = Software, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = software, card = opt),
)]
struct SoftwareDom {
    name: Option<String>,
    version: Option<String>,
    hash: Option<String>,
    firmware_manifest_uri: Option<String>,
    params: Option<String>,
}

#[pydom(py = PySolarSource, target = SolarSource, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPowerSource, ptype = PowerSource, field = solar, card = opt),
)]
struct SolarSourceDom {
    cell_type: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    peak_power: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    area: Option<()>,
    efficiency: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    voltage_mpp: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    current_mpp: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    open_circuit_voltage: Option<()>,
    mppt: Option<String>,
}

#[pydom(py = PySphere, target = Sphere, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollisionGeometry, ptype = CollisionGeometry, field = sphere, card = opt),
    site(parent = PyGeometry, ptype = Geometry, field = sphere, card = opt),
)]
struct SphereDom {
    radius: Option<String>,
}

#[pydom(py = PyStar, target = Star, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = star, card = vec),
)]
struct StarDom {
    name: String,
    description: Option<String>,
    #[pydom(nested = PyNetworkSelection, clone_nested)]
    selected: Option<()>,
    #[pydom(nested = PyNetworkConfiguration)]
    configuration: Option<()>,
    #[pydom(required_nested = PyParticipantReference)]
    coordinator: (),
    #[pydom(list = PyParticipant, name_field = req, clone_list)]
    participant: Vec<()>,
}

#[pydom(py = PyStreamProfileResource, target = StreamProfileResource, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = stream_profile, card = vec),
)]
struct StreamProfileResourceDom {
    uri: String,
    sha: Option<String>,
    #[pydom(required_scalar)]
    required: bool,
    #[pydom(mapped_enum)]
    selection_role: Option<()>,
}

#[pydom(py = PySubmesh, target = Submesh, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMesh, ptype = Mesh, field = submesh, card = opt),
)]
struct SubmeshDom {
    name: Option<String>,
    center: Option<String>,
}

#[pydom(py = PySupercapacitorSource, target = SupercapacitorSource, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPowerSource, ptype = PowerSource, field = supercapacitor, card = opt),
)]
struct SupercapacitorSourceDom {
    #[pydom(nested = PyMeasuredValue)]
    capacitance: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_voltage: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    esr: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_current: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    peak_current: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    energy: Option<()>,
    cells_series: Option<String>,
    cycle_life: Option<String>,
}

#[pydom(py = PySurface, target = Surface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyCollision, ptype = Collision, field = surface, card = opt),
)]
struct SurfaceDom {
    #[pydom(nested = PyFriction)]
    friction: Option<()>,
    restitution: Option<String>,
    #[pydom(nested = PyContact)]
    contact: Option<()>,
}

#[pydom(py = PySwingLimit, target = SwingLimit, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = swing_limit, card = opt),
)]
struct SwingLimitDom {
    swing1: Option<String>,
    swing2: Option<String>,
    effort: Option<String>,
    velocity: Option<String>,
}

#[pydom(py = PyTankSource, target = TankSource, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPowerSource, ptype = PowerSource, field = tank, card = opt),
)]
struct TankSourceDom {
    fuel: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    volume: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    pressure: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    energy: Option<()>,
    #[pydom(nested = PyFlowCharacterization)]
    flow: Option<()>,
}

#[pydom(py = PyTermination, target = Termination, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyAssembly, ptype = Assembly, field = termination, card = vec),
    site(parent = PyComp, ptype = Comp, field = termination, card = vec),
)]
struct TerminationDom {
    name: String,
    kind: String,
    #[pydom(mapped_enum)]
    mounting: (),
    #[pydom(mapped_enum)]
    fidelity: (),
    profile: Option<String>,
    #[pydom(list = PyPhysicalEndpointRef, name_field = none, clone_list)]
    attachment: Vec<()>,
    #[pydom(list = PyNamedQuantity, name_field = none, clone_list)]
    quantity: Vec<()>,
    #[pydom(nested = PyRepresentation, clone_nested)]
    representation: Option<()>,
}

#[pydom(py = PyTopologySegmentRef, target = TopologySegmentRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyMacsecOverride, ptype = MacsecOverride, field = target, card = req),
)]
struct TopologySegmentRefDom {
    #[pydom(choice = PyTopologySegmentChoice)]
    segment: (),
}

#[pydom(py = PyTrackSurface, target = TrackSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDynamicSurface, ptype = DynamicSurface, field = track, card = opt),
)]
struct TrackSurfaceDom {
    #[pydom(nested = PyMeasuredValue)]
    width: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    length: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    ground_pressure: Option<()>,
    #[pydom(nested = PyFriction)]
    friction: Option<()>,
}

#[pydom(py = PyTrafficClass, target = TrafficClass, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyNetworkConfiguration, ptype = NetworkConfiguration, field = traffic_class, card = vec),
)]
struct TrafficClassDom {
    name: String,
    #[pydom(required_scalar)]
    number: u8,
    #[pydom(mapped_enum)]
    preemption: Option<()>,
    description: Option<String>,
    #[pydom(list = PyPcpValue, name_field = none, clone_list)]
    pcp: Vec<()>,
}

#[pydom(py = PyTrafficClassRef, target = TrafficClassRef, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyOpenTrafficClasses, ptype = OpenTrafficClasses, field = traffic_class, card = vec),
)]
struct TrafficClassRefDom {
    network: String,
    traffic_class: String,
    #[pydom(nested = PyInstanceRef)]
    instance: Option<()>,
}

#[pydom(py = PyTransmission, target = Transmission, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = transmission, card = vec),
)]
struct TransmissionDom {
    name: Option<String>,
    #[pydom(mapped_enum)]
    type_: Option<()>,
    encoder: Option<String>,
    visual: Option<String>,
    mesh: Option<String>,
    #[pydom(list = PyEndpoint, name_field = none)]
    motor: Vec<()>,
    #[pydom(list = PyEndpoint, name_field = none)]
    joint: Vec<()>,
    reduction: Option<String>,
    efficiency: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    backlash: Option<()>,
    #[pydom(list = PyTransmissionSpring, name_field = none)]
    spring: Vec<()>,
}

#[pydom(py = PyTransmissionSpring, target = TransmissionSpring, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyTransmission, ptype = Transmission, field = spring, card = vec),
)]
struct TransmissionSpringDom {
    placement: Option<String>,
    type_: Option<String>,
    torque_sensing: Option<String>,
    clutch: Option<String>,
    equilibrium_actuator: Option<String>,
    stiffness_actuator: Option<String>,
    #[pydom(nested = PyRangeValue)]
    stiffness: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    max_deflection: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    damping: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    equilibrium: Option<()>,
    #[pydom(nested = PyRangeValue)]
    equilibrium_range: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    clutch_power: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    engage_time: Option<()>,
}

#[pydom(py = PyTree, target = Tree, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHcdf, ptype = Hcdf, field = tree, card = vec),
)]
struct TreeDom {
    name: String,
    description: Option<String>,
    #[pydom(nested = PyNetworkSelection, clone_nested)]
    selected: Option<()>,
    #[pydom(nested = PyNetworkConfiguration)]
    configuration: Option<()>,
    #[pydom(required_nested = PyHopReference)]
    root: (),
    #[pydom(list = PyParticipant, name_field = req, clone_list)]
    participant: Vec<()>,
    #[pydom(list = PyHop, name_field = req, clone_list)]
    hop: Vec<()>,
    #[pydom(list = PyLeg, name_field = req, clone_list)]
    leg: Vec<()>,
}

#[pydom(py = PyUrdfCompatComp, target = UrdfCompatComp, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = urdf_compat, card = opt),
)]
struct UrdfCompatCompDom {
    link_type: Option<String>,
}

#[pydom(py = PyUrdfCompatJoint, target = UrdfCompatJoint, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyJoint, ptype = Joint, field = urdf_compat, card = opt),
)]
struct UrdfCompatJointDom {
    #[pydom(nested = PySafetyController)]
    safety_controller: Option<()>,
}

#[pydom(py = PyVisual, target = Visual, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyComp, ptype = Comp, field = visual, card = vec),
)]
struct VisualDom {
    name: String,
    toggle: Option<String>,
    #[pydom(mapped_enum)]
    name_origin: Option<()>,
    #[pydom(nested = PyPose)]
    pose: Option<()>,
    #[pydom(data_enum = PyVisualAppearance)]
    appearance: (),
}

#[pydom(py = PyWheelSurface, target = WheelSurface, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDynamicSurface, ptype = DynamicSurface, field = wheel, card = opt),
)]
struct WheelSurfaceDom {
    type_: Option<String>,
    #[pydom(nested = PyMeasuredValue)]
    radius: Option<()>,
    #[pydom(nested = PyMeasuredValue)]
    width: Option<()>,
    #[pydom(nested = PyFriction)]
    friction: Option<()>,
}

#[pydom(py = PyWorldRouteFrame, target = WorldRouteFrame, owner(target = Hcdf, py = PyHcdf),
    choice_site(parent = PyRouteFrameChoice, ptype = RouteFrameChoice, variant = World),
)]
struct WorldRouteFrameDom {}

fn __draft_functional_endpoint_choice_port() -> PortRef {
    PortRef {
        component: String::new(),
        port: String::new(),
        instance: None,
    }
}
fn __draft_functional_endpoint_choice_channel() -> ChannelRef {
    ChannelRef {
        component: String::new(),
        port: String::new(),
        channel: String::new(),
        instance: None,
    }
}
#[pychoice(py = PyFunctionalEndpointChoice, target = FunctionalEndpointChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyFunctionalEndpointRef, ptype = FunctionalEndpointRef, field = endpoint, card = req),
)]
enum FunctionalEndpointChoiceDom {
    #[pychoice(name = "port-ref", payload = PyPortRef, draft = __draft_functional_endpoint_choice_port)]
    Port(PortRef),
    #[pychoice(name = "channel-ref", payload = PyChannelRef, draft = __draft_functional_endpoint_choice_channel)]
    Channel(ChannelRef),
}

fn __draft_hop_owner_choice_component() -> ComponentRef {
    ComponentRef {
        component: String::new(),
        instance: None,
    }
}
fn __draft_hop_owner_choice_function() -> ConnectivityFunctionRef {
    ConnectivityFunctionRef {
        component: String::new(),
        function: String::new(),
        instance: None,
    }
}
#[pychoice(py = PyHopOwnerChoice, target = HopOwnerChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyHopOwnerRef, ptype = HopOwnerRef, field = owner, card = req),
)]
enum HopOwnerChoiceDom {
    #[pychoice(name = "component-ref", payload = PyComponentRef, draft = __draft_hop_owner_choice_component)]
    Component(ComponentRef),
    #[pychoice(name = "function-ref", payload = PyConnectivityFunctionRef, draft = __draft_hop_owner_choice_function)]
    Function(ConnectivityFunctionRef),
}

fn __draft_model_root_choice_component_visual() -> ComponentVisualRoot {
    ComponentVisualRoot {
        component: String::new(),
        visual: String::new(),
        instance: None,
    }
}
fn __draft_model_root_choice_assembly_model() -> AssemblyModelRoot {
    AssemblyModelRoot {
        assembly: String::new(),
        instance: None,
    }
}
#[pychoice(py = PyModelRootChoice, target = ModelRootChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyModelRoot, ptype = ModelRoot, field = root, card = req),
)]
enum ModelRootChoiceDom {
    #[pychoice(name = "component-visual", payload = PyComponentVisualRoot, draft = __draft_model_root_choice_component_visual)]
    ComponentVisual(ComponentVisualRoot),
    #[pychoice(name = "assembly-model", payload = PyAssemblyModelRoot, draft = __draft_model_root_choice_assembly_model)]
    AssemblyModel(AssemblyModelRoot),
}

fn __draft_physical_endpoint_choice_connector() -> ConnectorRef {
    ConnectorRef {
        connector: String::new(),
        owner: PhysicalOwnerChoice::Component(ComponentRef {
            component: String::new(),
            instance: None,
        }),
    }
}
fn __draft_physical_endpoint_choice_position() -> PositionRef {
    PositionRef {
        connector: String::new(),
        position: String::new(),
        owner: PhysicalOwnerChoice::Component(ComponentRef {
            component: String::new(),
            instance: None,
        }),
    }
}
fn __draft_physical_endpoint_choice_junction() -> JunctionRef {
    JunctionRef {
        junction: String::new(),
        owner: PhysicalOwnerChoice::Component(ComponentRef {
            component: String::new(),
            instance: None,
        }),
    }
}
#[pychoice(py = PyPhysicalEndpointChoice, target = PhysicalEndpointChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPhysicalEndpointRef, ptype = PhysicalEndpointRef, field = endpoint, card = req),
)]
enum PhysicalEndpointChoiceDom {
    #[pychoice(name = "connector-ref", payload = PyConnectorRef, draft = __draft_physical_endpoint_choice_connector)]
    Connector(ConnectorRef),
    #[pychoice(name = "position-ref", payload = PyPositionRef, draft = __draft_physical_endpoint_choice_position)]
    Position(PositionRef),
    #[pychoice(name = "junction-ref", payload = PyJunctionRef, draft = __draft_physical_endpoint_choice_junction)]
    Junction(JunctionRef),
}

fn __draft_physical_owner_choice_component() -> ComponentRef {
    ComponentRef {
        component: String::new(),
        instance: None,
    }
}
fn __draft_physical_owner_choice_assembly() -> AssemblyRef {
    AssemblyRef {
        assembly: String::new(),
        instance: None,
    }
}
#[pychoice(py = PyPhysicalOwnerChoice, target = PhysicalOwnerChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyConnectorRef, ptype = ConnectorRef, field = owner, card = req),
    site(parent = PyJunctionRef, ptype = JunctionRef, field = owner, card = req),
    site(parent = PyPositionRef, ptype = PositionRef, field = owner, card = req),
)]
enum PhysicalOwnerChoiceDom {
    #[pychoice(name = "component-ref", payload = PyComponentRef, draft = __draft_physical_owner_choice_component)]
    Component(ComponentRef),
    #[pychoice(name = "assembly-ref", payload = PyAssemblyRef, draft = __draft_physical_owner_choice_assembly)]
    Assembly(AssemblyRef),
}

fn __draft_placement_rotation_choice_rpy() -> RpyRotation {
    RpyRotation {
        value: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
    }
}
fn __draft_placement_rotation_choice_quaternion() -> QuaternionRotation {
    QuaternionRotation {
        value: std::array::from_fn::<_, 4usize, _>(|_| 0.0),
    }
}
#[pychoice(py = PyPlacementRotationChoice, target = PlacementRotationChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyPlacementRotation, ptype = PlacementRotation, field = rotation, card = req),
)]
enum PlacementRotationChoiceDom {
    #[pychoice(name = "rpy", payload = PyRpyRotation, draft = __draft_placement_rotation_choice_rpy)]
    Rpy(RpyRotation),
    #[pychoice(name = "quaternion", payload = PyQuaternionRotation, draft = __draft_placement_rotation_choice_quaternion)]
    Quaternion(QuaternionRotation),
}

fn __draft_representation_choice_box() -> PrimitiveBox {
    PrimitiveBox {
        size: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
        placement: Placement {
            xyz: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
            frame: RouteFrame {
                frame: RouteFrameChoice::World(<WorldRouteFrame as Default>::default()),
            },
            rotation: PlacementRotation {
                rotation: PlacementRotationChoice::Rpy(RpyRotation {
                    value: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
                }),
            },
        },
    }
}
fn __draft_representation_choice_cylinder() -> PrimitiveCylinder {
    PrimitiveCylinder {
        radius: 0.0,
        length: 0.0,
        placement: Placement {
            xyz: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
            frame: RouteFrame {
                frame: RouteFrameChoice::World(<WorldRouteFrame as Default>::default()),
            },
            rotation: PlacementRotation {
                rotation: PlacementRotationChoice::Rpy(RpyRotation {
                    value: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
                }),
            },
        },
    }
}
fn __draft_representation_choice_sphere() -> PrimitiveSphere {
    PrimitiveSphere {
        radius: 0.0,
        placement: Placement {
            xyz: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
            frame: RouteFrame {
                frame: RouteFrameChoice::World(<WorldRouteFrame as Default>::default()),
            },
            rotation: PlacementRotation {
                rotation: PlacementRotationChoice::Rpy(RpyRotation {
                    value: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
                }),
            },
        },
    }
}
fn __draft_representation_choice_model() -> ModelRepresentation {
    ModelRepresentation {
        uri: String::new(),
        sha: None,
        node_path: None,
        placement: Placement {
            xyz: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
            frame: RouteFrame {
                frame: RouteFrameChoice::World(<WorldRouteFrame as Default>::default()),
            },
            rotation: PlacementRotation {
                rotation: PlacementRotationChoice::Rpy(RpyRotation {
                    value: std::array::from_fn::<_, 3usize, _>(|_| 0.0),
                }),
            },
        },
    }
}
fn __draft_representation_choice_model_part() -> ModelPartRepresentation {
    ModelPartRepresentation {
        node_path: String::new(),
        submesh_fallback: None,
        model_root: ModelRoot {
            root: ModelRootChoice::ComponentVisual(ComponentVisualRoot {
                component: String::new(),
                visual: String::new(),
                instance: None,
            }),
        },
    }
}
fn __draft_representation_choice_derived_route() -> DerivedRouteRepresentation {
    <DerivedRouteRepresentation as Default>::default()
}
#[pychoice(py = PyRepresentationChoice, target = RepresentationChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyRepresentation, ptype = Representation, field = variant, card = req),
)]
enum RepresentationChoiceDom {
    #[pychoice(name = "box", payload = PyPrimitiveBox, draft = __draft_representation_choice_box)]
    Box(PrimitiveBox),
    #[pychoice(name = "cylinder", payload = PyPrimitiveCylinder, draft = __draft_representation_choice_cylinder)]
    Cylinder(PrimitiveCylinder),
    #[pychoice(name = "sphere", payload = PyPrimitiveSphere, draft = __draft_representation_choice_sphere)]
    Sphere(PrimitiveSphere),
    #[pychoice(name = "model", payload = PyModelRepresentation, draft = __draft_representation_choice_model)]
    Model(ModelRepresentation),
    #[pychoice(name = "model-part", payload = PyModelPartRepresentation, draft = __draft_representation_choice_model_part)]
    ModelPart(ModelPartRepresentation),
    #[pychoice(name = "derived-route", payload = PyDerivedRouteRepresentation, draft = __draft_representation_choice_derived_route)]
    DerivedRoute(DerivedRouteRepresentation),
}

fn __draft_rf_channel_selection_choice_numbered() -> RfNumberedChannel {
    RfNumberedChannel {
        number: 0,
        center_frequency: None,
        bandwidth: None,
    }
}
fn __draft_rf_channel_selection_choice_frequency_defined() -> RfFrequencyDefinedChannel {
    RfFrequencyDefinedChannel {
        center_frequency: SelectionQuantity {
            selection: SelectionQuantityChoice::Nominal(Quantity {
                value: 0.0,
                unit: String::new(),
            }),
        },
        bandwidth: None,
    }
}
#[pychoice(py = PyRfChannelSelectionChoice, target = RfChannelSelectionChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyRfChannelSelection, ptype = RfChannelSelection, field = selection, card = req),
)]
enum RfChannelSelectionChoiceDom {
    #[pychoice(name = "numbered", payload = PyRfNumberedChannel, draft = __draft_rf_channel_selection_choice_numbered)]
    Numbered(RfNumberedChannel),
    #[pychoice(name = "frequency-defined", payload = PyRfFrequencyDefinedChannel, draft = __draft_rf_channel_selection_choice_frequency_defined)]
    FrequencyDefined(RfFrequencyDefinedChannel),
}

fn __draft_route_frame_choice_world() -> WorldRouteFrame {
    <WorldRouteFrame as Default>::default()
}
fn __draft_route_frame_choice_component_origin() -> ComponentOriginFrame {
    ComponentOriginFrame {
        component: String::new(),
        instance: None,
    }
}
fn __draft_route_frame_choice_component_frame() -> ComponentNamedFrame {
    ComponentNamedFrame {
        component: String::new(),
        frame: String::new(),
        instance: None,
    }
}
#[pychoice(py = PyRouteFrameChoice, target = RouteFrameChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyRouteFrame, ptype = RouteFrame, field = frame, card = req),
)]
enum RouteFrameChoiceDom {
    #[pychoice(name = "world", payload = PyWorldRouteFrame, draft = __draft_route_frame_choice_world)]
    World(WorldRouteFrame),
    #[pychoice(name = "component-origin", payload = PyComponentOriginFrame, draft = __draft_route_frame_choice_component_origin)]
    ComponentOrigin(ComponentOriginFrame),
    #[pychoice(name = "component-frame", payload = PyComponentNamedFrame, draft = __draft_route_frame_choice_component_frame)]
    ComponentFrame(ComponentNamedFrame),
}

fn __draft_route_section_choice_round() -> RoundRouteSection {
    RoundRouteSection { diameter: 0.0 }
}
fn __draft_route_section_choice_rectangular() -> RectangularRouteSection {
    RectangularRouteSection {
        width: 0.0,
        height: 0.0,
    }
}
#[pychoice(py = PyRouteSectionChoice, target = RouteSectionChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyDerivedRouteRepresentation, ptype = DerivedRouteRepresentation, field = section, card = opt),
)]
enum RouteSectionChoiceDom {
    #[pychoice(name = "round-section", payload = PyRoundRouteSection, draft = __draft_route_section_choice_round)]
    Round(RoundRouteSection),
    #[pychoice(name = "rectangular-section", payload = PyRectangularRouteSection, draft = __draft_route_section_choice_rectangular)]
    Rectangular(RectangularRouteSection),
}

fn __draft_selection_quantity_choice_nominal() -> Quantity {
    Quantity {
        value: 0.0,
        unit: String::new(),
    }
}
fn __draft_selection_quantity_choice_range() -> SelectionRange {
    SelectionRange {
        minimum: 0.0,
        maximum: 0.0,
        nominal: None,
        unit: String::new(),
    }
}
#[pychoice(py = PySelectionQuantityChoice, target = SelectionQuantityChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PySelectionQuantity, ptype = SelectionQuantity, field = selection, card = req),
)]
enum SelectionQuantityChoiceDom {
    #[pychoice(name = "nominal", payload = PyQuantity, draft = __draft_selection_quantity_choice_nominal)]
    Nominal(Quantity),
    #[pychoice(name = "range", payload = PySelectionRange, draft = __draft_selection_quantity_choice_range)]
    Range(SelectionRange),
}

fn __draft_topology_segment_choice_network() -> NetworkRef {
    NetworkRef {
        network: String::new(),
        instance: None,
    }
}
fn __draft_topology_segment_choice_leg() -> LegRef {
    LegRef {
        network: String::new(),
        leg: String::new(),
        instance: None,
    }
}
#[pychoice(py = PyTopologySegmentChoice, target = TopologySegmentChoice, owner(target = Hcdf, py = PyHcdf),
    site(parent = PyTopologySegmentRef, ptype = TopologySegmentRef, field = segment, card = req),
)]
enum TopologySegmentChoiceDom {
    #[pychoice(name = "network-ref", payload = PyNetworkRef, draft = __draft_topology_segment_choice_network)]
    Network(NetworkRef),
    #[pychoice(name = "leg-ref", payload = PyLegRef, draft = __draft_topology_segment_choice_leg)]
    Leg(LegRef),
}

/// Register every generated DOM pyclass on the `hcdf` extension module.
pub fn register_generated(m: &pyo3::Bound<'_, pyo3::types::PyModule>) -> pyo3::PyResult<()> {
    __register_py_aerofoil_surface(m)?;
    __register_py_antenna(m)?;
    __register_py_assembly(m)?;
    __register_py_assembly_model_root(m)?;
    __register_py_assembly_ref(m)?;
    __register_py_axis(m)?;
    __register_py_axis_align(m)?;
    __register_py_axis_noise(m)?;
    __register_py_battery_source(m)?;
    __register_py_binding(m)?;
    __register_py_box(m)?;
    __register_py_bus(m)?;
    __register_py_camera_binning(m)?;
    __register_py_camera_calibration(m)?;
    __register_py_camera_distortion(m)?;
    __register_py_camera_intrinsics(m)?;
    __register_py_camera_lens(m)?;
    __register_py_camera_lens_custom_function(m)?;
    __register_py_camera_matrix(m)?;
    __register_py_camera_params(m)?;
    __register_py_camera_projection(m)?;
    __register_py_camera_roi(m)?;
    __register_py_capabilities(m)?;
    __register_py_capsule(m)?;
    __register_py_carrier_capability(m)?;
    __register_py_chain(m)?;
    __register_py_channel(m)?;
    __register_py_channel_ref(m)?;
    __register_py_collision(m)?;
    __register_py_collision_geometry(m)?;
    __register_py_collision_pair(m)?;
    __register_py_color(m)?;
    __register_py_comp(m)?;
    __register_py_component_named_frame(m)?;
    __register_py_component_origin_frame(m)?;
    __register_py_component_ref(m)?;
    __register_py_component_visual_root(m)?;
    __register_py_cone(m)?;
    __register_py_connectivity_function_ref(m)?;
    __register_py_connectivity_mesh(m)?;
    __register_py_connector(m)?;
    __register_py_connector_ref(m)?;
    __register_py_contact(m)?;
    __register_py_control_modes(m)?;
    __register_py_control_surface(m)?;
    __register_py_cylinder(m)?;
    __register_py_data_output(m)?;
    __register_py_derived_route_representation(m)?;
    __register_py_discovered(m)?;
    __register_py_dynamic_surface(m)?;
    __register_py_eee_configuration(m)?;
    __register_py_eee_override(m)?;
    __register_py_ellipsoid(m)?;
    __register_py_endpoint(m)?;
    __register_py_extension(m)?;
    __register_py_fifo_config(m)?;
    __register_py_flow_characterization(m)?;
    __register_py_fluid_probe(m)?;
    __register_py_fluid_sensor(m)?;
    __register_py_force_axis_noise(m)?;
    __register_py_frame(m)?;
    __register_py_friction(m)?;
    __register_py_friction_direction(m)?;
    __register_py_frustum(m)?;
    __register_py_fuel_cell(m)?;
    __register_py_function(m)?;
    __register_py_functional_endpoint_ref(m)?;
    __register_py_gate_control_entry(m)?;
    __register_py_gate_schedule(m)?;
    __register_py_geometry(m)?;
    __register_py_gnss_params(m)?;
    __register_py_gptp_clock(m)?;
    __register_py_gptp_domain(m)?;
    __register_py_gptp_port_defaults(m)?;
    __register_py_gripper_surface(m)?;
    __register_py_hcdf(m)?;
    __register_py_hmi_element(m)?;
    __register_py_hop(m)?;
    __register_py_hop_owner_ref(m)?;
    __register_py_hop_ref(m)?;
    __register_py_hop_reference(m)?;
    __register_py_hydrofoil_surface(m)?;
    __register_py_include(m)?;
    __register_py_inertial(m)?;
    __register_py_inertial_sensor(m)?;
    __register_py_instance_ref(m)?;
    __register_py_instance_segment(m)?;
    __register_py_joint(m)?;
    __register_py_joint_calibration(m)?;
    __register_py_joint_dynamics(m)?;
    __register_py_joint_endpoint(m)?;
    __register_py_joint_group(m)?;
    __register_py_joint_limit(m)?;
    __register_py_joint_position(m)?;
    __register_py_junction(m)?;
    __register_py_junction_ref(m)?;
    __register_py_kinematic_state(m)?;
    __register_py_led_illumination(m)?;
    __register_py_leg(m)?;
    __register_py_leg_end(m)?;
    __register_py_leg_ref(m)?;
    __register_py_lidar_params(m)?;
    __register_py_lidar_range(m)?;
    __register_py_lidar_scan_axis(m)?;
    __register_py_lidar_scan_pattern(m)?;
    __register_py_light_attenuation(m)?;
    __register_py_link(m)?;
    __register_py_loop_closure(m)?;
    __register_py_macsec_configuration(m)?;
    __register_py_macsec_default_policy(m)?;
    __register_py_macsec_override(m)?;
    __register_py_macsec_policy_definition(m)?;
    __register_py_macsec_policy_ref(m)?;
    __register_py_mate(m)?;
    __register_py_measured_value(m)?;
    __register_py_mesh(m)?;
    __register_py_mimic(m)?;
    __register_py_model_part_representation(m)?;
    __register_py_model_representation(m)?;
    __register_py_model_root(m)?;
    __register_py_motor(m)?;
    __register_py_named_quantity(m)?;
    __register_py_network_configuration(m)?;
    __register_py_network_ref(m)?;
    __register_py_network_selection(m)?;
    __register_py_noise(m)?;
    __register_py_open_traffic_classes(m)?;
    __register_py_optical_sensor(m)?;
    __register_py_participant(m)?;
    __register_py_participant_ref(m)?;
    __register_py_participant_reference(m)?;
    __register_py_path(m)?;
    __register_py_pcp_value(m)?;
    __register_py_physical_endpoint_ref(m)?;
    __register_py_placement(m)?;
    __register_py_placement_rotation(m)?;
    __register_py_plca_configuration(m)?;
    __register_py_plca_node(m)?;
    __register_py_port(m)?;
    __register_py_port_ref(m)?;
    __register_py_pose(m)?;
    __register_py_position(m)?;
    __register_py_position_mapping(m)?;
    __register_py_position_ref(m)?;
    __register_py_power_source(m)?;
    __register_py_primitive_box(m)?;
    __register_py_primitive_cylinder(m)?;
    __register_py_primitive_sphere(m)?;
    __register_py_profile_capability(m)?;
    __register_py_prop_surface(m)?;
    __register_py_purpose_capability(m)?;
    __register_py_quantity(m)?;
    __register_py_quantity_range(m)?;
    __register_py_quaternion_rotation(m)?;
    __register_py_radar_params(m)?;
    __register_py_range_value(m)?;
    __register_py_rated_value(m)?;
    __register_py_rectangular_route_section(m)?;
    __register_py_ref(m)?;
    __register_py_representation(m)?;
    __register_py_rf_channel_selection(m)?;
    __register_py_rf_frequency_defined_channel(m)?;
    __register_py_rf_numbered_channel(m)?;
    __register_py_rf_selection(m)?;
    __register_py_ring(m)?;
    __register_py_round_route_section(m)?;
    __register_py_route_frame(m)?;
    __register_py_route_point(m)?;
    __register_py_rpy_rotation(m)?;
    __register_py_safety_controller(m)?;
    __register_py_schedule_assignment(m)?;
    __register_py_schedule_ref(m)?;
    __register_py_schedule_target(m)?;
    __register_py_selected_profile(m)?;
    __register_py_selection_quantity(m)?;
    __register_py_selection_range(m)?;
    __register_py_self_collision_disable(m)?;
    __register_py_sensor(m)?;
    __register_py_sensor_category(m)?;
    __register_py_sensor_driver(m)?;
    __register_py_sensor_fov(m)?;
    __register_py_sensor_params(m)?;
    __register_py_software(m)?;
    __register_py_solar_source(m)?;
    __register_py_sphere(m)?;
    __register_py_star(m)?;
    __register_py_stream_profile_resource(m)?;
    __register_py_submesh(m)?;
    __register_py_supercapacitor_source(m)?;
    __register_py_surface(m)?;
    __register_py_swing_limit(m)?;
    __register_py_tank_source(m)?;
    __register_py_termination(m)?;
    __register_py_topology_segment_ref(m)?;
    __register_py_track_surface(m)?;
    __register_py_traffic_class(m)?;
    __register_py_traffic_class_ref(m)?;
    __register_py_transmission(m)?;
    __register_py_transmission_spring(m)?;
    __register_py_tree(m)?;
    __register_py_urdf_compat_comp(m)?;
    __register_py_urdf_compat_joint(m)?;
    __register_py_visual(m)?;
    __register_py_wheel_surface(m)?;
    __register_py_world_route_frame(m)?;
    __register_py_functional_endpoint_choice(m)?;
    __register_py_hop_owner_choice(m)?;
    __register_py_model_root_choice(m)?;
    __register_py_physical_endpoint_choice(m)?;
    __register_py_physical_owner_choice(m)?;
    __register_py_placement_rotation_choice(m)?;
    __register_py_representation_choice(m)?;
    __register_py_rf_channel_selection_choice(m)?;
    __register_py_route_frame_choice(m)?;
    __register_py_route_section_choice(m)?;
    __register_py_selection_quantity_choice(m)?;
    __register_py_topology_segment_choice(m)?;
    Ok(())
}

/// The concatenated `.pyi` stub for every generated handle + its list sub-handles.
pub fn pyi_generated() -> String {
    let mut s = String::new();
    s.push_str(__pyi_py_aerofoil_surface());
    s.push('\n');
    s.push_str(__pyi_py_antenna());
    s.push('\n');
    s.push_str(__pyi_py_assembly());
    s.push('\n');
    s.push_str(__pyi_py_assembly_model_root());
    s.push('\n');
    s.push_str(__pyi_py_assembly_ref());
    s.push('\n');
    s.push_str(__pyi_py_axis());
    s.push('\n');
    s.push_str(__pyi_py_axis_align());
    s.push('\n');
    s.push_str(__pyi_py_axis_noise());
    s.push('\n');
    s.push_str(__pyi_py_battery_source());
    s.push('\n');
    s.push_str(__pyi_py_binding());
    s.push('\n');
    s.push_str(__pyi_py_box());
    s.push('\n');
    s.push_str(__pyi_py_bus());
    s.push('\n');
    s.push_str(__pyi_py_camera_binning());
    s.push('\n');
    s.push_str(__pyi_py_camera_calibration());
    s.push('\n');
    s.push_str(__pyi_py_camera_distortion());
    s.push('\n');
    s.push_str(__pyi_py_camera_intrinsics());
    s.push('\n');
    s.push_str(__pyi_py_camera_lens());
    s.push('\n');
    s.push_str(__pyi_py_camera_lens_custom_function());
    s.push('\n');
    s.push_str(__pyi_py_camera_matrix());
    s.push('\n');
    s.push_str(__pyi_py_camera_params());
    s.push('\n');
    s.push_str(__pyi_py_camera_projection());
    s.push('\n');
    s.push_str(__pyi_py_camera_roi());
    s.push('\n');
    s.push_str(__pyi_py_capabilities());
    s.push('\n');
    s.push_str(__pyi_py_capsule());
    s.push('\n');
    s.push_str(__pyi_py_carrier_capability());
    s.push('\n');
    s.push_str(__pyi_py_chain());
    s.push('\n');
    s.push_str(__pyi_py_channel());
    s.push('\n');
    s.push_str(__pyi_py_channel_ref());
    s.push('\n');
    s.push_str(__pyi_py_collision());
    s.push('\n');
    s.push_str(__pyi_py_collision_geometry());
    s.push('\n');
    s.push_str(__pyi_py_collision_pair());
    s.push('\n');
    s.push_str(__pyi_py_color());
    s.push('\n');
    s.push_str(__pyi_py_comp());
    s.push('\n');
    s.push_str(__pyi_py_component_named_frame());
    s.push('\n');
    s.push_str(__pyi_py_component_origin_frame());
    s.push('\n');
    s.push_str(__pyi_py_component_ref());
    s.push('\n');
    s.push_str(__pyi_py_component_visual_root());
    s.push('\n');
    s.push_str(__pyi_py_cone());
    s.push('\n');
    s.push_str(__pyi_py_connectivity_function_ref());
    s.push('\n');
    s.push_str(__pyi_py_connectivity_mesh());
    s.push('\n');
    s.push_str(__pyi_py_connector());
    s.push('\n');
    s.push_str(__pyi_py_connector_ref());
    s.push('\n');
    s.push_str(__pyi_py_contact());
    s.push('\n');
    s.push_str(__pyi_py_control_modes());
    s.push('\n');
    s.push_str(__pyi_py_control_surface());
    s.push('\n');
    s.push_str(__pyi_py_cylinder());
    s.push('\n');
    s.push_str(__pyi_py_data_output());
    s.push('\n');
    s.push_str(__pyi_py_derived_route_representation());
    s.push('\n');
    s.push_str(__pyi_py_discovered());
    s.push('\n');
    s.push_str(__pyi_py_dynamic_surface());
    s.push('\n');
    s.push_str(__pyi_py_eee_configuration());
    s.push('\n');
    s.push_str(__pyi_py_eee_override());
    s.push('\n');
    s.push_str(__pyi_py_ellipsoid());
    s.push('\n');
    s.push_str(__pyi_py_endpoint());
    s.push('\n');
    s.push_str(__pyi_py_extension());
    s.push('\n');
    s.push_str(__pyi_py_fifo_config());
    s.push('\n');
    s.push_str(__pyi_py_flow_characterization());
    s.push('\n');
    s.push_str(__pyi_py_fluid_probe());
    s.push('\n');
    s.push_str(__pyi_py_fluid_sensor());
    s.push('\n');
    s.push_str(__pyi_py_force_axis_noise());
    s.push('\n');
    s.push_str(__pyi_py_frame());
    s.push('\n');
    s.push_str(__pyi_py_friction());
    s.push('\n');
    s.push_str(__pyi_py_friction_direction());
    s.push('\n');
    s.push_str(__pyi_py_frustum());
    s.push('\n');
    s.push_str(__pyi_py_fuel_cell());
    s.push('\n');
    s.push_str(__pyi_py_function());
    s.push('\n');
    s.push_str(__pyi_py_functional_endpoint_ref());
    s.push('\n');
    s.push_str(__pyi_py_gate_control_entry());
    s.push('\n');
    s.push_str(__pyi_py_gate_schedule());
    s.push('\n');
    s.push_str(__pyi_py_geometry());
    s.push('\n');
    s.push_str(__pyi_py_gnss_params());
    s.push('\n');
    s.push_str(__pyi_py_gptp_clock());
    s.push('\n');
    s.push_str(__pyi_py_gptp_domain());
    s.push('\n');
    s.push_str(__pyi_py_gptp_port_defaults());
    s.push('\n');
    s.push_str(__pyi_py_gripper_surface());
    s.push('\n');
    s.push_str(__pyi_py_hcdf());
    s.push('\n');
    s.push_str(__pyi_py_hmi_element());
    s.push('\n');
    s.push_str(__pyi_py_hop());
    s.push('\n');
    s.push_str(__pyi_py_hop_owner_ref());
    s.push('\n');
    s.push_str(__pyi_py_hop_ref());
    s.push('\n');
    s.push_str(__pyi_py_hop_reference());
    s.push('\n');
    s.push_str(__pyi_py_hydrofoil_surface());
    s.push('\n');
    s.push_str(__pyi_py_include());
    s.push('\n');
    s.push_str(__pyi_py_inertial());
    s.push('\n');
    s.push_str(__pyi_py_inertial_sensor());
    s.push('\n');
    s.push_str(__pyi_py_instance_ref());
    s.push('\n');
    s.push_str(__pyi_py_instance_segment());
    s.push('\n');
    s.push_str(__pyi_py_joint());
    s.push('\n');
    s.push_str(__pyi_py_joint_calibration());
    s.push('\n');
    s.push_str(__pyi_py_joint_dynamics());
    s.push('\n');
    s.push_str(__pyi_py_joint_endpoint());
    s.push('\n');
    s.push_str(__pyi_py_joint_group());
    s.push('\n');
    s.push_str(__pyi_py_joint_limit());
    s.push('\n');
    s.push_str(__pyi_py_joint_position());
    s.push('\n');
    s.push_str(__pyi_py_junction());
    s.push('\n');
    s.push_str(__pyi_py_junction_ref());
    s.push('\n');
    s.push_str(__pyi_py_kinematic_state());
    s.push('\n');
    s.push_str(__pyi_py_led_illumination());
    s.push('\n');
    s.push_str(__pyi_py_leg());
    s.push('\n');
    s.push_str(__pyi_py_leg_end());
    s.push('\n');
    s.push_str(__pyi_py_leg_ref());
    s.push('\n');
    s.push_str(__pyi_py_lidar_params());
    s.push('\n');
    s.push_str(__pyi_py_lidar_range());
    s.push('\n');
    s.push_str(__pyi_py_lidar_scan_axis());
    s.push('\n');
    s.push_str(__pyi_py_lidar_scan_pattern());
    s.push('\n');
    s.push_str(__pyi_py_light_attenuation());
    s.push('\n');
    s.push_str(__pyi_py_link());
    s.push('\n');
    s.push_str(__pyi_py_loop_closure());
    s.push('\n');
    s.push_str(__pyi_py_macsec_configuration());
    s.push('\n');
    s.push_str(__pyi_py_macsec_default_policy());
    s.push('\n');
    s.push_str(__pyi_py_macsec_override());
    s.push('\n');
    s.push_str(__pyi_py_macsec_policy_definition());
    s.push('\n');
    s.push_str(__pyi_py_macsec_policy_ref());
    s.push('\n');
    s.push_str(__pyi_py_mate());
    s.push('\n');
    s.push_str(__pyi_py_measured_value());
    s.push('\n');
    s.push_str(__pyi_py_mesh());
    s.push('\n');
    s.push_str(__pyi_py_mimic());
    s.push('\n');
    s.push_str(__pyi_py_model_part_representation());
    s.push('\n');
    s.push_str(__pyi_py_model_representation());
    s.push('\n');
    s.push_str(__pyi_py_model_root());
    s.push('\n');
    s.push_str(__pyi_py_motor());
    s.push('\n');
    s.push_str(__pyi_py_named_quantity());
    s.push('\n');
    s.push_str(__pyi_py_network_configuration());
    s.push('\n');
    s.push_str(__pyi_py_network_ref());
    s.push('\n');
    s.push_str(__pyi_py_network_selection());
    s.push('\n');
    s.push_str(__pyi_py_noise());
    s.push('\n');
    s.push_str(__pyi_py_open_traffic_classes());
    s.push('\n');
    s.push_str(__pyi_py_optical_sensor());
    s.push('\n');
    s.push_str(__pyi_py_participant());
    s.push('\n');
    s.push_str(__pyi_py_participant_ref());
    s.push('\n');
    s.push_str(__pyi_py_participant_reference());
    s.push('\n');
    s.push_str(__pyi_py_path());
    s.push('\n');
    s.push_str(__pyi_py_pcp_value());
    s.push('\n');
    s.push_str(__pyi_py_physical_endpoint_ref());
    s.push('\n');
    s.push_str(__pyi_py_placement());
    s.push('\n');
    s.push_str(__pyi_py_placement_rotation());
    s.push('\n');
    s.push_str(__pyi_py_plca_configuration());
    s.push('\n');
    s.push_str(__pyi_py_plca_node());
    s.push('\n');
    s.push_str(__pyi_py_port());
    s.push('\n');
    s.push_str(__pyi_py_port_ref());
    s.push('\n');
    s.push_str(__pyi_py_pose());
    s.push('\n');
    s.push_str(__pyi_py_position());
    s.push('\n');
    s.push_str(__pyi_py_position_mapping());
    s.push('\n');
    s.push_str(__pyi_py_position_ref());
    s.push('\n');
    s.push_str(__pyi_py_power_source());
    s.push('\n');
    s.push_str(__pyi_py_primitive_box());
    s.push('\n');
    s.push_str(__pyi_py_primitive_cylinder());
    s.push('\n');
    s.push_str(__pyi_py_primitive_sphere());
    s.push('\n');
    s.push_str(__pyi_py_profile_capability());
    s.push('\n');
    s.push_str(__pyi_py_prop_surface());
    s.push('\n');
    s.push_str(__pyi_py_purpose_capability());
    s.push('\n');
    s.push_str(__pyi_py_quantity());
    s.push('\n');
    s.push_str(__pyi_py_quantity_range());
    s.push('\n');
    s.push_str(__pyi_py_quaternion_rotation());
    s.push('\n');
    s.push_str(__pyi_py_radar_params());
    s.push('\n');
    s.push_str(__pyi_py_range_value());
    s.push('\n');
    s.push_str(__pyi_py_rated_value());
    s.push('\n');
    s.push_str(__pyi_py_rectangular_route_section());
    s.push('\n');
    s.push_str(__pyi_py_ref());
    s.push('\n');
    s.push_str(__pyi_py_representation());
    s.push('\n');
    s.push_str(__pyi_py_rf_channel_selection());
    s.push('\n');
    s.push_str(__pyi_py_rf_frequency_defined_channel());
    s.push('\n');
    s.push_str(__pyi_py_rf_numbered_channel());
    s.push('\n');
    s.push_str(__pyi_py_rf_selection());
    s.push('\n');
    s.push_str(__pyi_py_ring());
    s.push('\n');
    s.push_str(__pyi_py_round_route_section());
    s.push('\n');
    s.push_str(__pyi_py_route_frame());
    s.push('\n');
    s.push_str(__pyi_py_route_point());
    s.push('\n');
    s.push_str(__pyi_py_rpy_rotation());
    s.push('\n');
    s.push_str(__pyi_py_safety_controller());
    s.push('\n');
    s.push_str(__pyi_py_schedule_assignment());
    s.push('\n');
    s.push_str(__pyi_py_schedule_ref());
    s.push('\n');
    s.push_str(__pyi_py_schedule_target());
    s.push('\n');
    s.push_str(__pyi_py_selected_profile());
    s.push('\n');
    s.push_str(__pyi_py_selection_quantity());
    s.push('\n');
    s.push_str(__pyi_py_selection_range());
    s.push('\n');
    s.push_str(__pyi_py_self_collision_disable());
    s.push('\n');
    s.push_str(__pyi_py_sensor());
    s.push('\n');
    s.push_str(__pyi_py_sensor_category());
    s.push('\n');
    s.push_str(__pyi_py_sensor_driver());
    s.push('\n');
    s.push_str(__pyi_py_sensor_fov());
    s.push('\n');
    s.push_str(__pyi_py_sensor_params());
    s.push('\n');
    s.push_str(__pyi_py_software());
    s.push('\n');
    s.push_str(__pyi_py_solar_source());
    s.push('\n');
    s.push_str(__pyi_py_sphere());
    s.push('\n');
    s.push_str(__pyi_py_star());
    s.push('\n');
    s.push_str(__pyi_py_stream_profile_resource());
    s.push('\n');
    s.push_str(__pyi_py_submesh());
    s.push('\n');
    s.push_str(__pyi_py_supercapacitor_source());
    s.push('\n');
    s.push_str(__pyi_py_surface());
    s.push('\n');
    s.push_str(__pyi_py_swing_limit());
    s.push('\n');
    s.push_str(__pyi_py_tank_source());
    s.push('\n');
    s.push_str(__pyi_py_termination());
    s.push('\n');
    s.push_str(__pyi_py_topology_segment_ref());
    s.push('\n');
    s.push_str(__pyi_py_track_surface());
    s.push('\n');
    s.push_str(__pyi_py_traffic_class());
    s.push('\n');
    s.push_str(__pyi_py_traffic_class_ref());
    s.push('\n');
    s.push_str(__pyi_py_transmission());
    s.push('\n');
    s.push_str(__pyi_py_transmission_spring());
    s.push('\n');
    s.push_str(__pyi_py_tree());
    s.push('\n');
    s.push_str(__pyi_py_urdf_compat_comp());
    s.push('\n');
    s.push_str(__pyi_py_urdf_compat_joint());
    s.push('\n');
    s.push_str(__pyi_py_visual());
    s.push('\n');
    s.push_str(__pyi_py_wheel_surface());
    s.push('\n');
    s.push_str(__pyi_py_world_route_frame());
    s.push('\n');
    s.push_str(__pyi_py_functional_endpoint_choice());
    s.push('\n');
    s.push_str(__pyi_py_hop_owner_choice());
    s.push('\n');
    s.push_str(__pyi_py_model_root_choice());
    s.push('\n');
    s.push_str(__pyi_py_physical_endpoint_choice());
    s.push('\n');
    s.push_str(__pyi_py_physical_owner_choice());
    s.push('\n');
    s.push_str(__pyi_py_placement_rotation_choice());
    s.push('\n');
    s.push_str(__pyi_py_representation_choice());
    s.push('\n');
    s.push_str(__pyi_py_rf_channel_selection_choice());
    s.push('\n');
    s.push_str(__pyi_py_route_frame_choice());
    s.push('\n');
    s.push_str(__pyi_py_route_section_choice());
    s.push('\n');
    s.push_str(__pyi_py_selection_quantity_choice());
    s.push('\n');
    s.push_str(__pyi_py_topology_segment_choice());
    s.push('\n');
    s
}
