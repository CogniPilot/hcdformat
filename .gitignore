tools/hcdf-cli/target/
tools/hcdf-cli/Cargo.lock
__pycache__/
build/
dist/
*.egg-info/
*.pyc
log/
# Rust build trees + the maturin binding wheel build
rust/hcdformat-rs/target/
rust/hcdformat-py/target/
rust/hcdformat-pyderive/target/
rust/hcdformat-domgen/target/
rust/xacro-pure/target/
# Inplace dev native binding (built into the stub package by `maturin develop`; the wheel carries its
# own copy). The committed contents of that directory are the generated `hcdf/*.pyi` stubs + `py.typed`.
rust/hcdformat-py/hcdf/*.so
.cargo/config.toml
