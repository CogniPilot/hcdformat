"""hcdf — shared support modules for the HCDF toolchain.

Holds the mesh asset pipeline (``assets``) and the XML/JSON converter (``convert``) as a proper
package, so they do not occupy generic top-level import names once installed.
"""
